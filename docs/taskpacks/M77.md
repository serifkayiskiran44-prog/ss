# M77 XML delta, scheduler ve stok koruması

REAL_WORK_COUNT: 1
VERIFICATION_ONLY_COUNT: 9
FILES_CHANGED: XmlRunStore.cs, docs/taskpacks/M77.md, TODO.md, IMPLEMENTATION_STATUS.md
TESTS_ADDED_OR_CHANGED: Release build verification; external test project is not present in this clone

- XML run history now enforces one `Running` run per source with a SQLite partial unique index, closing the check-then-insert race.
- Constraint conflicts are converted to the same safe user-facing duplicate-run error; existing counters, retry history and local stock transaction boundaries remain unchanged.
- No live marketplace write or XML variant mapping was introduced.
