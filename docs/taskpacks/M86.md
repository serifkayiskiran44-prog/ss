# M86 — Sipariş ingestion ve normalizasyon

REAL_WORK_COUNT=3
VERIFICATION_ONLY_COUNT=7
FILES_CHANGED=OrdersModels.cs; OrdersEtsyClient.cs; docs/taskpacks/M86.md; TODO.md; IMPLEMENTATION_STATUS.md
TESTS_ADDED_OR_CHANGED=Yok; harici MonoBridgeDesktop.Tests projesi bu klonda bulunmuyor.
TEST_RESULT=Release build ve self-contained publish başarılı; canlı sipariş write testi yapılmadı.

- Etsy receipt ingestion sonrası merkezi `OrderNormalizer` bounded metin, lowercase status, uppercase/3 karakter currency, shipment state ve source alanlarını normalize eder.
- Kaynak `updated_timestamp` `SourceUpdatedAt` olarak ayrılır; `LastSync` yerel alım zamanıdır ve `UpdatedAt` stale merge için kaynak zamanını korur.
- Receipt/order duplicate kimliği ve `(marketplace, shop, order)` SQLite idempotency, pagination/since-last-modified ve eksik SKU’nun exception reconciliation akışı korunmuştur; müşteri PII alanı eklenmemiştir.
