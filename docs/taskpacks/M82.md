# M82 — Çoklu mağaza izolasyonu

REAL_WORK_COUNT=3
VERIFICATION_ONLY_COUNT=7
FILES_CHANGED=Catalog/SyncStore.cs; Catalog/AutomationRunner.cs; docs/taskpacks/M82.md; TODO.md; IMPLEMENTATION_STATUS.md
TESTS_ADDED_OR_CHANGED=Yok; harici test projesi bu klonda yok.
TEST_RESULT=Release build başarılı; canlı marketplace write testi `LIVE_API_BLOCKED`.

- Sync request/job artık `ShopId` taşır; idempotency lookup ve unique index kanal+mağaza+işlem+varlık+sürüm anahtarını kullanır.
- Restart/upgrade sırasında eski `UX_SyncJobs_Key` kaldırılıp mağaza kapsamlı unique index yeniden kurulur.
- Otomasyon kuyruğu XML/health/sync/stock/price işlerinde shop bağlamını kaybetmeden enqueue eder.
