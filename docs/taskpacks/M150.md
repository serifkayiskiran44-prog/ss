# M150 — Fatura merkezi

Issue #188 gerçek teslimatı: sağlayıcıdan bağımsız fatura önizleme ve Trendyol fatura linki güvenlik çekirdeği.

- `InvoiceCenter` Trendyol alanlarını HTTPS, kapsam, yaş ve deterministic idempotency anahtarıyla doğrular.
- 200/201, 400, 401, 409 ve 5xx yanıtları sınıflandırılır; tekrar gönderim engellenir.
- NES/FAST için doğrulanmamış canlı sözleşme `LIVE_API_BLOCKED`, eksik ayar `NOT_CONFIGURED` kalır; HTTP/live write yoktur.
- Testler wrong-shop/order, stale preview, duplicate ve 401/503 fixture davranışını kapsar.
