# M240 — Windows CI ve clean-clone kanıtı

Issue #278 için repo içi gerçek test projesini kullanan Windows Release test/publish workflow’u eklendi. Workflow aynı checkout SHA’da restore, TRX test sonucu, self-contained win-x64 publish ve artifact yükleme yapar; credential veya marketplace write içermez.

REAL_WORK_COUNT=1
VERIFICATION_ONLY_COUNT=9
FILES_CHANGED=.github/workflows/windows-release-evidence.yml;AGENTS.md;docs/taskpacks/M240.md;TODO.md;IMPLEMENTATION_STATUS.md
TESTS_ADDED_OR_CHANGED=Windows CI workflow TRX discovery
TEST_RESULT=LOCAL 93/93; GitHub runner execution pending
PUBLISH=Windows-M240-CI-Evidence
BLOCKER=CI_RUNNER_CONSTRAINT: GitHub Windows runner sonucu bu local ortamda çalıştırılamadı.
