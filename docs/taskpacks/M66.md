# M66 Performance / concurrency (Issue #101)

REAL_WORK_COUNT: 3
VERIFICATION_ONLY_COUNT: 7
FILES_CHANGED: OrdersStore.cs, Catalog/SyncStore.cs, docs/taskpacks/M66.md, TODO.md, IMPLEMENTATION_STATUS.md
TESTS_ADDED_OR_CHANGED: Release regression suite; restart-recovery and SQLite concurrency paths require targeted coverage in the external test project

- SQL-side filtering, count, ordering and LIMIT/OFFSET replaced full ReadAll materialization in `OrdersStore.ReadPage`.
- Search remains parameterized and marketplace/shop/status filters are preserved.
- Orders and sync SQLite connections use WAL, bounded busy timeout, pooling, and targeted indexes for concurrent read/write workloads.
- `SyncStore.RecoverAbandonedRunning` requeues stale `Running` jobs after an interrupted process, while `TryStart` remains an atomic Pending claim.
- No live connector writes or deferred scopes changed.
