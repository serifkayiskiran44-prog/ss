# M90 — API health, rate-limit ve backoff

REAL_WORK_COUNT=2
VERIFICATION_ONLY_COUNT=8
FILES_CHANGED=ApiHealth.cs; ApiHealthPanel.cs; docs/taskpacks/M90.md; TODO.md; IMPLEMENTATION_STATUS.md
TESTS_ADDED_OR_CHANGED=Yok; harici MonoBridgeDesktop.Tests projesi bu klonda bulunmuyor.
TEST_RESULT=Release build ve self-contained publish başarılı; gerçek marketplace isteği yapılmadı.

- Kullanıcı cancellation’ı artık `CANCELLED` olarak kaydedilir; `TIMEOUT`/backoff olarak yanlış sınıflandırılmaz ve UI filtresinde görünür.
- 401/403, 408, 429, 5xx ve network/exception sınıfları korunurken Retry-After yalnız 0–86400 saniye, reset yalnız doğrulanabilir aralık, rate-limit sayıları yalnız negatif olmayan değer olarak kabul edilir.
- Shop-scoped health persistence, redacted error, rate-limit snapshot ve backoff/defer gate korunmuştur; endpoint/header uydurulmamıştır.
