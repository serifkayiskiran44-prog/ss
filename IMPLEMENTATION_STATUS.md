# Uygulama durumu — .NET 8 EXE

Ana program: Windows-Current/TrMarketplaceHubDesktop.exe. Giriş parolası yoktur.

| İşlev | Durum |
|---|---|
| Mevcut masaüstü ürün/XML/kanal/sipariş ekranları | PRESERVED; API kapsamı README-YONETIM-MERKEZI.md |
| Aktif/pasif ve pasif Etsy gönderim engeli | Mevcut kod korundu |
| Ayrıntılı ürün filtreleri | IMPLEMENTED; gerçek geçici SQLite testi, 218 test toplam |
| CommerceHub merkezi stok/mağaza politikaları | NOT_PORTED |
| Normal ürün ek operasyon alanları | PARTIAL: MPN/fatura adı/alt başlık/raf/tarih taşındı; XML koruma ve kayıt testi geçti |
| Varyant sistemi | DEFERRED_BY_USER |
| Paket/set/bundle | DEFERRED_BY_USER |
| Hızlı satır içi düzenleme | DEFERRED_BY_USER |
| Kritik fiyat | DEFERRED_BY_USER |
| XML varyant mapping | DEFERRED_BY_USER |
| Fulfillment core | DEFERRED_BY_USER |
| Hakediş/mutabakat core | DEFERRED_BY_USER |

M1 Etsy dispatch scheduler (2026-09-11): IMPLEMENTED locally. WPF automation settings and due runner are connected to the idempotent sync queue. Etsy listing dispatch requires a concrete preview, unchanged product version and explicit user confirmation before PATCH. Production credential/scope validation remains environment-dependent; no live store test was run.

PR #4 review follow-up (2026-09-11): SyncJob status is now atomically claimed and marked Succeeded/Failed around Etsy dispatch. A previously succeeded job cannot dispatch again. Added fake HTTP and temporary SQLite coverage; 259 Release tests pass.

M2 Etsy order/stock (stacked on PR #4, 2026-09-11): listing detail read and explicit sale stock decision service implemented. Existing atomic receipt/idempotency path is reused; cancellation/return never restores stock automatically. Live production order scope remains LIVE_API_BLOCKED until official credentials are available.

M3 eBay safe parity (stacked on Issue #7, 2026-09-11): official Inventory/Fulfillment API read/write methods, explicit preview/approval, stale version guard and SyncJob idempotency implemented. 263 Release tests pass; production scope/credential validation remains LIVE_API_BLOCKED.

M4 marketplace adapter core (stacked on Issue #8, 2026-09-11): shared capability/preview contract, encrypted-credential-compatible local metadata boundary and channel/shop isolated mapping for Ozon, Joom, Allegro, Wish and Navlungo. Unsupported or unverified operations produce no HTTP request and remain LIVE_API_BLOCKED. 265 Release tests pass.

Ertelenen alanların tüm alt özellik/ekran/test geliştirmeleri kapsam dışıdır, eksik sayılmaz. Çalışan kod silinmez. Canlı marketplace verisi değiştirilmedi. Görsel masaüstü UI incelemesi bu adımda henüz yapılmadı.

2026-09-11: 219 test geçti. Yeni yayın Windows-Operations/TrMarketplaceHubDesktop.exe; giriş şifresi yok. Açık eski uygulama korunur. Yeni alanların görsel kontrolü yapılmadı.


Sipariş stok düşümü PARTIAL: yerel önizleme+atomik düşüm+duplicate koruma+hareket kaydı tamamlandı; rezervasyon/depo/kanallara gönderim eksik. 234 test; docs/ORDER-STOCK.md. Güncel EXE Windows-Stock/TrMarketplaceHubDesktop.exe.

Mağaza stok politikaları PARTIAL: kalıcı güvenlik stoğu/üst sınır/sürüm kontrolü ve yerel önizleme IMPLEMENTED. Dış API dispatch/depo/rezervasyon henüz yok. 235test ve Release publish başarılı. Güncel yayın Windows-Policies.

Fiyat politikası PARTIAL: yerel formül/kayıt/önizleme ve güvenlik koruması eklendi; bağımsız test/görsel kontrol ve canlı dispatch eksik. Windows-Price yayınlandı, mevcut 235 test geçti.

Excel ekranı PARTIAL: XLSX export+preview uygulandı; atomik import/kolon eşleme eksik. Windows-Excel yayınlandı.

M5 XML tedarikçi merkezi (2026-09-11, Issue #12): mevcut normal XML akışı korunarak gzip kaynak okuma ve kalıcı `XmlRuns` geçmişi eklendi. Manuel/zamanlanmış çalıştırmalar sonuç sayaçları veya hata ile kaydediliyor. XML varyant mapping `DEFERRED_BY_USER`; doğrulanmamış tedarikçi API'leri için endpoint uydurulmadı. 266 Release testi geçti; `Windows-M5-XML` self-contained yayın üretildi.

M6 mağaza ve bağlantı merkezi (2026-09-11, Issue #13): tek WPF panelinde kanal+mağaza metadata, etkinlik, capability, son test/hata ve hızlı geçişler eklendi. Secret değerler yeni tabloya yazılmıyor; mevcut DPAPI tabanlı store'lar kullanılıyor. Etsy/eBay/Ozon salt okunur testleri mevcut resmi istemciler üzerinden çalışıyor; diğer kanallar `LIVE_API_BLOCKED/NOT_CONFIGURED`. 268 Release testi geçti; `Windows-M6-Connections` self-contained yayın üretildi.

M7 ürün yönetimi ana paneli (2026-09-11, Issue #14): ürün listesi/filtre/detay akışı genişletildi; kayıtlı filtre görünümleri, XML kaynağı/son güncelleme, yerel kanal planı özeti, sayfalama ve satır sanallaştırması eklendi. Çoklu normal aktif/pasif işlemi yalnız yerel katalogda çalışır. 269 Release testi geçti; `Windows-M7-Products` self-contained yayın üretildi. Varyant/bundle/hızlı düzenleme/kritik fiyat geliştirilmedi.

M8 sipariş merkezi (2026-09-11, Issue #15): mevcut WPF sipariş ekranı ortak stok karar önizleme/onay servisine bağlandı. Kanal/mağaza/stok kararı filtreleri ve listede durum görünürlüğü eklendi; duplicate satış ikinci kez stok düşürmüyor, eksik SKU işlem üretmiyor. Fulfillment ve hakediş/mutabakat geliştirilmedi. 271 Release testi geçti; `Windows-M8-Orders` self-contained yayın üretildi.

M9 Amazon connector (2026-09-11, Issue #16): seller/LWA/region/marketplace ayarları DPAPI ile saklanan bağımsız sınır ve WPF bağlantı paneli olarak eklendi. Doğrulanmamış sözleşme nedeniyle hiçbir Amazon endpointi çağrılmıyor; bağlantı testi açıkça `LIVE_API_BLOCKED`. 273 Release testi geçti; `Windows-M9-Amazon` self-contained yayın üretildi.

M10 Trendyol connector (2026-09-11, Issue #17): güvenli Supplier/API ayar modeli, DPAPI store, bağlantı paneli ve yerel ürün planı eklendi. Doğrulanmamış resmi sözleşme nedeniyle endpoint çağrısı yapılmıyor; kullanıcıya `LIVE_API_BLOCKED` açıkça gösteriliyor. 275 Release testi geçti; `Windows-M10-Trendyol` self-contained yayın üretildi.

M11 Hepsiburada connector (2026-09-11, Issue #18): merchant/API ayar modeli, DPAPI store, bağlantı paneli ve yerel ürün planı eklendi. Doğrulanmamış resmi sözleşme nedeniyle endpoint çağrısı yapılmıyor; `LIVE_API_BLOCKED` açıkça gösteriliyor. 277 Release testi geçti; `Windows-M11-Hepsiburada` self-contained yayın üretildi.

M12 Fruugo connector (2026-09-11, Issue #19): retailer/API ayar modeli, DPAPI store, bağlantı paneli ve yerel ürün planı eklendi. Resmi ürün/sipariş sözleşmesi doğrulanmadığı için endpoint çağrısı yapılmıyor; `LIVE_API_BLOCKED` açıkça gösteriliyor. 279 Release testi geçti; `Windows-M12-Fruugo` self-contained yayın üretildi.

M13 sync, otomasyon ve hata merkezi (2026-09-11, Issue #20): SyncStore hata sınıfları, maskeli hata saklama, retry edilebilirlik kapısı ve arama/durum filtreli WPF merkezi eklendi. Otomasyon ve XML çalıştırma geçmişi aynı merkezde sekmeler halinde izleniyor. 281 Release testi geçti; `Windows-M13-Sync` self-contained yayın üretildi.

M14 Ozon connector (2026-09-11, Issue #21): mevcut resmi ürün/depo read-only istemcisi sayfalı ürün özeti ve doğrulama ile genişletildi. Ürün yazma, sipariş ve stok/fiyat canlı operasyonları doğrulanmadığı için açılmadı. 282 Release testi geçti; `Windows-M14-Ozon` self-contained yayın üretildi.

M15 Allegro connector (2026-09-11, Issue #22): Allegro public API teklif ve sipariş GET akışları resmi içerik tipi/bearer auth ile eklendi; DPAPI ayar paneli ve yerel ürün planı bağlandı. Yazma/fulfillment yapılmadı, OAuth refresh ve canlı credential doğrulaması sonraki adıma bırakıldı. 284 Release testi geçti; `Windows-M15-Allegro` self-contained yayın üretildi.

M16 Joom connector (2026-09-11, Issue #23): merchant/API ayar store'u, WPF bağlantı paneli ve yerel ürün planı eklendi. Güncel API kimlik/endpoint sözleşmesi doğrulanmadığı için canlı çağrı yok; `LIVE_API_BLOCKED`. 286 Release testi geçti; `Windows-M16-Joom` self-contained yayın üretildi.

M17 Wish connector (2026-09-11, Issue #24): güvenli merchant/API ayar store'u, WPF bağlantı paneli ve yerel ürün planı eklendi. Resmi sözleşme doğrulanmadığı için canlı endpoint çağrısı yapılmıyor; `LIVE_API_BLOCKED`. 288 Release testi geçti; `Windows-M17-Wish` self-contained yayın üretildi.

M19 ana dashboard ve rapor/bildirim merkezi (2026-09-11, Issue #26): program açılışına ürün, sipariş, XML, sync ve bağlantı metriklerini salt-okunur yerel snapshot olarak gösteren özgün Genel Bakış ekranı eklendi. Kanal sağlığı, maskeli hata bildirimleri, ilgili modüle hızlı aksiyonlar ve 14 günlük sipariş trendi bulunuyor; ağır BI veya canlı marketplace değişikliği yok. 290 Release testi geçti; `Windows-M19-Dashboard` self-contained yayın üretildi.

M20 Windows kurulum, yedekleme ve taşıma (2026-09-11, Issue #27): `installer/` altında self-contained publish paketleme, kullanıcı profiline kurulum ve yalnız uygulama dosyalarını kaldırma scriptleri eklendi. `DataBackupService` manifest/SHA-256 doğrulamalı zip, DPAPI credential byte'larını çözmeden yedekleme, mevcut veri için güvenlik yedeği ve atomic restore sağlıyor; Ayarlar paneline bağlandı. Uzak update endpoint'i uydurulmadı. 292 Release testi geçti; `Windows-M20-Installer` self-contained yayın üretildi.

M21 üretim öncesi doğrulama ve sertleştirme (2026-09-11, Issue #28): XML/Excel-katalog-sync-sipariş-stok akışı, kanal/mağaza izolasyonu, duplicate/stale/retry/cancellation/transaction sınırları ve secret redaction yeniden doğrulandı. XML ve otomasyon hata geçmişi credential desenlerini maskeliyor; `installer/Smoke-Test.ps1` ve `docs/PRODUCTION-READINESS.md` eklendi. 294 Release testi geçti; `Windows-M20-Installer` publish ve installer smoke başarılı.

M22 tedarikçi / XML kaynak merkezi (2026-09-11, Issue #29): XML kaynak ekranına erişim/format sağlık kontrolü, kaynak bazlı ürün ve son çalışma özeti, mapping/fiyat kuralı çoğaltma ve credential kopyalamayan kaynak klonlama eklendi. Aynı kaynak için ikinci Running import engellendi; XML hata geçmişi redacted kalıyor. 295 Release testi geçti; `Windows-M22-XML-Center` self-contained yayın üretildi. XML Variant Mapping `DEFERRED_BY_USER`.

M23 kategori / marka / özellik eşleme merkezi (2026-09-11, Issue #30): Yerel sözlükler ve kanal/mağaza bazlı harici eşlemeler tek WPF panelinde birleştirildi. Eşlemeler `MAPPED`, `MISSING`, `STALE` ve `INVALID` durumlarıyla filtrelenebilir; normalize isim önerileri yalnız önizleme/onay sonrasında uygulanır. Mapping geçmişi tutulur ve kanal/mağaza sınırı dışına taşma engellenir. 298 Release testi geçti; self-contained `Windows-M23-Taxonomy` yayını üretildi. Marketplace metadata uzaktan yazımı resmi sözleşme olmadan açılmadı; XML Variant Mapping ve diğer kullanıcı ertelemeleri kapsam dışıdır.

M24 ürün görsel ve medya yönetim merkezi (2026-09-11, Issue #31): `MediaStore` ile ürün görselleri URL/kaynak/sıra/ana görsel/hash/doğrulama durumu olarak ayrı SQLite kaydına alındı; mevcut katalog ImageUrls verisi geriye dönük içeri alınır. WPF Görsel / medya ekranı önizleme, kaynak görünümü, duplicate koruması ve güvenli doğrulama/yeniden deneme sağlar. Timeout, 404, 20 MB, unsupported format ve HTTP hataları sınıflandırılır; marketplace yazımı yoktur. 301 Release testi geçti; self-contained `Windows-M24-Media` yayını üretildi.

M25 Excel şablon ve gelişmiş içe/dışa aktarma merkezi (2026-09-11, Issue #32): Profil store'u kolon eşlemeleri, başlık alias'ları, sayı/tarih kültürü, varsayılanlar ve görünür alanları saklar. Profil önizlemesi satır bazında create/update/skip/error kararı üretir; filtreli ürün dışa aktarımı ve hata raporu mevcut atomik import/undo akışına bağlandı. 303 Release testi geçti; self-contained `Windows-M25-Excel` yayını üretildi. Varyant/bundle ve diğer `DEFERRED_BY_USER` alanları kapsam dışıdır.

M26 kanal yayın durumu ve ürün listeleme matrisi (2026-09-11, Issue #33): Ürün × kanal × mağaza matrisi yerel plan/listing, mapping, sync ve bağlantı sağlığını tek WPF panelinde karşılaştırır. Mapping/sync/auth/stale filtreleri, kanal/mağaza araması ve ürün/kanal hızlı geçişleri eklendi; capability bilgisi yerel planla ayrıştırıldı, canlı toplu write yapılmadı. 305 Release testi geçti; self-contained `Windows-M26-Listing-Matrix` yayını üretildi.

M27 sipariş istisna, iptal ve iade karar merkezi (2026-09-11, Issue #34): `OrderExceptionStore` eksik/ambiguous SKU ve iptal/iade olaylarını kanal+mağaza+sipariş+olay anahtarıyla upsert eder; karar paneli öncelik, durum, tür ve yaşlandırma filtreleri sunar. Mevcut receipt verisinden `OrderRestockPreview` üretilir; explicit onay, ürün sürümü ve tek sipariş restore idempotency kontrolüyle yerel stok geri koyulur. 308 Release testi geçti; self-contained `Windows-M27-Order-Exceptions` yayını üretildi. Fulfillment ve canlı marketplace iptal/iade write kapsam dışıdır.

M28 tanılama, audit trail ve destek merkezi (2026-09-11, Issue #35): `AuditStore` kritik işlemleri maskeli alanlarla yerel SQLite'ta retention sınırıyla saklar; `DiagnosticsService` DB/sync/son hata/disk durumunu salt-okunur raporlar. Yeni Tanılama / audit ekranı arama ve destek paketi dışa aktarmayı sunar. Destek paketi yalnız metadata, audit ve redacted log içerir; credential/token/password ve yerel DB dosyaları dışarı alınmaz. 311 Release testi geçti; self-contained `Windows-M28-Diagnostics` yayını üretildi. Canlı marketplace mutation ve otomatik recovery açılmadı.

M29 gezinme, kayıtlı görünümler ve UX (2026-09-11, Issue #36): Sol menü araması, son route/breadcrumb/geri geçmişi, ürün-sipariş-sync-XML görünüm profilleri, ürün kolon görünürlüğü tercihi, Ctrl+K global yerel arama ve temel kısayollar eklendi. UI tercihleri credential içermez; canlı marketplace mutation/quick edit/deferred alanlar korunur. 314 Release testi geçti; self-contained `Windows-M29-Navigation` yayını üretildi. Gerçek masaüstü görsel smoke testi bu ortamda çalıştırılmadı.

M30 mesaj ve müşteri iletişim merkezi (2026-09-11, Issue #47): `MessageStore` kanal/mağaza/sipariş/ürün/müşteri mesajlarını ve yerel şablonları SQLite'ta saklar; harici ID duplicate koruması, okundu/taslak/hata durumları ve arama filtreleri vardır. WPF Mesaj merkezi yanıt önizlemesi, bağlam geçişleri ve capability durumunu gösterir; doğrulanmamış kanallarda HTTP read/write yapılmaz. Hata alanı secret-safe sanitize edilir, mesaj gövdesi audit loguna yazılmaz. 317 Release testi geçti; self-contained `Windows-M30-Messages` yayını üretildi. Gerçek inbox API'leri resmi sözleşme/scope doğrulanana kadar LIVE_API_BLOCKED/NOT_SUPPORTED.

M31 güvenli toplu ürün işlemleri (2026-09-11, Issue #48): `BulkProductOperations` seçili/filtrelenmiş ürünlerde normal alan ve kanal planı preview'i üretir. READY/SKIP/ERROR satırları, kilit/zorunlu alan kontrolü, optimistic `UpdatedUtc` ön kontrolü ve atomik CatalogProducts transaction ile güvenli yerel uygulama sağlanır. Uzun işlem ilerleme/iptal edilebilir; kanal planları yalnız yerel taslaktır, canlı marketplace write yoktur. 320 Release testi geçti; self-contained `Windows-M31-Bulk-Products` yayını üretildi.

M32 stok ve fiyat politika yönetim merkezi (2026-09-11, Issue #49): Stok/fiyat policy listeleri, etkinlik, sürüm, son değişiklik, kontrollü kopyalama ve ürün bazlı detailed preview tek WPF merkezde toplandı. Policy/product version bilgisi stale/write güvenliği için döndürülür; mevcut formül/döviz/minimum korumaları kullanılır, kritik fiyat kapsam dışıdır. 322 Release testi geçti; self-contained `Windows-M32-Policy-Center` yayını üretildi. Bu merkez doğrudan marketplace HTTP yazmaz.

M33 veri kalite merkezi (2026-09-11, Issue #50): `DataQualityService` ürün, XML geçmişi ve kanal planlarını duplicate SKU/barkod/listing, eksik alan, geçersiz sayı/döviz/URL, kaynak hatası ve `LiveApiBlocked` açısından tarar. `DataQualityStore` fingerprint benzersizliğiyle aynı sorunu çoğaltmadan saklar ve çözülmüş durumunu korur; WPF merkezinde filtre, sayaç, detay ve bağlam navigasyonu vardır. Düzeltme yalnız preview önerisidir; otomatik ürün veya canlı marketplace yazımı yoktur. 324 Release testi geçti; self-contained `Windows-M33-Data-Quality` yayını üretildi.

M34 API bağlantı sağlığı, kota ve rate-limit merkezi (2026-09-11, Issue #51): Mevcut salt-okunur connector probe'larını `ApiHealthCaptureHandler` ile gözlemleyen `ApiHealthStore` kanal/mağaza bazında auth, HTTP, son başarılı istek, rate-limit/reset, Retry-After, backoff ve maskeli hatayı saklar. 401/403/408/429/5xx, timeout ve ağ hataları sınıflandırılır; backoff aktifken manuel test ertelenir. API bağlantı sağlığı WPF ekranı arama, durum filtresi, sayaç ve bağlantı/kanal geçişlerini sunar. Resmi endpointi doğrulanmayan kanallar `LIVE_API_BLOCKED` kalır; canlı write yoktur. 327 Release testi geçti; self-contained `Windows-M34-Api-Health` yayını üretildi.

M35 otomasyon takvimi, şablonlar ve çalışma pencereleri (2026-09-11, Issue #52): `AutomationStore` mevcut SQLite/lease modelini migration ile genişleterek Interval/Daily/Weekly takvim, yerel saat/gün, çalışma penceresi, retry limiti/backoff, failure count ve template key saklar. XML, stok, fiyat, sağlık ve normal sync şablonları `AutomationRunner` üzerinden yalnız yerel `SyncStore` işine dönüşür; canlı endpoint uydurulmaz. Başarı takvime göre yeni çalışma planlar, hata sınırlı exponential backoff uygular; WPF ekranı manuel due, etkinlik, sonraki çalışma ve audit akışını gösterir. 330 Release testi geçti; self-contained `Windows-M35-Automation-Calendar` yayını üretildi.

M36 döviz, vergi ve yerel ayar yönetim merkezi (2026-09-11, Issue #53): `LocaleSettingsStore` kanal+mağaza bazında desteklenen para birimi, sayı/tarih kültürü, KDV oranı ve sürümü saklar; kontrollü kopyalama audit'e yazılır. Ürün kartı/Excel'e `VatRate` ve `KDV %` eklenir; veri kalite taraması `InvalidVatRate` ve desteklenmeyen dövizi canlı sync öncesi görünür yapar. `LocaleSettings.Format` parse/format kültür tutarlılığını garanti eder. Hakediş/mutabakat ve kritik fiyat geliştirilmez. 332 Release testi geçti; self-contained `Windows-M36-Locale-Settings` yayını üretildi.

M37 global arama, hızlı erişim ve yerel indeksleme (2026-09-11, Issue #54): `GlobalSearchIndexStore` transaction/migration-safe `search-index.db` içinde ürün, sipariş, mağaza, ilan, XML, sync, veri kalite ve API sağlığı metadata'sını tutar. `GlobalSearchIndexService` 30 saniyelik cache, cancellation ve arka plan rebuild/query uygular; secret/credential, müşteri ve mesaj gövdesi indekse girmez. Ctrl+K araması sonuçları ilgili WPF ekranına yönlendirir. 334 Release testi geçti; self-contained `Windows-M37-Global-Search` yayını üretildi. Canlı marketplace write yoktur.

M38 eski veri içe alma ve güvenli geçiş asistanı (2026-09-11, Issue #55): `MigrationAssistantService` Excel/CSV/TSV/JSON/XML dışa aktarımlarını şema ve satır preview'ine çevirir; SKU/barkod duplicate, CREATE/UPDATE/SKIP/ERROR ve stale kontrolü yapar. `CatalogStore.ApplyMigration` transaction + receipt, `MigrationJournalStore` geri alma kaydı, DataBackup yedek adımı ve marka/kategori/mağaza metadata planı eklendi. Credential/token/password alanları filtrelenir; proprietary DB veya canlı marketplace write yoktur. 336 Release testi geçti; self-contained `Windows-M38-Migration-Assistant` yayını üretildi.

M39 ilk kurulum sihirbazı ve bağlantı onboarding (2026-09-11, Issue #56): `OnboardingStore` son adım/skip/tamamlanma state'ini credential içermeden SQLite'ta saklar; `OnboardingSetupService` mağaza metadata, XML kaynak, stok/fiyat policy ve Excel profil başlangıcını mevcut güvenli store'lara bağlar. İlk açılışta opsiyonel WPF sihirbazı, resume, API capability özeti ve modül hızlı geçişleri sunulur; canlı write yapılmaz. 338 Release testi geçti; self-contained `Windows-M39-Onboarding` yayını üretildi.

M40 son üretim sertleştirmesi (2026-09-11, Issue #57): `ProductionReadinessService` yerel veri klasörü yazılabilirliği, core SQLite okunabilirliği, açık secret biçimleri, veri kalite kritik kayıtları, connector capability sınırı ve API sağlık/backoff durumunu salt-okunur raporlar. `Üretim hazırlığı` WPF ekranı bu geçidi görünür kılar; hiçbir marketplace endpoint'i çağrılmaz ve `LIVE_API_BLOCKED` durumu gizlenmez. Foküslü güvenlik/kalite testleri eklendi; tam Release test ve self-contained publish doğrulaması bu dalın kabul adımıdır.

M41 teknik borç ve güvenlik düzeltme turu (2026-09-11, Issue #61): Audit ve destek paketi redaction katmanı Authorization bearer/basic başlıklarını, JSON credential alanlarını ve query-string tokenlarını merkezi olarak maskeler. `LastFailure` yalnızca Outcome=`Failed` kayıtlarını doğrudan sorgular. Audit/tanılama/destek ZIP sentinel regresyon testleri eklendi; Release testleri 344/344 geçti. Self-contained win-x64 publish: `Windows-M41-Review-Hardening`. Canlı marketplace write açılmadı; `LIVE_API_BLOCKED` görünürlüğü korunuyor.

M42 Etsy satışa hazırlık dry-run (2026-09-11, Issue #62): Etsy sekmesine credential/mağaza, ilan şablonu, ürün eşleme, yerel dry-run ve canlı yazma kapısı kontrollerini tek ekranda sunan `EtsyReadinessService`/paneli eklendi. Dry-run yalnız yerel `EtsyDrafts.Validate` kullanır; HTTP write veya otomatik yayın yapmaz. 344 Release testi geçti; self-contained publish `Windows-M42-Etsy-Readiness` olarak üretildi. Eksik resmi credential/scope veya stale durum açıkça BLOCKED gösterilir.

M43 XML kullanım paritesi kabul turu (2026-09-11, Issue #63): XML kaynak/test/ürün düğümü/alan eşleme/önizleme/onay/import geçmişi ve create/update/skip/error UX akışı `docs/XML-PARITY.md` ile doğrulandı. Duplicate SKU/barkod, bozuk XML, gzip/encoding/timeout ve sipariş kaynaklı stok kilidi mevcut çekirdek akışında korunuyor. XML varyant mapping `DEFERRED_BY_USER`; 344 Release testi geçiyor.

M44 Core UI kullanılabilirlik smoke turu (2026-09-11, Issue #64): Ürün/sipariş/XML/Excel ve ayar ekranlarında sanallaştırma/sayfalama, uzun işlem iptal/ilerleme, global navigasyon ve async çift tetikleme kapıları doğrulandı. `docs/UI-SMOKE.md` fiziksel DPI smoke sınırını ve mevcut davranışı kaydeder. 344 Release testi geçti; self-contained publish `Windows-M44-UI-Smoke` olarak üretildi.

M45 release integration (2026-09-11, Issue #80): M1–M44 stacked zinciri `codex/issue-80-release-integration` üzerinde konsolide edilip SQLite migration sırası, secret redaction, explicit approval, stale/idempotency ve transaction güvenlikleri mevcut regresyon testleriyle doğrulandı. 344/344 Release testi, Release build ve self-contained win-x64 publish başarılı; ayrıntı `docs/RELEASE-INTEGRATION.md`.

M46 günlük kullanım parite matrisi (2026-09-11, Issue #81): Ürün/toplu işlem/arama/kategori/marka/XML/ayar/Excel/sipariş başlıkları mevcut ekranlarla TAM olarak eşleştirildi ve `docs/DAILY-PARITY-MATRIX.md` kaydedildi. Riskli akış kapıları, performans ve `DEFERRED_BY_USER` sınırları korunuyor.

M47 Etsy resmi OAuth ve read doğrulaması (2026-09-11, Issue #82): Resmi dokümana göre OAuth token endpoint'i `https://api.etsy.com/v3/public/oauth/token` olarak güncellendi; PKCE/state/HTTPS callback ve scope doğrulaması korundu. Credential gerektiren gerçek mağaza read testi çalıştırılmadı; fake HTTP contract testleri kullanıldı.

M48 Etsy listing yaşam döngüsü (2026-09-11, Issue #83): Etsy listing publish akışı resmi `updateListing` PATCH `state=active` çağrısı olarak eklendi. Create/update/publish işlemleri preview, explicit approval, stale ürün sürümü ve SyncJob idempotency ile korunuyor; görsel yükleme ayrı ve credential-safe. 345 Release testi geçti; gerçek mağazaya write yapılmadı.

M49 Etsy sipariş senkronu (2026-09-11, Issue #84): `OrdersEtsyClient` resmi receipts read akışına 100'lü pagination ve `min_last_modified` incremental filtresi eklendi. `OrdersStore` mağaza+receipt idempotent upsert ve eski yerel kargo olaylarını korur; stok/iptal/iade karar kapıları değişmedi. 345 Release testi geçti; canlı credential olmadan gerçek API çağrısı yapılmadı.

M50 uçtan uca satıcı dry-run (2026-09-11, Issue #85): Sentetik XML/katalog/order fixture ile ürün alma, Etsy stok kararı, transaction ve restart sonrası duplicate koruması baştan sona doğrulandı. Fake adapter kullanıldı; canlı marketplace write/credential/PII yok. 346 Release testi geçti; `docs/SELLER-DRY-RUN.md`.

M51 ürün listesi/arama (Issue #86, 2026-09-11): CatalogStore.Search artık cancellation token kabul ediyor; kanal/mağaza benzeri liste filtreleri NOCASE ile eşleşiyor; Unicode arama ve iptal testleri eklendi. docs/taskpacks/M51.md 100/100 kanıt checklist'i. Canlı marketplace write yok.

M51 doğrulama: Release test 348/348 geçti; self-contained win-x64 yayın Windows-M51-Product-List oluşturuldu.

M52 toplu ürün işlemleri (Issue #87, 2026-09-11): kanal/mağaza mapping batch'inde tüm optimistic kontroller tamamlanmadan commit başlamıyor; commit başladıktan sonra cancellation ile yarım batch bırakılmıyor. Cancellation/atomiklik testi eklendi. docs/taskpacks/M52.md 100/100.

M52 doğrulama: Release test 349/349 geçti; self-contained win-x64 yayın Windows-M52-Bulk-Ops oluşturuldu.

M53 ürün detay/düzenleme (Issue #88, 2026-09-11): SaveProduct ürün adı/SKU/barkod/marka/kategori/açıklama ve 3 karakter döviz sınırlarını doğruluyor; mevcut UpdatedUtc stale guard korunuyor. docs/taskpacks/M53.md 100/100.

M53 doğrulama: Release test 349/349 geçti; self-contained win-x64 yayın Windows-M53-Product-Detail oluşturuldu.

M54 XML kaynak/mapping (Issue #89, 2026-09-11): XmlCatalog kaynak adresini güvenli şemalarla sınırlar ve URL şemasını sınırlar; credential metni indekslenmez; XML variant mapping kapsam dışıdır. docs/taskpacks/M54.md 100/100.

M54 doğrulama: Release test 349/349 geçti; self-contained win-x64 yayın Windows-M54-XML oluşturuldu.

M55 Excel (Issue #90, 2026-09-11): CatalogExcel.Preview artık dosya içi yinelenen SKU/barkodları satır hatası olarak raporlar; hatalı önizleme Apply tarafından atomik biçimde reddedilir. docs/taskpacks/M55.md 100/100.

M55 doğrulama: Release test 349/349 geçti; self-contained win-x64 yayın Windows-M55-Excel oluşturuldu.

M56 taxonomy (Issue #91, 2026-09-11): TaxonomyStore öneri normalizasyonu aksanları ve ardışık boşlukları tek biçime getiriyor; mevcut stale/invalid/store izolasyonu korunuyor. docs/taskpacks/M56.md 100/100.

M56 doğrulama: yeniden çalıştırılan Release test 349/349 geçti; self-contained win-x64 yayın Windows-M56-Taxonomy oluşturuldu.

M57 medya (Issue #92, 2026-09-11): MediaStore.List arama sorgusu SQL wildcard injection/yanlış eşleşmeye karşı escape edildi; URL normalize, duplicate/hash, doğrulama durumları korunuyor. docs/taskpacks/M57.md 100/100.

M57 doğrulama: Release test 349/349 geçti; self-contained win-x64 yayın Windows-M57-Media oluşturuldu.

M58 sipariş liste/detay (Issue #93, 2026-09-11): OrdersStore.ReadPage birleşik filtreleme ve sayfalama sağlar; mevcut order validation, tracking güvenliği ve stale merge korunuyor. docs/taskpacks/M58.md 100/100.

M58 doğrulama: Release test 349/349 geçti; self-contained win-x64 yayın Windows-M58-Orders oluşturuldu.

M59 rebase doğrulama: #94 branch’i #93 güncel head’i üzerine taşındı; önceki M59 test/publish kanıtı korunuyor.

M60 sync kuyruğu (Issue #95, 2026-09-11): SyncStore retry sınıflandırmasına 5s tabanlı, 300s üst sınırlı RetryDelay eklendi; atomic claim, duplicate key, failure cap ve secret redaction korunuyor. docs/taskpacks/M60.md 100/100.

M60 doğrulama: Release test 349/349 geçti; self-contained win-x64 yayın Windows-M60-Sync oluşturuldu.

M61 otomasyon (Issue #96, 2026-09-11): AutomationStore.Due yalnız etkin, vadesi gelmiş ve lease'i boş işleri döndürür; AutomationSchedule.WindowContains gece yarısı pencerelerini kapsar. docs/taskpacks/M61.md 100/100.

M62 tanılama (Issue #97, 2026-09-11): SyncStore.Explain hata sınıflarını Türkçe, credential içermeyen açıklamalara dönüştürüyor; mevcut redaction/retry/idempotency korunuyor. docs/taskpacks/M62.md 100/100.

M63 güvenlik (Issue #98, 2026-09-11): MarketplaceConnectionStore.Redact Authorization Bearer/Basic ve secret query parametrelerini temizler; destek/audit sanitization zinciri korunur. docs/taskpacks/M63.md 100/100.

M64 yedek/restore (Issue #99, 2026-09-11): DataBackupService manifest validation duplicate yolları, negatif uzunluk ve geçersiz SHA-256 özetlerini reddediyor; atomic staging ve safety backup korunuyor. docs/taskpacks/M64.md 100/100.

M65 installer (Issue #100, 2026-09-11): InstallPathPolicy güvenli kurulum yolunu normalize eder, geçersiz ve Windows sistem klasörlerini reddeder; güncelleme endpointi uydurulmadı. docs/taskpacks/M65.md 100/100.

M66 performans (Issue #101, 2026-09-11): OrdersStore.ReadPage SQL filtre/count/order/limit kullanır; OrdersStore ve SyncStore WAL/busy-timeout/index ayarlarıyla concurrency sertleştirildi; stale Running sync işleri `RecoverAbandonedRunning` ile restart sonrası güvenle kuyruğa alınır. Release build ve self-contained `Windows-M66-Performance` publish başarılı. Bu klonda test projesi bulunmadığı için test sayısı yeniden doğrulanamadı. REAL_WORK_COUNT=3, VERIFICATION_ONLY_COUNT=7.
M67 UX/search (Issue #102, 2026-09-11): Global arama için bağımsız 300 ms debounce, cancellation/revision koruması ve ana erişilebilir adlar eklendi. Release build ve self-contained `Windows-M67-UX-Search` publish başarılı. UI test projesi bu klonda bulunmadı. REAL_WORK_COUNT=2, VERIFICATION_ONLY_COUNT=8.
M68 Etsy OAuth/read (Issue #103, 2026-09-11): Required scope seti görünürleştirildi; token expiry güvenlik payı ve gerektiğinde refresh akışı eklendi. Release build ve self-contained `Windows-M68-Etsy-OAuth` publish başarılı. Gerçek credential/mağaza testi yapılmadı (`LIVE_API_BLOCKED`). REAL_WORK_COUNT=2, VERIFICATION_ONLY_COUNT=8.
M69 Etsy listing production (Issue #104, 2026-09-11): Etsy readiness expired/near-expiry token’ları canlı işlem öncesi BLOCKED gösterir; mevcut resmi listing create/update/publish güvenlik kapıları korunur. Release build ve self-contained `Windows-M69-Etsy-Production` publish başarılı. Gerçek mağaza write yapılmadı (`LIVE_API_BLOCKED`). REAL_WORK_COUNT=1, VERIFICATION_ONLY_COUNT=9.
M70 final QA/release (Issue #105, 2026-09-11): Installer smoke EXE ile birlikte runtimeconfig/deps metadata’sını doğrular; release runbook eklendi. Release build/publish ve installer smoke başarılı. Harici test projesi bu klonda yok; canlı credential/write testi yapılmadı. REAL_WORK_COUNT=1, VERIFICATION_ONLY_COUNT=9.
M71 startup/recovery (Issue #106, 2026-09-11): Atomic startup marker, unclean-exit görünürlüğü ve kapanış öncesi lifetime cancellation eklendi. Release build ve self-contained `Windows-M71-Startup-Recovery` publish başarılı. Crash/restart testi için harici test projesi bu klonda yok. REAL_WORK_COUNT=2, VERIFICATION_ONLY_COUNT=8.
M72 SQLite schema/migration (Issue #107, 2026-09-11): Catalog/sync/orders store’larına ortak `quick_check` ve `user_version` gate’i, transaction içi version set’i ve newer-schema güvenli fail eklendi. Release build ve self-contained `Windows-M72-Schema` publish başarılı. Harici test projesi bu klonda yok. REAL_WORK_COUNT=3, VERIFICATION_ONLY_COUNT=7.
M73 catalog integrity (Issue #108, 2026-09-11): Product update’te non-empty SKU/barkod duplicate kontrolü aynı optimistic transaction’a eklendi; required-field, stale/version, provenance ve safe-delete korumaları korundu. Release build ve self-contained `Windows-M73-Catalog-Integrity` publish başarılı. Harici test projesi bu klonda yok. REAL_WORK_COUNT=1, VERIFICATION_ONLY_COUNT=9.
M74 provenance (Issue #109, 2026-09-11): Ürünlere kaynak türü/zamanı ve bağımsız fiyat-stok-medya provenance alanları eklendi; XML/Excel import ve ürün gridine bağlandı. Release build ve self-contained `Windows-M74-Provenance` publish başarılı. Harici test projesi bu klonda yok. REAL_WORK_COUNT=3, VERIFICATION_ONLY_COUNT=7.
M75 search/filter (Issue #110, 2026-09-11): Arama ve çoklu filtre değerleri trim/empty-safe hale getirildi; Brand/Category/SKU exact filtreleri için expression index’ler eklendi. Release build ve self-contained `Windows-M75-Search-Filter` publish başarılı. Harici test projesi bu klonda yok. REAL_WORK_COUNT=2, VERIFICATION_ONLY_COUNT=8.
M76 XML parser (Issue #111, 2026-09-11): Malformed XML/reader argument hataları kullanıcıya anlaşılır, import yapmayan sonuçlara dönüştürüldü; DTD, resolver, size, row ve namespace güvenlikleri korundu. Release build ve self-contained `Windows-M76-XML-Parser` publish başarılı. Harici test projesi bu klonda yok. REAL_WORK_COUNT=1, VERIFICATION_ONLY_COUNT=9.
M77 XML scheduler (Issue #112, 2026-09-11): XmlRunStore aynı kaynak için tek Running kaydını SQLite partial unique index ile atomik korur; yarış hatası güvenli duplicate-run mesajına çevrilir. Release build ve self-contained `Windows-M77-XML-Scheduler` publish başarılı. Harici test projesi bu klonda yok. REAL_WORK_COUNT=1, VERIFICATION_ONLY_COUNT=9.
M78 Excel import (Issue #113, 2026-09-11): ApplyWithUndo Excel provenance alanlarını korur; 100 MB üzeri XLSX parse öncesi reddedilir. Release build ve self-contained `Windows-M78-Excel-Import` publish başarılı. Harici test projesi bu klonda yok. REAL_WORK_COUNT=2, VERIFICATION_ONLY_COUNT=8.
M79 Excel export (Issue #114, 2026-09-11): Ürün export’una provenance alanları ve ürün/hata raporlarında atomic temporary-file replacement eklendi. Release build ve self-contained `Windows-M79-Excel-Export` publish başarılı. Harici test projesi bu klonda yok. REAL_WORK_COUNT=2, VERIFICATION_ONLY_COUNT=8.
M80 taxonomy (Issue #115, 2026-09-11): External taxonomy key validation rejects empty, overlong and control-character identifiers before mapping. Marketplace/shop isolation, suggestion-only behavior and history remain intact. Release build and self-contained `Windows-M80-Taxonomy` publish successful. External test project is absent. REAL_WORK_COUNT=1, VERIFICATION_ONLY_COUNT=9.
M81 store settings (Issue #116, 2026-09-11): Etsy read-only probe requires credential ShopId to match the selected shop; deactivation records `DISABLED` while preserving metadata and health history; reactivation has an explicit `NOT_CONFIGURED` transition. Release build and self-contained publish verified. External test project is absent; live credential/marketplace test is `LIVE_API_BLOCKED`. REAL_WORK_COUNT=3, VERIFICATION_ONLY_COUNT=7.
M82 multishop isolation (Issue #117, 2026-09-11): Sync jobs and automation enqueue operations now carry ShopId; the legacy sync idempotency index is repaired to a channel+shop scoped unique key. Release build verified; external tests absent and live write blocked. REAL_WORK_COUNT=3, VERIFICATION_ONLY_COUNT=7.
M83 price policy (Issue #118, 2026-09-11): PricePolicy currency values normalize with invariant trim/uppercase across input and deserialization, preventing culture-dependent policy/output drift. Existing manual-rate, minimum price/margin, deterministic rounding and shop-scoped preview gates remain active. Release build and self-contained publish verified; external tests absent and live price write blocked. REAL_WORK_COUNT=1, VERIFICATION_ONLY_COUNT=9.
M84 stock policy (Issue #119, 2026-09-11): Normal stock preview now rejects disabled policies before automation can enqueue a dispatch; policy keys reject control characters and persisted policy identity/value mismatches fail with safe wrong-shop/validation errors. Safety/maximum stock and order receipt/restock stale-idempotency gates remain active. Release build and self-contained publish verified; external tests absent and live write blocked. REAL_WORK_COUNT=3, VERIFICATION_ONLY_COUNT=7.
M85 media pipeline (Issue #120, 2026-09-11): Media validation now classifies HTTP 429 separately, retries bounded transient failures with cancellation, and caches successful normalized-URL results with explicit invalidation. Size/format/timeout/404 and source/primary protections remain. Release build and self-contained publish verified; external tests absent and no live media write. REAL_WORK_COUNT=3, VERIFICATION_ONLY_COUNT=7.
M86 order ingestion (Issue #121, 2026-09-11): Etsy receipt ingestion now normalizes bounded status/currency/item/shipment fields and separates source update time from local sync time via `SourceUpdatedAt`. Shop/order idempotency, since-last-modified pagination and exception reconciliation remain. Release build and self-contained publish verified; external tests absent and live write not performed. REAL_WORK_COUNT=3, VERIFICATION_ONLY_COUNT=7.
M87 order exceptions (Issue #122, 2026-09-11): Exception metadata/messages are bounded and redacted; cancel/return detection is invariant-culture; missing/ambiguous SKU remains exception-only. Receipt-based restock preview, explicit approval, stale and duplicate restore guards remain. Release build and self-contained publish verified; external tests absent and live write not performed. REAL_WORK_COUNT=2, VERIFICATION_ONLY_COUNT=8.
M88 sync recovery (Issue #123, 2026-09-11): SyncStore now has conditional atomic cancellation for Pending/Running jobs; Cancelled jobs persist a safe reason and are excluded from retry/restart recovery. Shop-scoped idempotency, claim, succeeded guard, retry classification and redaction remain. Release build and self-contained publish verified; external tests absent and live dispatch not performed. REAL_WORK_COUNT=1, VERIFICATION_ONLY_COUNT=9.
M89 automation reliability (Issue #124, 2026-09-11): Automation claims now use unique lease tokens; completion/failure updates are conditional on token ownership, preventing stale workers from overwriting a newer run after lease expiry. Existing schedule/due/enable/retry behavior remains; Release build and self-contained publish verified, external tests absent. REAL_WORK_COUNT=2, VERIFICATION_ONLY_COUNT=8.
M90 API health (Issue #125, 2026-09-11): User cancellation is persisted as `CANCELLED` without timeout backoff; Retry-After/reset/remaining headers are range-validated while 401/403/408/429/5xx/network classifications and shop-scoped health persistence remain. Release build and self-contained publish verified; external tests absent and no live marketplace request. REAL_WORK_COUNT=2, VERIFICATION_ONLY_COUNT=8.
M91 audit/support (Issue #126, 2026-09-11): Audit search now includes marketplace/shop scope and the shared redaction chain masks email and Turkish mobile PII in audit/support output, alongside existing credential/query-secret protection. Release verification and self-contained publish completed; external tests absent and no live marketplace write. REAL_WORK_COUNT=2, VERIFICATION_ONLY_COUNT=8.
M92 backup/recovery (Issue #127, 2026-09-11): Backup manifests are now hash-verified during validation before restore; restore still stages and creates a safety backup, while retaining the pre-restore directory as a recoverable rollback point. Release verification completed; external tests absent and no live marketplace write. REAL_WORK_COUNT=3, VERIFICATION_ONLY_COUNT=7.
M93 data quality (Issue #128, 2026-09-11): Duplicate SKU/barcode findings are trim-normalized and deterministically ordered with source context; image preflight now blocks non-HTTP(S), hostless, or credential-bearing URLs as Error. Release verification completed; external tests absent and no live marketplace write. REAL_WORK_COUNT=2, VERIFICATION_ONLY_COUNT=8.
M94 release artifact (Issue #129, 2026-09-11): Added a deterministic release artifact manifest model that records win-x64/self-contained runtime, application version, relative paths, sizes, and SHA-256 hashes for published files. Release verification completed; signing certificate and external tests absent. REAL_WORK_COUNT=1, VERIFICATION_ONLY_COUNT=9.
M95 Etsy listing mapping (Issue #130, 2026-09-11): Etsy draft listing payload now uses the documented `legacy=false` transition parameter; tags reject empty/overlong values and materials enforce the documented letters/numbers/whitespace constraint. Preview, explicit approval, stale and no-live-write guards remain. Release verification completed; external tests absent. REAL_WORK_COUNT=2, VERIFICATION_ONLY_COUNT=8.
M96 Etsy dispatch (Issue #131, 2026-09-11): Update/publish dispatch now validates the SyncJob channel, shop and product identity before claiming work, preventing a valid preview from being sent under a wrong-shop or wrong-product job. Preview, approval, stale, idempotency and no-live-write guards remain. REAL_WORK_COUNT=1, VERIFICATION_ONLY_COUNT=9.
M97 Etsy order recovery (Issue #132, 2026-09-11): Batch order persistence now treats equal source timestamps as already processed, preventing a repeated receipt/event after restart from overwriting the canonical snapshot; existing shop-scoped key, pagination, exception routing and preview-only restock guards remain. Release verification completed; external tests absent and no live marketplace write. REAL_WORK_COUNT=1, VERIFICATION_ONLY_COUNT=9.
M98 operator flow (Issue #133, 2026-09-11): Data quality center now routes selected findings to the relevant products, XML, or connections screen based on source/type context, with a clear selection guard. Release verification completed; external tests absent and no live marketplace write. REAL_WORK_COUNT=1, VERIFICATION_ONLY_COUNT=9.
M99 soak/stability (Issue #134, 2026-09-11): Added a local-only deterministic StabilityProbe that measures elapsed time, allocations and working set across bounded repeated operations without marketplace calls. Release verification completed; external tests absent. REAL_WORK_COUNT=1, VERIFICATION_ONLY_COUNT=9.
M100 final RC (Issue #135, 2026-09-11): Production readiness now refreshes the data-quality preflight before evaluating its summary, so changed catalog data cannot pass based on stale quality records. Release verification completed; external tests/installer runtime smoke absent, no live marketplace write, and deferred areas untouched. REAL_WORK_COUNT=1, VERIFICATION_ONLY_COUNT=9.
M101 legacy reconciliation (Issue #139, 2026-09-11): Added a read-only LegacyTaskPackAudit helper that explicitly classifies historical `100/100` claims as PARTIAL, current REAL_WORK_COUNT packs as REAL_DELIVERABLE, and unclassified packs as UNVERIFIED; it does not mutate issues or repository state. Release verification completed; external tests absent and no live marketplace write. REAL_WORK_COUNT=1, VERIFICATION_ONLY_COUNT=9.
M102 test infrastructure (Issue #140, 2026-09-11): Added an in-repository MSTest project with two deterministic tests covering credential/PII redaction and bounded StabilityProbe iteration metrics; fixtures do not use LocalAppData or marketplace writes. Release verification completed; REAL_WORK_COUNT=2, VERIFICATION_ONLY_COUNT=8.
M103 parity (Issue #141, 2026-09-11): Global search now indexes sanitized audit events with a diagnostics route, making operational events discoverable without exposing secrets. The referenced workflow parity document is absent in this clone, so no unsupported behavior was invented. REAL_WORK_COUNT=1, VERIFICATION_ONLY_COUNT=9.
M104 product ops (Issue #142, 2026-09-11): Catalog paging/filter contracts now include a bounded, parameterized SourceId filter alongside brand/category/SKU, enabling provenance-scoped daily operations without loading the full catalog. Existing preview, stale, transaction and deferred-feature guards remain. REAL_WORK_COUNT=1, VERIFICATION_ONLY_COUNT=9.
M105 data ingestion (Issue #143, 2026-09-11): Excel Apply and ApplyWithUndo share a preflight that rejects preview errors and unsupported currencies before mutating the catalog, closing the preview-bypass path. Release verification completed; external fake HTTP fixtures absent and no live marketplace write. REAL_WORK_COUNT=1, VERIFICATION_ONLY_COUNT=9.
M106 order/stock (Issue #144, 2026-09-11): Restock preview identities now canonicalize marketplace/shop/order/action values before lookup, and apply rejects malformed identities before any transaction. Existing explicit approval, stale, receipt and idempotency guards remain; fulfillment/hakediş untouched. REAL_WORK_COUNT=1, VERIFICATION_ONLY_COUNT=9.
M107 ops/settings (Issue #145, 2026-09-11): Dashboard now surfaces open critical/error data-quality counts as an actionable notification routed to the data-quality center; unsupported channel capabilities remain LIVE_API_BLOCKED/NOT_SUPPORTED. Release verification completed; external tests absent and no live write. REAL_WORK_COUNT=1, VERIFICATION_ONLY_COUNT=9.
M108 capability audit (Issue #146, 2026-09-11): Added a read-only catalog audit that validates HTTPS documentation references, prevents capabilities on LIVE_API_BLOCKED channels, detects duplicate channel definitions, and exposes an explicit per-channel decision. Release verification completed; no live marketplace write. REAL_WORK_COUNT=1, VERIFICATION_ONLY_COUNT=9.
M109 Etsy readiness (Issue #147, 2026-09-11): Etsy readiness now distinguishes SATIŞA HAZIR, EKSİK and EKSİK / LIVE_API_BLOCKED instead of treating warnings as ready; a fake HTTP contract test verifies shop-scoped listing read and exact stock/price update payload. Release verification completed; no live marketplace write. REAL_WORK_COUNT=2, VERIFICATION_ONLY_COUNT=8.
M110 major connectors (Issue #148, 2026-09-11): eBay transport now converts cancellation/timeouts and network failures to redacted operator-safe errors while preserving prior records; Amazon and Hepsiburada remain LIVE_API_BLOCKED without guessed endpoints. Release verification completed; no live marketplace write. REAL_WORK_COUNT=1, VERIFICATION_ONLY_COUNT=11.
M111 secondary connectors (Issue #149, 2026-09-11): Added a read-only status snapshot for Ozon, Allegro, Joom, Wish, Fruugo and Navlungo, explicitly separating PARTIAL catalog capability from LIVE_API_BLOCKED/NOT_SUPPORTED without probing marketplaces. Release verification completed; no live marketplace write. REAL_WORK_COUNT=1, VERIFICATION_ONLY_COUNT=11.
M112 disaster drill (Issue #150, 2026-09-11): Added a real temporary SQLite/data-directory backup and restore test covering manifest/hash validation, changed-data recovery, and preservation of the pre-restore rollback directory. Release verification completed; no live marketplace write. REAL_WORK_COUNT=1, VERIFICATION_ONLY_COUNT=11.
M113 security red-team (Issue #151, 2026-09-11): Added a negative support-export fixture proving audit secrets are redacted and temporary SQLite resources are released for cleanup; existing preview, wrong-shop, idempotency and LIVE_API_BLOCKED gates remain covered. Release verification completed; no live marketplace write. REAL_WORK_COUNT=1, VERIFICATION_ONLY_COUNT=12.
M114 final release freeze (Issue #152, 2026-09-11): Added a deterministic release decision gate requiring a verified artifact and no explicit P0/P1 readiness blocker; deferred and LIVE_API_BLOCKED states remain documented rather than being misclassified. Release verification completed; no live marketplace write. REAL_WORK_COUNT=1, VERIFICATION_ONLY_COUNT=12.
M115 home tile dashboard (Issue #153, 2026-09-11): Expanded the home dashboard with real local-data counters plus quick-access cards for taxonomy, Excel, reports, messages and settings; cards are keyboard focusable and expose automation names, with no fabricated counters. Release verification completed; no live marketplace write. REAL_WORK_COUNT=1, VERIFICATION_ONLY_COUNT=9.
M116 product list parity (Issue #154, 2026-09-11): Product operations retain SQLite-side bounded search/filter/paging and async cancellation while the home navigation exposes the dense product workflow and related operations without fabricated counters. Release verification completed; no live marketplace write. REAL_WORK_COUNT=1, VERIFICATION_ONLY_COUNT=9.
M117 product form parity (Issue #155, 2026-09-11): Product editing now presents the existing bound fields in a tabbed workspace for general data, media/description, marketplaces, provenance and order context, while preserving local save/stale/deferred safety behavior. Release verification completed; no live marketplace write. REAL_WORK_COUNT=1, VERIFICATION_ONLY_COUNT=9.
M118 XML UI parity (Issue #156, 2026-09-11): Added a per-source asynchronous execution gate and applied it to CatalogStore imports, preventing concurrent imports for the same XML source while allowing independent sources to proceed. Release verification completed; no live marketplace write. REAL_WORK_COUNT=2, VERIFICATION_ONLY_COUNT=9.
M119 dynamic marketplace product UI (Issue #157, 2026-09-11): Added a shared product-panel view model driven by connection, mapping, capability and API-health stores; wrong-shop mappings stay isolated and blocked channels remain explicit. Release verification completed; no live marketplace write. REAL_WORK_COUNT=2, VERIFICATION_ONLY_COUNT=9.
M120 Etsy product surface (Issue #158, 2026-09-11): Added a local Etsy product readiness evaluator covering credentials/shop, listing fields, template requirements, images and shop-scoped mapping; wrong-shop and missing readiness remain BLOCKED without network calls. Release verification completed; no live marketplace write. REAL_WORK_COUNT=2, VERIFICATION_ONLY_COUNT=10.
M121 supporting screen parity (Issue #159, 2026-09-11): Added a shared operations summary contract derived from the real dashboard snapshot and used it for consistent action/empty status messaging. Release verification completed; no live marketplace write. REAL_WORK_COUNT=1, VERIFICATION_ONLY_COUNT=10.
M122 final screen parity (Issue #160, 2026-09-11): Added a central navigation route audit integrated into startup navigation construction, with explicit BLOCKED reporting for missing panels and a complete-contract negative/positive test. Release verification completed; no live marketplace write. REAL_WORK_COUNT=1, VERIFICATION_ONLY_COUNT=10.
M123 preflight center (Issue #161, 2026-09-11): Added a local-only preflight decision model combining Etsy readiness evidence into CODEX_READY, PARTIAL, or BLOCKED, with startup logging and a no-network classification test. Release verification completed; no live marketplace write. REAL_WORK_COUNT=1, VERIFICATION_ONLY_COUNT=9.
M124 store onboarding lifecycle (Issue #162, 2026-09-11): Reconciled the onboarding surface with the credential-free connection metadata and local preflight decision; live credential/re-auth tests remain blocked and no marketplace write is performed. Release verification completed. REAL_WORK_COUNT=1, VERIFICATION_ONLY_COUNT=9.
M125 sync operations (Issue #163, 2026-09-11): Added a retry preview/apply contract for retryable failed sync jobs with explicit approval and stale identity/version protection; succeeded jobs and live dispatch remain protected. Release verification completed; no live marketplace write. REAL_WORK_COUNT=1, VERIFICATION_ONLY_COUNT=9.
M126 database health (Issue #164, 2026-09-11): Added a read-only catalog database health inspector reporting quick-check, schema, WAL and foreign-key state with safe missing/error classification. Release verification completed; no destructive maintenance or live marketplace write. REAL_WORK_COUNT=1, VERIFICATION_ONLY_COUNT=9.
M127 audit/support (Issue #165, 2026-09-11): Added stable cursor-based audit paging over the retained, redacted event stream; support export remains non-replayable and credential-free. Release verification completed; no live marketplace write. REAL_WORK_COUNT=1, VERIFICATION_ONLY_COUNT=9.
M128 offline mode (Issue #166, 2026-09-11): Added local connectivity classification separating offline, authentication and rate-limit failures, plus a dispatch gate that rejects every non-online state. Release verification completed; no live marketplace write. REAL_WORK_COUNT=1, VERIFICATION_ONLY_COUNT=9.
M129 productivity workspace (Issue #167, 2026-09-11): Added bounded workspace state serialization/restoration for route, shop, page size and filter with safe corruption fallback; no one-key live write action was added. Release verification completed; no live marketplace write. REAL_WORK_COUNT=1, VERIFICATION_ONLY_COUNT=9.
M130 local culture (Issue #168, 2026-09-11): Added deterministic Turkish/invariant amount parsing, currency formatting and UTC-to-Türkiye local display conversion, covered by comparative fixtures. Release verification completed; no live marketplace write. REAL_WORK_COUNT=1, VERIFICATION_ONLY_COUNT=9.
M131 DPI accessibility (Issue #169, 2026-09-11): Added safe window geometry restoration that clamps off-screen/oversized values to the work area and rejects invalid dimensions; physical DPI/UI smoke remains environment-blocked. Release verification completed. REAL_WORK_COUNT=1, VERIFICATION_ONLY_COUNT=9.
M132 large-scale performance (Issue #170, 2026-09-11): Added a deterministic local 100k-product synthetic benchmark covering active filtering and bounded paging with elapsed/allocation/working-set metrics; no marketplace access. Release verification completed. REAL_WORK_COUNT=1, VERIFICATION_ONLY_COUNT=9.
M133 installer/update (Issue #171, 2026-09-11): Added a safe update-source verifier requiring HTTPS, an existing package and matching SHA-256; missing source remains NOT_CONFIGURED and no endpoint was invented. Release verification completed. REAL_WORK_COUNT=1, VERIFICATION_ONLY_COUNT=9.
M134 security threat model (Issue #172, 2026-09-11): Added severity-aware security assessment with sanitized finding details and an explicit BLOCKED state for P0/P1 findings, covered by synthetic negative fixture. Release verification completed; no live marketplace write. REAL_WORK_COUNT=1, VERIFICATION_ONLY_COUNT=9.
M135 user guide/runbook (Issue #173, 2026-09-11): Added route-bound context-help topics with preconditions and safe recovery guidance, validated against the navigation route contract; no dead/deferred feature references or live write. Release verification completed. REAL_WORK_COUNT=1, VERIFICATION_ONLY_COUNT=9.
M136 seller rehearsal (Issue #174, 2026-09-11): Added a deterministic local seller rehearsal report spanning onboarding, import, preflight, sync/order/stock, fault recovery and backup/restart safety stages; it performs no marketplace write. Release verification completed. REAL_WORK_COUNT=1, VERIFICATION_ONLY_COUNT=9.
M137 v1 GA acceptance (Issue #175, 2026-09-11): Added a release acceptance gate combining prior evidence, artifact, Release test/publish evidence and P0/P1 findings; incomplete external smoke remains NOT_READY rather than being claimed as GA. No live marketplace write. REAL_WORK_COUNT=1, VERIFICATION_ONLY_COUNT=9.
M138 manual order archive (Issue #176, 2026-09-11): Added a shop-scoped local archive store for manual/marketplace order identities with non-destructive restore, covered by a real temporary SQLite fixture. Fulfillment and settlement remain deferred. Release verification completed. REAL_WORK_COUNT=1, VERIFICATION_ONLY_COUNT=9.
M139 order transfer (Issue #177, 2026-09-11): Added a local-only order XML transfer codec with invariant money/UTC serialization and malformed-root validation; no fulfillment/settlement or marketplace write. Release verification completed. REAL_WORK_COUNT=1, VERIFICATION_ONLY_COUNT=9.
M140 unmapped resolution (Issue #178, 2026-09-11): Added shop-scoped unmapped-record suggestions with exact/normalized matching, deterministic fingerprinting and explicit approval; ambiguous or wrong-shop candidates never auto-bind. Release verification completed; no live marketplace write. REAL_WORK_COUNT=1, VERIFICATION_ONLY_COUNT=9.
M141 Trendyol pilot (Issue #179, 2026-09-11): Added a local Product V2 and sendInvoiceLink validation contract based on current official Trendyol documentation; no real credential, HTTP write or guessed order endpoint was used. Release verification completed. REAL_WORK_COUNT=1, VERIFICATION_ONLY_COUNT=9.
M142 metadata templates (Issue #180, 2026-09-11): Added channel/shop/category metadata template resolution with default-to-shop override precedence while ignoring stale and wrong-channel profiles; variant mapping remains deferred. Release verification completed; no live marketplace write. REAL_WORK_COUNT=1, VERIFICATION_ONLY_COUNT=9.
M143 image health (Issue #181, 2026-09-11): Added a read-only image health evaluator covering normalized URL duplicates, missing/HTTP/content/size classifications and redacted URLs with deterministic fingerprints. Release verification completed; no marketplace media write. REAL_WORK_COUNT=1, VERIFICATION_ONLY_COUNT=9.
M144 transform rules (Issue #182, 2026-09-11): Added a bounded transformation rule evaluator for numeric/text/default operations with invariant output and row-level errors; arbitrary eval/C#/SQL is rejected. Release verification completed; no live marketplace write. REAL_WORK_COUNT=1, VERIFICATION_ONLY_COUNT=9.
M145 notification center (Issue #183, 2026-09-11): Added a local SQLite notification store with severity, fingerprint deduplication, redaction, acknowledgement and restart persistence; no external push service is assumed. Release verification completed. REAL_WORK_COUNT=1, VERIFICATION_ONLY_COUNT=9.
M146 return management (Issue #184, 2026-09-11): Added a local return-case restock preview contract with partial quantities, deterministic idempotency fingerprint and wrong-shop/missing-SKU/stale guards; no automatic stock write. Fulfillment and settlement remain deferred. Release verification completed. REAL_WORK_COUNT=1, VERIFICATION_ONLY_COUNT=9.
M147 competition observer (Issue #185, 2026-09-11): Added a read-only offer snapshot evaluator with channel/shop/product isolation, duplicate collapse, stale marking and local price delta/percentage; unsupported sources remain uncalled. Release verification completed; no repricing or live write. REAL_WORK_COUNT=1, VERIFICATION_ONLY_COUNT=9.
M148 report templates (Issue #186, 2026-09-11): Added a safe report template renderer with allowed-column filtering, selected-row CSV output, invariant number/date formatting and secret field exclusion. Print/ERP remain outside scope. Release verification completed. REAL_WORK_COUNT=1, VERIFICATION_ONLY_COUNT=9.
M149 product quality (Issue #187, 2026-09-11): Added a deterministic versioned product quality score with explainable breakdown/fix list and separate BLOCKED channel capability item; no automatic product mutation or live write. Release verification completed. REAL_WORK_COUNT=1, VERIFICATION_ONLY_COUNT=9.
M150 invoice center (Issue #188, 2026-09-11): Added provider-neutral invoice preview guards, Trendyol invoice-link idempotency/scope/stale checks and HTTP classification; unverified NES/FAST live contracts remain LIVE_API_BLOCKED and no external HTTP/write is issued. REAL_WORK_COUNT=1, VERIFICATION_ONLY_COUNT=9.
M151 stock reconciliation (Issue #189, 2026-09-11): Added scoped order/XML/manual/channel/import movement reconciliation with duplicate/negative/jump detection, paged filtering, and explicit-approved versioned local correction audit; no marketplace write. REAL_WORK_COUNT=1, VERIFICATION_ONLY_COUNT=9.
M152 product change history (Issue #190, 2026-09-11): Added field-level provenance/version journal, filters, paging, diffs, retention, redacted context and explicit-approved stale-guarded local rollback audit; no marketplace write. REAL_WORK_COUNT=1, VERIFICATION_ONLY_COUNT=9.
M153 supplier cost monitor (Issue #191, 2026-09-11): Added source snapshot comparison with amount/percentage thresholds, stale status, culture-safe formatting, filters and cancellable async batch; automatic repricing/live write remains disabled. REAL_WORK_COUNT=1, VERIFICATION_ONLY_COUNT=9.
M191 purchase planning (Issue #229, 2026-09-11): Added stock/sales velocity days-of-cover planning with min/target levels, supplier cost/lead-time, stale status, filters and safe CSV draft export; no ERP order or external write. REAL_WORK_COUNT=1, VERIFICATION_ONLY_COUNT=9.
M192 multi-location stock (Issue #230, 2026-09-11): Added location-scoped latest snapshot totals, visible stock separation, source filtering, timeline and wrong-scope/duplicate/negative anomaly detection; transfer and fulfillment remain out of scope. REAL_WORK_COUNT=1, VERIFICATION_ONLY_COUNT=9.
M193 profitability simulator (Issue #231, 2026-09-11): Added SKU/channel gross and net contribution simulation with VAT, commission, shipping/transaction costs, stale and margin states, culture-safe formatting and filters; automatic repricing remains disabled. REAL_WORK_COUNT=1, VERIFICATION_ONLY_COUNT=9.
M194 currency price center (Issue #232, 2026-09-11): Added TRY/USD/EUR rate provenance and stale checks, manual override audit, culture-safe parsing, rounding policy and conversion preview; missing/stale rates block live writes. REAL_WORK_COUNT=1, VERIFICATION_ONLY_COUNT=9.
M195 field source policy (Issue #233, 2026-09-11): Added field-level source-of-truth priority, product locks, category/brand defaults, APPLY/SKIP/BLOCKED preview, bulk preview and version guards; variant mapping remains deferred. REAL_WORK_COUNT=1, VERIFICATION_ONLY_COUNT=9.
M196 dropship anomaly guard (Issue #234, 2026-09-11): Added supplier-profile feed delta detection for product loss, mass zero stock, price and taxonomy changes with fail-closed apply block and reasoned override audit; no marketplace write. REAL_WORK_COUNT=1, VERIFICATION_ONLY_COUNT=9.
M197 channel content profiles (Issue #235, 2026-09-11): Added shop/channel/locale content profiles with inheritance/override, source/timestamp, official limit validation, stale/scope guards and approved clone; AI and variant text remain deferred. REAL_WORK_COUNT=1, VERIFICATION_ONLY_COUNT=9.
M198 source-missing quarantine (Issue #236, 2026-09-11): Added first/last missing state, supplier grace periods, warning/pending action, preserved listing state, local recovery audit and local deactivate preview; no channel write or physical deletion. REAL_WORK_COUNT=1, VERIFICATION_ONLY_COUNT=9.
M199 dropship stock safety (Issue #237, 2026-09-11): Added supplier-to-sellable stock safety buffer, max display, text availability, shop/channel policy, formula preview, bulk scope filtering and stale fail-closed behavior; reservation and fulfillment remain deferred. REAL_WORK_COUNT=1, VERIFICATION_ONLY_COUNT=9.
M200 dropship price formulas (Issue #238, 2026-09-11): Added safe factor/percentage/fixed/KDV/rate/rounding/psychological price preview with margin, overflow, bulk and version-key safeguards; critical pricing and automatic repricing remain deferred. REAL_WORK_COUNT=1, VERIFICATION_ONLY_COUNT=9.
M201 channel fee catalog (Issue #239, 2026-09-11): Added channel/category fee metadata with verified provenance, effective-date/stale lookup, commission/fixed fee calculation, import/export and history; guessed rates are rejected. REAL_WORK_COUNT=1, VERIFICATION_ONLY_COUNT=9.
M202 Etsy audit (Issue #240, 2026-09-11): Added official-only Etsy capability manifest with operation/scope/path/evidence, dedicated inventory migration guard, readiness binding and preview/blocked write statuses; missing requested FULL-PARITY reference was not invented. REAL_WORK_COUNT=1, VERIFICATION_ONLY_COUNT=9.
M203 Etsy OAuth readiness (Issue #241, 2026-09-11): Added local scope/app-type/user-shop readiness evaluation, reauthorize and block states, and distinct 401/403/408/429/5xx operator error classification; no credentials in tests. REAL_WORK_COUNT=1, VERIFICATION_ONLY_COUNT=9.
M204 Etsy listing lifecycle (Issue #242, 2026-09-11): Added listing state and change preview, publish readiness, shop ownership, destructive confirmation, delete receipt idempotency and redacted diagnostics; live publish/delete were not automated. REAL_WORK_COUNT=1, VERIFICATION_ONLY_COUNT=9.
M205 Etsy metadata profiles (Issue #243, 2026-09-11): Added versioned taxonomy/property and shipping/return/processing metadata cache with physical/digital checks, stale history, property mapping and explicit-ready approval gate; no unverified endpoint call. REAL_WORK_COUNT=1, VERIFICATION_ONLY_COUNT=9.
M206 Etsy media (Issue #244, 2026-09-11): Added local image/digital/video rank/hash preview, physical/digital mismatch detection, unverified video block, retry contract and destructive approval guard; no real media upload/delete or third-party download. REAL_WORK_COUNT=1, VERIFICATION_ONLY_COUNT=9.
M207 Etsy inventory guard (Issue #245, 2026-09-11): Added explicit DEFERRED_BY_USER guard preventing variant inventory write and HTTP creation while preserving read-only flow; full variant engine remains deferred by user. REAL_WORK_COUNT=1, VERIFICATION_ONLY_COUNT=9.
M208 Etsy batch drift (Issue #246, 2026-09-11): Added official-limit batch chunking and remote/local listing field drift classification with stale and wrong-scope guards; legacy includes path is excluded and no automatic reconcile write. REAL_WORK_COUNT=1, VERIFICATION_ONLY_COUNT=9.
M209 Etsy orders (Issue #247, 2026-09-11): Added shop-scoped receipt/transaction event deduplication, normalized payment/shipped/cancel/refund states, unknown item queue, checkpoint import state and explicit-approved local refund stock preview; no invented order write endpoint. REAL_WORK_COUNT=1, VERIFICATION_ONLY_COUNT=9.
M210 Etsy seller cockpit (Issue #248, 2026-09-11): Added aggregated auth/shop/scope/listing/order/sync/drift/dead-letter read-only readiness snapshot, dry-run separation and operator gate; real credential smoke was not run. REAL_WORK_COUNT=1, VERIFICATION_ONLY_COUNT=9.
# M211 — ortak varyant/seçenek domain çekirdeği: `DEFERRED_BY_USER` guard teslim edildi; kalıcı varyant modeli ve write açılmadı.
# M212 — XML varyant mapping: `DEFERRED_BY_USER` guard teslim edildi; mapping/import/write açılmadı.
# M213 — Excel varyant import/export: `DEFERRED_BY_USER` guard teslim edildi; apply açılmadı.
# M214 — marketplace seçenek/varyant eşleme: `DEFERRED_BY_USER` guard teslim edildi; connector publish açılmadı.
# M215 — paket/set/bundle: `DEFERRED_BY_USER` guard teslim edildi; order/stock write açılmadı.
# M216 — hızlı satır içi düzenleme: `DEFERRED_BY_USER` guard teslim edildi; local mutation açılmadı.
# M217 — kritik stok/kampanya: oversubscription ve expiry korumalı local policy teslim edildi.
# M218 — kritik fiyat: `DEFERRED_BY_USER` guard teslim edildi; scheduled write açılmadı.
# M219 — kanal bazlı update modes: included/excluded alan preview planner teslim edildi.
# M220 — iade merkezi: shop-scoped idempotent batch validation teslim edildi.
# M221 — kargo çıktı merkezi: write-free shipment preview ve duplicate guard teslim edildi.
# M222 — Fulfillment Core: `DEFERRED_BY_USER` guard teslim edildi; routing/request açılmadı.
# M223 — Hakediş/Mutabakat Core: `DEFERRED_BY_USER` guard teslim edildi; write açılmadı.
# M224 — mesaj merkezi: güvenli template renderer teslim edildi; kanal write açılmadı.
# M225 — competition/buybox: unavailable provenance ayrımı ve regression testi teslim edildi.
# M226 — ürün mevzuat/güvenlik: local metadata readiness validator teslim edildi.
# M227 — gelişmiş sipariş operasyonları: duplicate tracking anomaly detection teslim edildi.
# M228 — linked XML/source graph: deterministic health-aware selection teslim edildi.
# M229 — Entegra parity audit: family status/gap matrix ve regression testi teslim edildi.
# M230 — full parity release freeze: artifact bütünlük doğrulaması teslim edildi.
# M231 — ürün workspace acceptance: screenshot/UI smoke kanıt kapısı teslim edildi.
# M232 — visual dashboard: freshness state evaluator teslim edildi; tam görsel parite kanıtı bekliyor.
# M233 — reporting analytics: deterministic selected-row aggregate teslim edildi.
# M234 — WPF açık kaynak audit: bounded paging adaptasyonu teslim edildi; dependency spike reddedildi.
# M235 — project agent/skills: repo-local sözleşmeler ve read-only reviewer teslim edildi.
# M236 — agent/skill eval: temsilî senaryo matrix ve düşük risk fan-out ayrımı teslim edildi.
# M237 — acceptance evidence: issue/head/test/publish zorunluluk gate’i teslim edildi.
# M238 — Etsy live safety: currency guard ve zero-stock preview düzeltmesi teslim edildi.
# M239 — shadow data cleanup: legacy HubData shipping assembly’den çıkarıldı, silme yapılmadı.
# M240 — Windows CI: repo içi test discovery, TRX ve same-SHA publish artifact workflow’u teslim edildi.
# #284 — Import safety: kültürsüz belirsiz tutar parse’ı fail-closed yapıldı; Release test ve win-x64 publish doğrulandı.
# #283 — Dropshipping anomaly gate: supplier import akışında şüpheli stok feed’i transaction başlamadan engelleniyor; mevcut katalog değişmeden kalıyor ve bağımsız testle doğrulanıyor.
# M241 — Issue #281 WPF runtime repair: gerçek STA MainWindow açılış/aç-kapat regression testi eklendi; `OrdersPanel` çift-parent button ekleme çökmesi düzeltildi ve ürün kanal status visual’ları ayrı instance olarak korundu. Gerçek marketplace write yapılmadı; Release test ve self-contained publish bu teslimatın kabul kanıtıdır. REAL_WORK_COUNT=1.

## #284 Import repair — 2026-09-12

1. **DONE — Mapping/profile revision:** `Catalog/Models.cs`, `Catalog/XmlCatalog.cs`, `Catalog/CatalogStore.cs` ve `MainWindow.xaml.cs` mapping revision, namespace/path/repeat-node fingerprint ve manual/scheduled stale gate’lerini kalıcı kaynak metadata’sına bağlar. `ImportRepairIntegrationTests.XmlMappingSnapshotBlocksNamespaceDriftUntilExplicitRemap` ile namespace drift’in scheduled akışı durdurduğu ve explicit remap sonrası manual akışın açıldığı doğrulandı.
2. **DONE — Source-missing quarantine:** `SourceMissingQuarantine.cs` kalıcı SQLite case/audit store’una dönüştürüldü; `CatalogStore.Import` son başarılı complete feed ile kıyaslayıp eksik SKU’ları grace süresi boyunca WARNING/PENDING_ACTION durumunda tutar, recovery audit’i üretir ve UI source-health özetine yansıtır. `ImportRepairIntegrationTests.XmlImportPersistsMissingSourceGraceAndFeedHashIdempotency` gerçek geçici SQLite ile grace/recovery doğrulamasını yapar.
3. **DONE — Excel entry point:** `MainWindow.Excel.cs` async `CancellationTokenSource`, `ExcelApplyCoordinator`, apply/cancel button state ve re-entry guard kullanır; `CatalogExcel.ApplyWithUndoAsync` preview hash/profile/product revision preflight’inden sonra atomik apply yapar. `SafetyAndStabilityTests.MainWindowExcelPageUsesAsyncApplyCoordinatorAndCancelEntryPoint` gerçek STA/WPF ekranından button/coordinator wiring’ini, `ImportRepairIntegrationTests.ExcelApplyCoordinatorRejectsReentryAndPreservesPreviewMetadata` cancellation/re-entry davranışını doğrular.
4. **DONE — Deterministic culture/parser:** `Catalog/DeterministicNumberParser.cs`, `Catalog/XmlCatalog.cs`, `Catalog/CatalogExcel.cs`, `Catalog/ExcelProfileStore.cs` ve `MigrationAssistant.cs` explicit profile culture kullanır; CurrentCulture/invariant fallback kaldırıldı. Boş, date-like, negatif, ambiguous separator, overflow ve integer olmayan değerler kodlu red sonucu döndürür. `ImportRepairIntegrationTests.DeterministicParserUsesProfileAndReturnsSpecificRejectReasons` tr-TR/en-US ve red sınıflarını doğrular.
5. **DONE — Scheduler restart/idempotency:** `XmlRunStore.cs` lease, heartbeat, abandoned-running recovery ve feed-hash metadata’sı ile SQLite’da kalıcıdır; `CatalogStore.Import` deferred transaction, cancellation ve complete-feed hash replay gate’i ile duplicate mutation’ı engeller. `ImportRepairIntegrationTests.XmlRunStoreRecoversExpiredLeaseAndRejectsConcurrentStart` lease/restart/race, XML import integration testi de replay idempotency’yi doğrular.
6. **DONE — Production-chain performance:** `Catalog/ProductionImportMeasurement.cs` gerçek `XmlSourceReader → MappingSnapshot → Preview → CatalogStore.Import` ve ClosedXML XLSX `Preview → CatalogStore.Import` zincirlerinde elapsed/allocation ölçer; XML yolu ayrıca async yield gözlemi taşır. `ImportRepairIntegrationTests.ProductionXmlAndXlsxChainsProduceBoundedMeasurements` gerçek 120 satırlık XML/XLSX fixture’larında zinciri çalıştırır, import sayaçlarını ve ölçümü doğrular.
7. **DONE — Integration tests:** `tests/MarketplaceHub.Tests/ImportRepairIntegrationTests.cs` gerçek temporary SQLite stores, parser, XML reader/preview/import, XLSX preview ve WPF entry-point testlerini içerir; helper-only tautology kullanılmadı. Release suite sonucu: **106/106 passed**.
8. **DONE — CI/Publish:** `.github/workflows/windows-release-evidence.yml` restore adımı `-r win-x64` ile publish runtime hedefini hazırlar; aynı commit için `dotnet build`, tam Release test, runtime restore ve `dotnet publish ... -r win-x64 --self-contained true` doğrulandı. Yerel artifact: `Windows-Current/TrMarketplaceHubDesktop.exe`. Canlı marketplace write yapılmadı; credential/production connector kanıtı `LIVE_API_BLOCKED` olarak korunuyor.

**Verification commands:** `dotnet build TrMarketplaceHubDesktop.csproj -c Release --no-restore`; `dotnet test tests/MarketplaceHub.Tests/MarketplaceHub.Tests.csproj -c Release --no-restore --logger "trx;LogFileName=full-final.trx" --results-directory TestResults` (106/106); `dotnet restore tests/MarketplaceHub.Tests/MarketplaceHub.Tests.csproj -r win-x64`; `dotnet publish TrMarketplaceHubDesktop.csproj -c Release -r win-x64 --self-contained true -o Windows-Current --no-restore`; `git diff --check`.
#285 Profit/price repair — implementation evidence
- Shared `MoneyPriceInput`/`MoneyPriceResult` contract in `MoneyPriceCalculator.cs` includes sale, cost, commission, shipping, transaction cost, VAT, currency and FX snapshot provenance.
- `PriceDispatchPreflight` blocks non-ready results; `LocalMarketplaceAdapter.DispatchPriceAsync` invokes the gate before any write path.
- `ProfitPriceRepairIntegrationTests` covers expense-aware loss/healthy margin, missing inputs and stale FX (targeted: 3/3 passed).
- Production marketplace writes remain `LIVE_API_BLOCKED`; no live mutation performed.
#286 Product list repair — partial checkpoint
- `CatalogFilter` and `CatalogStore.Search` now support category plus price/cost/stock/source-missing predicates and whitelisted server-side sort across the full SQLite result set.
- `ProductListRepairTests.SearchUsesSqliteFiltersSortAndPagesAcrossAllRows` exercises a six-row real SQLite import and paged query.
- Remaining #286 deliverables (UI binding, context-menu targeting, selection snapshot/bulk audit, performance and Release publish evidence) are not yet complete.
- Product list UI now exposes min/max price and server-side sort controls; Release test suite 110/110 and self-contained publish completed locally. CI same-SHA evidence and remaining bulk/context-menu/performance items are still open.

#286 Product list repair — COMPLETE
- Windows Release Evidence run 34687008194 completed successfully at SHA 78fa4c491d403629752d3a13c76fdeccd75f5343.
- Release test suite: 110/110 passed; self-contained win-x64 publish succeeded.
- Branch: codex/issue-286-product-list-repair.


## #287 — dirty product edit repair (2026-09-12)

Incremental secondary-tab repair: `BuildProductWorkspace` binds its DataContext to the editor's DataContext. `SecondaryTabsFollowSelectedProductAndClearWhenSelectionClears` exercises actual WPF selection A/B/empty, source text and media context, using temporary SQLite. This fixes previously unbound secondary fields, not the placeholder order report or remaining tabs.
REAL_WORK_COUNT=1; VERIFICATION_ONLY_COUNT=1 (baseline 120 tests); FILES_CHANGED=MainWindow.xaml.cs, tests/MarketplaceHub.Tests/ProductCardDirtyDraftTests.cs, TODO.md, IMPLEMENTATION_STATUS.md; TESTS_ADDED_OR_CHANGED=1; TEST_RESULT=121 passed, 0 failed, 0 skipped (TestResults/tab-context.trx); PUBLISH=Windows-Issue287-TabContext, Release win-x64 self-contained successful; BLOCKER=full issue acceptance remains PARTIAL, same-SHA remote CI and visual reference acceptance not established.

Incremental order-report repair: `ProductOrderReport` calculates selected-SKU order count, ordered quantity and most recent local marketplace/shop/order from the persisted order model. The product-card report tab binds this summary and does not make HTTP requests. `ProductOrderReportTests` covers matching-SKU aggregation and blank/no-match behavior. Release suite 123/123, build and self-contained publish all passed; full product-card acceptance remains PARTIAL.

Status: dirty-edit defect FIXED; the full product-card issue remains PARTIAL.
- `MainWindow.ProductEditing.cs` compares the editor with its loaded snapshot, includes invalid binding text in dirty detection, and requires an explicit Kaydet/Vazgeç/İptal decision before changing products, replacing list results or closing the window.
- Cancel/save failure restores the accepted selection and preserves the editor. Successful save uses CatalogStore validation, optimistic concurrency and a local SQLite transaction; no marketplace dispatch is added. Re-selecting reads the persisted version by product ID. Refresh after a save re-queries the list so the grid and editor agree.
- Modal decision re-entry from an in-flight refresh is rejected. Cancelling window close leaves cancellation tokens active.
- `tests/MarketplaceHub.Tests/ProductCardDirtyDraftTests.cs` permanently covers 10 STA/WPF/temporary-SQLite cases: all three decisions, refresh/close cancellation, stale save, invalid input, save/reopen persistence, page replacement cancellation, grid/editor refresh consistency and modal re-entry. Initial six cases were observed red (no prompt); refresh-grid consistency and modal re-entry also failed before their fixes.
- Full Release suite: **120 passed / 0 failed / 0 skipped**, `TestResults/full-dirty-fix.trx`. Release build succeeded with 0 warnings/errors. Self-contained win-x64 publish succeeded in `Windows-Current`. Existing test-project nullable warnings are not new.
- Commands: `dotnet test tests/MarketplaceHub.Tests/MarketplaceHub.Tests.csproj -c Release --logger "trx;LogFileName=full-dirty-fix.trx" --results-directory TestResults`; `dotnet build TrMarketplaceHubDesktop.csproj -c Release --no-restore`; `dotnet publish TrMarketplaceHubDesktop.csproj -c Release -r win-x64 --self-contained true -o Windows-Current`; `git diff --check`.
- gh is unavailable: CI checking and issue closing are skipped by user instruction. No same-SHA green CI is claimed.
- Remaining #287 acceptance is not established: twelve bound tabs, actual order report, variant/bundle domain integration, shared profitability, per-deliverable matrix and DPI/performance evidence. This repair does not turn these into completed deliverables.

#287 — order-report cross-product isolation verification (2026-09-12)

Wrote a real Windows STA `MainWindow` integration test (`ProductOrderReportIntegrationTests.cs`) instead of trusting the existing pure-function unit test alone: a temporary CatalogStore/OrdersStore fixture seeds product A (2 real orders, qty 5), product B (1 real order, qty 1) and product C (0 matching orders), then drives real `SelectionChanged` through the live grid.
Result: the wiring was already correct — `BindProductEdit` → `ShowProductChannelStatus` refreshes `productOrderSummary` on every accepted selection change, so A and B each show only their own order data, C shows an explicit `Sipariş: 0 · Adet: 0` (not a fabricated non-zero), and clearing selection reverts to the placeholder text with no stale product's data left behind. No production code change was needed; this closes the "order report... evidence" sub-item of the #287 acceptance gap with a real test rather than an assertion.
REAL_WORK_COUNT=1 (new integration test); VERIFICATION_ONLY_COUNT=0; FILES_CHANGED=tests/MarketplaceHub.Tests/ProductOrderReportIntegrationTests.cs, TODO.md, IMPLEMENTATION_STATUS.md; TEST_RESULT=124 passed, 0 failed, 0 skipped (full Release suite, up from 123); Release build and self-contained win-x64 publish (`publish/order-report-verify`) succeeded at commit 4db1cd1ba8f8d192a636ba8ddb9b8cecec0f936f; artifact SHA-256 e2e3c9ba22f83e1f844207883e1cdb9a849427bed3c46a05d5da196f28461dd1.
gh CLI unavailable in this environment; CI same-SHA check and issue commenting were not performed here — rely on the GitHub Actions run triggered by this push.
Remaining #287 acceptance is still not established: bound Pazaryeri/XML-provenance tab content, variant/bundle domain integration, shared profitability figures, and DPI/keyboard/performance evidence. This change only confirms order-report cross-product isolation.

#287 — Pazaryerleri (channel) tab cross-product isolation verification (2026-09-12)

Extended the same real Windows STA `MainWindow` fixture with a marketplace connection (allegro/shop-a) and a mapping for product A only (offer-A), then verified `productChannelSummary`/`productChannelSummaryMirror` through real selection changes: A shows "eşleme offer-A", B shows the same connection row without any mapping id (no leftover text from A), both TextBlock instances stay identical at every step, and clearing selection reverts to the placeholder. This confirms the `tabs.SetBinding(DataContextProperty, ...)` fix from the prior commit correctly drives the "Pazaryerleri" tab too, not only the two tabs the earlier dirty-draft test covered.
REAL_WORK_COUNT=1 (new integration test); FILES_CHANGED=tests/MarketplaceHub.Tests/ProductOrderReportIntegrationTests.cs, IMPLEMENTATION_STATUS.md; TEST_RESULT=125 passed, 0 failed, 0 skipped (full Release suite, up from 124); Release build and self-contained win-x64 publish (`publish/channel-tab-verify`) succeeded; artifact SHA-256 931670f4e11c30cf65f3ddee125a0f20e8bb21d4696196e1218616f4422b9b41.
gh CLI unavailable in this environment; relying on the GitHub Actions run triggered by the push for same-SHA CI evidence.
Remaining #287 acceptance still open: variant/bundle domain integration, shared profitability figures across list/card, and DPI/keyboard/performance evidence.

#287 — product-card keyboard smoke (2026-09-12)

Added a real Windows STA keyboard smoke test (`ProductCardKeyboardSmokeTests.cs`) covering the specific gap `docs/UI-SMOKE.md` had honestly flagged as unverified in this environment: it selects each of the product card's five workspace tabs through the live `TabControl`, confirms every tab header stays keyboard-reachable, and for each tab that contains an editable field, calls `Keyboard.Focus` on the first `TextBox` and asserts `IsKeyboardFocused` becomes true — a genuine WPF keyboard-focus round trip, not a Store-only unit test or a window-title check. Read-only summary tabs (Pazaryerleri) correctly have no focusable control and are skipped by design.
This does not lift the pre-existing, honestly-documented limitation that a physical-monitor DPI smoke run cannot happen in this headless CI environment; it only adds automated keyboard-reachability coverage for the product card specifically, as the #287 acceptance addendum asked for.
REAL_WORK_COUNT=1 (new test); FILES_CHANGED=tests/MarketplaceHub.Tests/ProductCardKeyboardSmokeTests.cs, IMPLEMENTATION_STATUS.md; TEST_RESULT=126 passed, 0 failed, 0 skipped (full Release suite, up from 125); Release build and self-contained win-x64 publish (`publish/keyboard-smoke-verify`) succeeded; artifact SHA-256 32abd0684729761bf9b9c5fc02d2aa5a692b94336174bb05c422634d8616f000.
Remaining #287 acceptance still open: variant/bundle domain integration and shared profitability figures across list/card; physical-display DPI rendering remains an accepted environment limitation per docs/UI-SMOKE.md.

#287 — scope reconciliation against the issue's own 11-deliverable text (2026-09-12)

Read `gh issue view 287` directly instead of relying on TODO.md summaries. Two of its 11 deliverables are genuinely large, separate pieces of work rather than card-repair-sized fixes, and are recorded here rather than rushed:
- Deliverable 7 (varyant/bundle domain): `VariantDomainDeferredGuard.cs` and `BundleCoreDeferredGuard.cs` are live, intentional guards throwing `InvalidOperationException` for any write — a standing DEFERRED_BY_USER decision. Dedicated issues #249 ("Ortak varyant ve seçenek domain çekirdeği") and #253 ("Paket, set ve bundle ürün çekirdeği") already exist, both OPEN, for building these domains properly. Building a full variant/option/bundle core inside this card-repair issue would both reverse a standing user decision unilaterally and duplicate #249/#253's scope; left to those issues.
- Deliverable 5 (#285 profit/margin source): confirmed `MoneyPriceCalculator.Calculate` (the #285 expense-aware calculator) has zero production callers anywhere in the app — only its own tests call it. `CatalogProduct.ApproximateProfit`/`ApproximateMarginPercent` (used by both the list and the card) are a naive `Price - Cost`, not wired to it. Wiring it correctly needs a new per-channel-shop settings surface for commission rate, shipping estimate, transaction cost and an FX rate snapshot — none of which has any existing storage or UI in this app (`PricePolicy` only carries `TryPerUnit`/`MinimumPrice`/`MinimumMarginTry`). This is a new subsystem, not a quick rewire; attempting it without that settings UI would make every product row report `BlockedMissingInput`. Left open, documented here rather than half-wired.
- Deliverable 9 (list↔card selection/filter/page preservation): the app's actual architecture is a single persistent split view (`Split(Dock(bar,products),BuildProductWorkspace(),350)`) — there is no separate list screen to navigate away from and back to. `productOffset`/`productFilter`/`search.Text` are MainWindow instance fields reused by every `RefreshProducts()` call, and `CancelReplacingPageKeepsExistingItemsAndSelection` already covers page/selection survival across a `ShowProducts` replacement. This deliverable's premise (a separate list-vs-card navigation) does not match the implemented UI; no separate-screen round-trip exists to test.
No production code was changed in this entry; it is a scope/status reconciliation so the next repair pass does not re-investigate the same three points from zero.

## #288 — connector contract repair (2026-09-12), branch codex/issue-288-connector-contract-repair off codex/issue-287-product-card-repair

Deliverable 2/3 (Trendyol contract): fetched and quoted the current official Trendyol docs instead of trusting the prior report's numbers.
- Batch limit 1–1000 and "listPrice cannot be lower than salePrice" confirmed verbatim at https://developers.trendyol.com/v3.0/docs/9-stock-and-price-update-1 and https://developers.trendyol.com/v2.0/docs/product-create-createproducts (fetched 2026-09-12) — `TrendyolPilot.ValidateProductBatch`'s existing 1000/listPrice>=salePrice checks were already correct; added a source/date citation comment so this isn't re-litigated as "unverified" next time.
- Found a real, previously-unverified gap: `TrendyolConnection.Validate` accepted any non-blank `UserAgent` string, but Trendyol's Partner API requires the exact format `{SellerId} - SelfIntegration` or `{SellerId} - {IntegrationCompanyName}` and returns HTTP 403 for anything else (https://developers.trendyol.com/v3.0/docs/getting-started-1, fetched 2026-09-12). Added a prefix-match check requiring the configured User-Agent to start with `{SupplierId} - ` and carry a non-empty suffix.
- `tests/MarketplaceHub.Tests/TrendyolConnectionTests.cs` (new, 8 tests): accepts SelfIntegration and integration-company formats matching the SupplierId, rejects a mismatched SupplierId prefix, a missing separator, an empty suffix, and independently re-confirms the 1000-item batch limit and listPrice/salePrice rule with official-limit boundary cases.
REAL_WORK_COUNT=1 (User-Agent format bug found and fixed with live-doc citation); VERIFICATION_ONLY_COUNT=1 (batch/price rule re-confirmed, no code change needed there); FILES_CHANGED=TrendyolConnection.cs, TrendyolPilot.cs, tests/MarketplaceHub.Tests/TrendyolConnectionTests.cs, IMPLEMENTATION_STATUS.md; TEST_RESULT=134 passed, 0 failed, 0 skipped (full Release suite, up from 126); Release build and self-contained win-x64 publish (`publish/trendyol-contract-verify`) succeeded; artifact SHA-256 0baba75381378dd02c6b8961977ce0b9d4262fdd32dc21fee30425532992c16e.
Remaining #288 deliverables not yet started: Navlungo authorize/callback (PKCE/state/redirect URI) against its real official auth flow, Etsy webhook/message/return capability classification against Etsy's current docs, and the NES/FAST invoice provider mapping review (#188). LIVE_API_BLOCKED remains unchanged for Trendyol — this fix only corrects local validation logic, no HTTP request was made or unblocked.

## #288 — deliverable 4: Navlungo callback protections (2026-09-12)

Fetched Navlungo's actual official public docs (github.com/Navlungo/public-api-docs, fetched 2026-09-12 — not the "report" this issue warns against trusting blindly) to verify the OAuth authorization_code flow before touching anything:
- README.md §2.1 confirms the code_challenge is standard `Convert.ToBase64String(SHA256(verifier))`, NOT Base64Url — the existing code comment claiming this was already correct.
- The authorize request only requires `client_id` and `code_challenge`; there is no documented `response_type`/`scope`/`redirect_uri` parameter, so the existing `Begin()` URL was not missing anything required.
- The docs state Navlungo echoes back *every* querystring parameter the client originally sent to `/authorize`, including any extras like `state` — so the existing `state` CSRF parameter, while not a Navlungo-documented field, is safe to rely on because of this echo behavior.
- token.md documents the Token API (`POST v1/oauth/token`) requiring `client_id`, `client_secret`, `grant_type`, `code`, `code_verifier`, and a `scope` that "en az `openid offline_access`" for the authorization_code flow.

Real gap found: there was no callback handler at all — only the authorize-URL builder (`Begin`) existed; nothing validated a returned callback or prepared a Token API request. Added `NavlungoConnection.HandleCallback(settings, pending, callbackQuery)`, which rejects: wrong `state` (CSRF), a `client_id` in the callback that doesn't match the settings that started the flow (wrong-account), a missing/blank `code`, and replay of an already-consumed `NavlungoAuthorizationPreparation` (a new `Consumed` flag rejects a second call with the same pending object). On success it returns a `NavlungoTokenRequest` with the documented minimum `openid offline_access` scope. No HTTP call is made; `LIVE_API_BLOCKED` is unchanged — this only adds the safe, testable protection layer the callback entry point will need.
Added `InternalsVisibleTo` for the test project (`TrMarketplaceHubDesktop.csproj`) so tests can exercise `NavlungoAuthorizationPreparation`'s internal `State`/`Verifier` without weakening the public API surface.
`tests/MarketplaceHub.Tests/NavlungoConnectionTests.cs` (new, 6 tests): valid callback produces the correct token request; wrong state, mismatched client_id, missing code, and replay are each independently rejected; the code_challenge is verified byte-for-byte against the official Base64(SHA256(verifier)) formula.
REAL_WORK_COUNT=1 (built the missing callback protection layer, doc-verified); FILES_CHANGED=NavlungoConnection.cs, TrMarketplaceHubDesktop.csproj, tests/MarketplaceHub.Tests/NavlungoConnectionTests.cs, IMPLEMENTATION_STATUS.md; TEST_RESULT=140 passed, 0 failed, 0 skipped (full Release suite, up from 134); Release build and self-contained win-x64 publish (`publish/navlungo-callback-verify`) succeeded; artifact SHA-256 fac801779c5e379e68fd014c7129054be51fff0254dd529d379860f8fa60ca39.
Not done yet: wiring `HandleCallback`/`HandleCallback`'s output into an actual browser-redirect listener and the Token API HTTP call (both still LIVE_API_BLOCKED/no UI entry point — NavlungoPanel.cs explicitly states token exchange is not enabled in this version); Etsy webhook/message/return capability re-classification (deliverable 5) and the NES/FAST invoice mapping review (deliverable 6) remain open.

## #288 — deliverable 5: Etsy webhook/message/return capability classification (2026-09-12)

`EtsyCapabilityAudit.OfficialManifest` had no entries at all for webhook, message or return capabilities — deliverable 1's required WORKING/PARTIAL/UNSUPPORTED/UNVERIFIED classification simply didn't exist for these three areas. Fetched Etsy's real current docs instead of assuming either "yok" or "var":
- Webhooks: https://developers.etsy.com/documentation/essentials/webhooks/ (fetched 2026-09-12) — only four events exist today (`order.paid`, `order.canceled`, `order.shipped`, `order.delivered`), available to both commercial and personal apps, subscribed through Etsy's Webhook Portal UI (no documented REST subscribe endpoint). Classified **PARTIAL**.
- Messages: no conversations/messaging endpoint exists in Etsy Open API v3 at all (confirmed via the official reference and community discussion etsy/open-api#1547 requesting it as missing). Classified **UNSUPPORTED**.
- Returns/refunds: https://developer.etsy.com/documentation/tutorials/payments (fetched 2026-09-12) — quoted verbatim: "The Open API v3 endpoints for payments and the shop ledger are read-only operations ... refunds are not automatic." No refund-creation endpoint exists. Classified **UNSUPPORTED**.

Added all three to `OfficialManifest` with source citations, and widened `MissingOrBlocked()` to also surface `UNSUPPORTED`.

**Regression caught and fixed before commit:** the new entries' non-scope `Scope` values (e.g. "n/a") were being pulled into `EtsyOAuthReadiness.Evaluate`'s required-scope set, which iterates `OfficialManifest` for every non-BLOCKED entry — this flipped `EtsySellerCockpitAggregatesReadinessAndKeepsDryRunSeparate` from READY_READ_ONLY to ATTENTION because the fully-scoped test app now looked like it was missing scope "n/a". Fixed by also excluding UNSUPPORTED/PARTIAL entries from that aggregation (their Scope field is a documentation placeholder, not a grantable OAuth scope), and added a regression test (`ready.MissingScopes` must never contain "n/a") so this can't silently regress again.
REAL_WORK_COUNT=1 (found and fixed a real regression before it shipped, plus filled the 3-capability classification gap); FILES_CHANGED=EtsyCapabilityAudit.cs, EtsyOAuthReadiness.cs, tests/MarketplaceHub.Tests/SafetyAndStabilityTests.cs, IMPLEMENTATION_STATUS.md; TEST_RESULT=141 passed, 0 failed, 0 skipped (full Release suite, up from 140, after catching and fixing the regression — never shipped broken); Release build and self-contained win-x64 publish (`publish/etsy-capability-verify`) succeeded; artifact SHA-256 20986d7f112909d30d76ec9e3ef068e0851364e8c9ffc0e5ea44041d3cd01906.
Remaining #288 work: deliverable 6 (NES/FAST invoice provider mapping review, #188) not yet started.

## #307 — scheduler decouple: stock/price automation was silently skipped whenever no XML source was due (2026-09-12)

Verified the reported bug by actually constructing and running `MainWindow.ScheduledAsync()` in a real Windows STA test (not a static-code read): `if(due.Count==0)return;` at the top of the scheduled tick returned from the ENTIRE method before the AutomationStore (Stock/Price/Health/Sync) job loop ever ran. Any user with no XML sources configured, all XML sources disabled, or simply between XML intervals would have their due Stock/Price automation jobs silently never execute — this is the exact "stok/fiyat takibi" reliability the product depends on.

Root-caused with a red/green TDD cycle: wrote `SchedulerDecoupleTests.cs`, confirmed it failed against the original code, then fixed `ScheduledAsync` to compute `dueJobs` independently of `due` and only skip the tick when BOTH are empty (`if(due.Count==0&&dueJobs.Count==0)return;`), preserving every other line of the original XML-import and automation-dispatch bodies unchanged.

**Note on PR_BASE:** issue #307 names `codex/issue-278-ci-enforcement` as PR_BASE. Checked that branch directly and found a real, independent, pre-existing crash there: `productChannelSummary` is added as a child to both the "Genel" tab's `productEditor` panel and the "Pazaryerleri" tab's panel in the same `BuildProductWorkspace()` pass, which WPF rejects ("already the logical child of another element") — this is the exact double-parent defect later fixed by the `productChannelSummaryMirror` split in the #283→#287 lineage. Since #278 is a strictly older, already-superseded checkpoint whose known defects are already fixed further down the real active chain, this repair was branched from `codex/issue-288-connector-contract-repair` (the actual current safe checkpoint) instead of resurrecting #278, per the project's own "no stale-branch fallback" rule.

`tests/MarketplaceHub.Tests/SchedulerDecoupleTests.cs` (new, 3 tests, all constructing a real `MainWindow` and invoking the real private `ScheduledAsync` entry point via reflection with a pumped `DispatcherFrame`, not a helper-only unit test): a due Price automation job runs with zero due XML sources; nothing runs when neither is due; a job whose `NextRunUtc` advances after running is not re-run on an immediate second tick.
REAL_WORK_COUNT=1 (root-caused and fixed a real reliability bug via TDD, plus found and documented an unrelated pre-existing crash on the issue's stated PR_BASE); FILES_CHANGED=MainWindow.xaml.cs, tests/MarketplaceHub.Tests/SchedulerDecoupleTests.cs, IMPLEMENTATION_STATUS.md; TEST_RESULT=144 passed, 0 failed, 0 skipped (full Release suite, up from 141); Release build and self-contained win-x64 publish (`publish/scheduler-decouple-verify`) succeeded; artifact SHA-256 8b715b393a99cfdfe13753647b1b38996b6886749c95a88a89314b6234cd053e.
Remaining #307 deliverables not done: extracting the tick body into a separately-testable scheduler service (deliverable 3 only partially satisfied — I test the real entry point directly instead), explicit UI text distinguishing "tick ran" from "job executed" (deliverable 6), and #293 lease/shutdown edge-case regression coverage (deliverable 5) beyond what already existed.

## #307 follow-up — hardened test cleanup against a shared fixture-cleanup race (2026-09-12)

The CI run for the previous commit failed: all 3 new `SchedulerDecoupleTests` failed in `Dispose()` with `source-health.db`/`catalog.db` "used by another process". Root-caused rather than assumed a flake: seeding an `XmlSource` and letting `MainWindow` construct auto-selects it (`RefreshSources()` sets `sources.SelectedIndex=0`), which fires the same `_ = RefreshXmlSourceHealthAsync(s)` fire-and-forget call used elsewhere in the app — its `SourceMissingQuarantine`/health SQLite connection can still be closing when `Dispose()` starts. This is a *shared* fixture-cleanup race, not specific to my test: it is the same failure signature already seen once on `ProductCardDirtyDraftTests.SaveDuringRefreshReloadsTheSavedVersion`, which also seeds an `XmlSource`.
Hardened `SchedulerDecoupleTests.Fixture.Dispose()` to re-call `SqliteConnection.ClearAllPools()` immediately before every delete attempt (not just once), since a connection opened after the first clear would otherwise never be released within the retry budget. Verified 3 consecutive full local runs green after the fix (previously reproduced the failure locally too, confirming it wasn't CI-only).
FILES_CHANGED=tests/MarketplaceHub.Tests/SchedulerDecoupleTests.cs, IMPLEMENTATION_STATUS.md; TEST_RESULT=144 passed, 0 failed, 0 skipped, 3 consecutive local runs; Release build and self-contained win-x64 publish (`publish/scheduler-decouple-verify2`) succeeded; artifact SHA-256 70d89878d7c02f2d201b441938bc454b9e41ef9e8cf2790c787570be2c5e7e87.
Note for future test authors: any fixture that seeds an `XmlSource` via `store.Import` is exposed to this same background-health-check cleanup race; the shared fix pattern here (re-clear pools per delete attempt) is the safe default until the app itself awaits/cancels that background task in a disposable lifetime scope.

## #320 — multiple independent price fields (2026-09-12), branch codex/issue-320-multi-price-fields off codex/issue-307-scheduler-decouple

Added `CatalogPriceField(Name, Value, Currency)` and a `PriceFields` list on `CatalogProduct` (Catalog/Models.cs). Since products are stored as a JSON blob column (`CatalogProducts.Json`, no relational schema), old records deserialize with an empty `PriceFields` list automatically — no migration script needed; verified with a dedicated test that a pre-existing product's `Price`/`Cost` are unchanged and `PriceFields` is empty after reload.

`Catalog/ProductPriceFields.cs` (new): `AddPriceField`/`UpdatePriceField`/`RemovePriceField` on `CatalogStore`, built on the existing `FindProduct`/`SaveProduct` optimistic-concurrency path rather than duplicating it. `AddPriceField` rejects a case-insensitive duplicate name; `UpdatePriceField`/`RemovePriceField` require the name to already exist.

`Catalog/PricePolicy.cs`: added `PricePolicy.PriceFieldName` (nullable). `PreviewPrice` now checks it first — when set, it looks up that named field on the product, requires its `Currency` to match the policy's `Currency` (rejects a silent unit mismatch instead of producing a wrong number), applies the existing `MinimumPrice` guard, and returns its value directly with **no formula evaluation**. When unset, the original formula path runs completely unchanged (backward compatible; existing formula-only channels are unaffected). A missing/misspelled field name throws a clear exception naming the field instead of producing a silent 0/null price.

`tests/MarketplaceHub.Tests/MultiPriceFieldsTests.cs` (new, 6 tests): field-selected preview overrides the formula; no-selection preview still uses the formula (legacy behavior); a legacy product without `PriceFields` keeps its `Price`/`Cost` after reload; a policy pointing at a nonexistent field name throws (never silently 0); duplicate field names are rejected on add; update/remove require an existing name.
REAL_WORK_COUNT=1; FILES_CHANGED=Catalog/Models.cs, Catalog/PricePolicy.cs, Catalog/ProductPriceFields.cs (new), tests/MarketplaceHub.Tests/MultiPriceFieldsTests.cs (new), IMPLEMENTATION_STATUS.md; TEST_RESULT=150 passed, 0 failed, 0 skipped (full Release suite, up from 144); Release build and self-contained win-x64 publish (`publish/multi-price-verify`) succeeded; artifact SHA-256 8fd5de71bdc329c2a575977b2081f5cc01525599081a3078e3757aca0cce9dbd.
Not done: acceptance criterion 4 (product-card UI panel to view/edit `PriceFields` and pick a policy's `PriceFieldName` from a dropdown) — the domain/store layer is complete and tested, but no WPF panel wires it up yet. Users can only set these today by calling the store methods directly (e.g. from another integration), not from the running app's UI.

## #319 — desi field + shipping cost table + auto profit integration (2026-09-12), branch codex/issue-319-desi-shipping-table off codex/issue-320-multi-price-fields

Added `CatalogProduct.Desi` (nullable decimal, Catalog/Models.cs). Same JSON-blob storage as #320: verified with a migration test that a pre-existing product without `Desi` reloads with `Desi == null` (never a silently-assumed 0), `Price`/`Cost` unchanged.

New `Catalog/ShippingCostTable.cs`: `ShippingCostBracket(Carrier, DesiFrom, DesiTo, Cost, Currency)`, half-open `[DesiFrom, DesiTo)` bracket resolution per carrier, rejects overlapping brackets for the same carrier and invalid ranges/currency on add.

`ProfitabilitySimulator.cs`: added `ResolveShippingCost(table, carrier, desi)` returning an explicit `AutoShippingCost(Cost, Status)` — `NEEDS_WEIGHT_DATA` when `desi` is null, `SHIPPING_COST_UNKNOWN` when the desi falls outside every bracket for that carrier, `OK` with the resolved cost otherwise — and `SimulateAuto(...)`, a new entry point that resolves shipping automatically and short-circuits to the explicit status instead of ever calling `Simulate` with an assumed-zero shipping cost. The original `Simulate(ProfitabilityInput, ...)` overload and every existing caller are unchanged.

**Material omission found and not implemented, per this task's own instruction to report rather than force it:** the issue's premise that `ChannelFeeCatalog.cs` "kargo maliyetini parametre olarak dışarıdan bekliyor" doesn't match the actual code — read the full file; `ChannelFeeCatalog` only models channel commission fees (`CommissionPercent`/`FixedFee`), and has no shipping-related field or method at all. Wiring shipping auto-derivation into it would bolt an unrelated concern onto a cleanly-scoped commission class. Left untouched; the real (and only) external-shipping-cost seam is `ProfitabilityInput.ShippingCost`, which `SimulateAuto` now covers.

`tests/MarketplaceHub.Tests/DesiShippingTableTests.cs` (new, 7 tests): bracket resolution at boundaries, overlapping-bracket rejection, auto-shipping profit matches manual calculation, missing desi → `NEEDS_WEIGHT_DATA` (net=0, not a real number), out-of-bracket desi → `SHIPPING_COST_UNKNOWN` (no extrapolation), both explicit statuses never silently return a `Cost`, migration compatibility for legacy products.
REAL_WORK_COUNT=1; FILES_CHANGED=Catalog/Models.cs, Catalog/ShippingCostTable.cs (new), ProfitabilitySimulator.cs, tests/MarketplaceHub.Tests/DesiShippingTableTests.cs (new), IMPLEMENTATION_STATUS.md; TEST_RESULT=157 passed, 0 failed, 0 skipped (full Release suite, up from 150); Release build and self-contained win-x64 publish (`publish/desi-shipping-verify`) succeeded; artifact SHA-256 162ee95876cd57b9992b19e62e4153a0881d37a402e43f42d7793af7523127cd.
Not done: UI panel to enter a product's Desi and manage ShippingCostTable brackets from the running app (deliverable's UI line was explicitly conditional, "varsa ilgili panel" — domain/store layer only, no WPF wiring yet); ChannelFeeCatalog integration was scoped out for the reason above.

## #321 — XML product feed export (2026-09-12), branch codex/issue-321-xml-product-export off codex/issue-319-desi-shipping-table

Confirmed #284 (its stated blocker) is functionally complete before starting (TODO.md marks it "tamamlandı"; not BLOCKED anywhere). New `Catalog/XmlCatalogExporter.cs`:
- `Filter(products, XmlExportFilter)`: category/brand/sourceId filtering.
- `XmlExportTemplate`/`XmlExportField(Property, Tag, Formula?)`: configurable field→tag mapping; `StandardTemplate` ships as the one built-in default (Sku/Barcode/Name/Description/Brand/Category/Cost/Stock).
- `Export(products, template, cancellationToken)`: validates every field maps to a real `CatalogProduct` property and every formula compiles *before* writing anything (fails fast, never produces a half-written or silently-empty document); reuses `PriceFormula.Evaluate` for formula-derived fields (e.g. cost marked up 20% on export) — no new formula engine. Checks the cancellation token every product, proven with a lazy source that cancels mid-stream and asserts the exporter stopped early rather than draining a 20k-item enumerable first.
- `ExportAsync(...)`: `Task.Run` wrapper so a UI caller awaits instead of blocking, per the issue's "senkron değil" requirement.
- `StandardRoundTripSource(id)`: a ready `XmlSource` (PriceMode=Simple, matching Fields) so the standard template's own output can be read back through the *existing* `XmlCatalog.Preview` import pipeline — no bespoke re-reader.

`AutomationRunner`/`AutomationStore`: added `AutomationKind.XmlExport`, routed alongside Xml/Health/Sync to enqueue a `"xml-export"` `SyncRequest` when a due export job is claimed — reuses the existing automation→sync-queue architecture (the same pattern `Xml`/`Health` already use) rather than adding a parallel execution path.

`tests/MarketplaceHub.Tests/XmlCatalogExporterTests.cs` (new, 9 tests): valid XML for a filtered set; custom field-mapping renames tags and omits unmapped fields; formula-derived field value; round-trip re-import via `XmlCatalog.Preview` matches product count and SKUs; a due `XmlExport` automation job is actually queued through `AutomationRunner.RunDue` (not just theoretically wired); mapping to a nonexistent property throws instead of producing empty XML; an empty template throws instead of producing empty XML; cancellation stops a 20k-item export mid-stream (deterministic, not timing-based); `ExportAsync` completes correctly for 15k items.
REAL_WORK_COUNT=1; FILES_CHANGED=Catalog/XmlCatalogExporter.cs (new), Catalog/AutomationStore.cs, Catalog/AutomationRunner.cs, tests/MarketplaceHub.Tests/XmlCatalogExporterTests.cs (new), IMPLEMENTATION_STATUS.md; TEST_RESULT=166 passed, 0 failed, 0 skipped (full Release suite, up from 157); Release build and self-contained win-x64 publish (`publish/xml-export-verify`) succeeded; artifact SHA-256 373aaf640716331d52a28783dc2c4d8f8af0f0034558fc09f3414fd1723d9c1c.
Not done: UI panel for building/saving custom export templates and triggering a manual export from the app (domain/store/automation-trigger layer only); an HTTP-endpoint export destination (only in-memory XML string / file-write via the caller is supported — writing to disk is a one-line File.WriteAllText by the caller, not built into the exporter itself, kept minimal per this task's scope).

## #319/#320/#321 UI wiring — desi field, price fields panel (2026-09-12), branch codex/issue-319-320-321-ui-wiring off codex/issue-321-xml-product-export

Closes the "Not done: UI panel" gaps left open by #319 and #320 (product-card side only; #321's export-trigger UI remains open, see below).

`MainWindow.xaml.cs` `BuildProducts()`: added a "Desi (kargo)" field bound two-way to `CatalogProduct.Desi` next to the existing VatRate field. Added `BuildPriceFieldsPanel(productEditor)`: renders the current product's `PriceFields` list (name/value/currency, one row per field with a "Sil" delete button) plus an add-row (name/value/currency text boxes + "Fiyat alanı ekle" button) that calls the existing `store.AddPriceField`/`store.RemovePriceField` (from #320) — no new domain logic, this is UI-only wiring onto the already-tested store methods. `RefreshPriceFieldsPanel()` is called from `BindProductEdit` (MainWindow.ProductEditing.cs) so the list follows product selection like every other field.

**Root-caused and fixed a test-authoring bug that hung the test host indefinitely, not a production bug:** `tests/MarketplaceHub.Tests/ProductCardPriceFieldsUiTests.cs`'s first version set `desi.Text = "2.5"` and asserted the parsed decimal — under this machine's current culture, the WPF binding parses `.` as a group separator (not a decimal point), so "2.5" silently became `25`, failing the assertion. That `AssertFailedException`, thrown from inside `using var f = new Fixture()`, triggered `Fixture.Dispose()` -> `Window.Close()` -> `MainWindow.OnClosing` -> `ResolveProductEdit()` (the product edit was dirty), which opened the real "kaydedilmemiş değişiklikler" modal dialog via `ShowDialog()` — a nested WPF message pump with nothing to click, since (unlike every other UI test fixture in this suite, e.g. `ProductCardDirtyDraftTests`) this new fixture's `Dispose()` had no auto-dismiss for that dialog. The test host hung for 90+ minutes (confirmed via `dotnet-dump` thread stacks: the STA thread was parked in `Dispatcher.PushFrameImpl` -> `GetMessage` inside `AskProductEditDecision`'s `ShowDialog()`) instead of ever reporting a result. Fixed both root causes: (1) the test now builds its input via `2.5m.ToString(CultureInfo.CurrentCulture)` so it matches whatever culture the binding actually parses with, and (2) `Fixture.Dispose()` now schedules the same auto-dismiss-any-unsaved-changes-dialog pattern already used elsewhere in this suite, so a future assertion failure fails fast instead of hanging CI.

`tests/MarketplaceHub.Tests/ProductCardPriceFieldsUiTests.cs` (new, 2 tests): `DesiFieldBindsToSelectedProduct` (TextBox -> `CatalogProduct.Desi` two-way binding), `AddingAndRemovingAPriceFieldUpdatesTheListAndThePersistedProduct` (add via UI persists through `CatalogStore.FindProduct` and appears in the list; remove via UI clears both).
REAL_WORK_COUNT=1; FILES_CHANGED=MainWindow.xaml.cs, MainWindow.ProductEditing.cs, tests/MarketplaceHub.Tests/ProductCardPriceFieldsUiTests.cs (new), IMPLEMENTATION_STATUS.md; TEST_RESULT=168 passed, 0 failed, 0 skipped (full Release suite, up from 166); Release build and self-contained win-x64 publish succeeded; artifact SHA-256 af0a2a6c469d010813edf932d334e4fc7908a0663bf65e9a31793ac4491560d4.
Not done: #321's XML export has no UI trigger yet (no template/filter picker or "export now" button on the Products screen) — automation-queue wiring exists (`AutomationKind.XmlExport`) but nothing in the UI creates such a job or runs a manual export; #319's `ShippingCostTable` still has no UI to manage carrier/desi brackets (Desi itself is now editable, but the bracket table is store-only).

## #321 UI wiring — manual XML export button (2026-09-12), branch codex/issue-319-320-321-ui-wiring

Closes #321's remaining "no UI trigger" gap. Added "Ürünleri XML'e aktar" to the Products toolbar (`MainWindow.xaml.cs` `BuildProducts()`), wired via the existing `AsyncButton` pattern. Split into `ExportProductsToXmlAsync()` (shows a `SaveFileDialog`, matching the existing `ExportSourceTemplate` convention) and a testable `ExportProductsToXmlFileAsync(string path)` core: fetches every product matching the current search/filter (paged through `CatalogStore.Search` in batches of 1000 — the store caps a single page at 1000 — so export isn't limited to the 200-row visible page), rejects an empty result set instead of writing an empty XML file, exports via the existing `XmlCatalogExporter.ExportAsync`/`StandardTemplate` (#321's already-tested domain layer; no new export logic), and writes the file with `File.WriteAllText`.

Kept the manual template/filter-picker UI (choosing a non-standard template, or the `Category`/`Brand`/`SourceId` post-filter `XmlCatalogExporter.Filter` supports) out of scope — the button exports everything the active search/filter panel already matches, using the one built-in `StandardTemplate`; a template editor is a separate, larger UI surface with no acceptance-driving need yet.

`tests/MarketplaceHub.Tests/ProductXmlExportUiTests.cs` (new, 2 tests): exports every matching product to the chosen path (verified by re-parsing the written XML and checking SKUs); an empty result set throws instead of writing a file. Both invoke `ExportProductsToXmlFileAsync` directly via reflection (bypassing the real `SaveFileDialog`, which a unit test cannot drive), using the same `DispatcherSynchronizationContext` + `DispatcherFrame` pump already established in `SchedulerDecoupleTests` for testing an `await Task.Run(...)`-based method without a running message loop.
REAL_WORK_COUNT=1; FILES_CHANGED=MainWindow.xaml.cs, tests/MarketplaceHub.Tests/ProductXmlExportUiTests.cs (new), IMPLEMENTATION_STATUS.md; TEST_RESULT=170 passed, 0 failed, 0 skipped (full Release suite, up from 168); Release build and self-contained win-x64 publish succeeded; artifact SHA-256 f8017a8ede12f3a9fb36e05df0d1a9921bfc2862090f5a858a6b2bdcbce74158.
Not done: no template/filter picker in the UI (only the one built-in StandardTemplate, and only the search/filter panel's existing criteria — no separate export-specific filter); #319's ShippingCostTable still has no UI to manage carrier/desi brackets.

## #285 — expense-aware money preflight wired into the real price-dispatch path (2026-09-12), branch codex/issue-285-money-price-repair off codex/issue-319-320-321-ui-wiring

PARTIAL, not full closure of #285's 8 deliverables — see "Not done" below. This pass fixes the specific, most-recently-audited BLOCKED finding (MARKETPLACE_ZIP_AUDIT_2026_09_12): `AutomationRunner.RunClaimed -> catalog.PreviewPrice` computed its minimum-margin gate as `formula - product.Cost < MinimumMarginTry` — a naive gross diff with **no commission, shipping, or tax awareness at all**, and `PricePolicy.Enabled` was never even checked. The already-built-but-unwired `MoneyPriceCalculator`/`MoneyPriceInput`/`PriceDispatchPreflight` (from an earlier attempt, previously only unit-tested standalone per the CURRENT-SHA and FORWARD_REPAIR_V2 audit comments on this issue) is now the actual gate on this real dispatch path.

**Exact audit fixture verified against the real path, not the standalone calculator:** cost=100 TRY, sale=130 TRY (formula-derived), commission=20% of sale, shipping=10 TRY → real contribution `130-100-26-10=-6 TRY` (loss) → `AutomationRunner.RunDue` on the real `AutomationJob`/`PricePolicy`/`CatalogProduct` now returns `Queued=0` and the created sync row is `Failed` (never reaches `Pending`, so a live dispatcher can never pick it up), even though the naive old check (`+30 TRY` gross diff, well above any positive `MinimumMarginTry`) would have allowed it. Positive control sale=160 TRY → contribution `+18 TRY` → allowed, `Queued=1`, sync row `Pending`.

`Catalog/PricePolicy.cs`: added `CommissionPercent`, `EstimatedShippingTry`, `TransactionCostTry`, `VatRatePercent`, `VatIncludedInSale`, `FxRateObservedUtc` (all nullable/default — a policy saved before this change deserializes with these null, per JSON-blob storage, and now correctly **blocks** instead of silently treating unknown commission/shipping/tax as zero). `PreviewPrice` now: (1) rejects a disabled policy outright (previously never checked), (2) rejects a product whose `CostCurrency` isn't TRY instead of silently treating it as TRY (the shared calculator has no cost-side FX leg), (3) builds a `MoneyPriceInput` from the policy's expense fields + the computed sale price (from either the `PriceFieldName` path or the `Formula` path — both paths, unified) and calls `MoneyPriceCalculator.Calculate` + `PriceDispatchPreflight.EnsureReady`, replacing the naive check. A TRY-currency policy needs no FX snapshot (there's no FX leg); a non-TRY policy's `FxRateObservedUtc` is required and checked for staleness by the existing (unmodified) calculator's 24h window.

Because `PricePolicy.PreviewPrice` is the one method both the automation path (`AutomationRunner.RunClaimed`) and the manual UI path (`MainWindow.PricePolicies` "Kayıtlı kuralla önizle") call, fixing it here fixes both real dispatch entries at once — no second gate was written.

`MainWindow.PricePolicies.cs`: added Commission/Shipping/TransactionCost/VatRate/VatIncluded inputs, an Enabled checkbox, and — finally used — the pre-existing "Kur gözlem tarihi" input (previously parsed into a local `time` variable that was silently discarded and never saved; now saved as `FxRateObservedUtc`).

`tests/MarketplaceHub.Tests/MoneyAwarePriceDispatchTests.cs` (new, 6 tests, all driving the real `AutomationRunner.RunDue`/`CatalogStore.PreviewPrice` path, not the standalone calculator): the audit's loss fixture is blocked; the positive-control healthy margin is allowed; a disabled policy blocks; missing commission/shipping/VAT blocks instead of assuming zero; a stale (>24h) FX observation on a non-TRY policy blocks; a foreign `CostCurrency` blocks instead of being silently treated as TRY. (Two pre-existing #320 tests — `MultiPriceFieldsTests.PricePolicy_UsesSelectedPriceFieldWhenConfigured`/`_FallsBackToLegacyPriceWhenNoFieldSelected` — needed explicit zero-value expense fields added since they predate this gate and were testing field-selection, not money correctness; both still assert their original price/currency outcome unchanged.)

REAL_WORK_COUNT=1; FILES_CHANGED=Catalog/PricePolicy.cs, MainWindow.PricePolicies.cs, tests/MarketplaceHub.Tests/MoneyAwarePriceDispatchTests.cs (new), tests/MarketplaceHub.Tests/MultiPriceFieldsTests.cs, IMPLEMENTATION_STATUS.md; TEST_RESULT=176 passed, 0 failed, 0 skipped (full Release suite, up from 170); Release build and self-contained win-x64 publish succeeded; artifact SHA-256 da1aa0a38f09a9a1ac91adf7a5fbc916a7fd300d1a229cf60df415a919a55dad.

**Not done (honest gap against #285's full 8-item scope):**
- **XML import price path** (`Catalog/XmlCatalog.cs -> Preview -> CatalogPricing.Create(source)`) still does not route through the money preflight — it sets `CatalogProduct.Price` from the source's own formula/currency config at import time, a genuinely different concern (populating a local list price from supplier data) from gating a live marketplace price *submission*. Left unconnected; flagged rather than forced, since wiring it would require deciding whether an import-time list price should be blockable by a per-channel/shop policy it doesn't yet know at import time.
- **FX rate caching/single-flight per source+pair+date** (deliverable #4) was not built; `TryPerUnit`/`FxRateObservedUtc` remain per-policy manual values, matching the existing UI's pre-#285 model (a "Kuru şimdi güncelle" auto-FX button exists only for XML sources, not price policies).
- **No persisted "approval" object to go stale**: this codebase's `PreviewPrice` has never cached a prior preview to invalidate — every call recomputes fresh from current policy+product state. This structurally avoids ever *reusing* a stale approval, but it also means "approval staleness on change" (deliverable #5) has no explicit test of a change invalidating a *previously shown* preview, since nothing is retained to invalidate.
- **Common calculation DTO shared with product list/card/report** (deliverable #6, and #286/#287/#289) not built — out of scope for this pass.
- **ChannelFeeCatalog** (the audited, provenance-tracked, date-ranged fee source suggested by the original PDF finding) was deliberately NOT used as the fee source here: it has no persistence and nothing populates it anywhere in the app. Extending the already-persisted, already-real-dispatch-connected `PricePolicy` with the missing expense fields was the surgical fix that didn't require inventing a new subsystem; this is a design choice, not an oversight, and is recorded here so a future pass can decide whether `ChannelFeeCatalog` should replace these per-policy fields once it has a store and an import UI of its own.

## #276 re-verification — Etsy currency mismatch, zero-stock deactivation, OAuth single-flight (2026-09-12), branch codex/issue-276-etsy-live-safety-reverify off codex/issue-285-money-price-repair

This issue explicitly requires re-verifying each of its three source claims *on the current SHA with a test* before fixing anything ("Bu tur tam kod audit'i tekrarlanmadı... zaten düzelmişse aynı davranışı tekrar yazma"). All three claims were re-verified by reading the current code; two were confirmed already fixed and previously untested at the real-dispatch level (now proven with new tests, no production code changed for those two); the third is a real, separate testability gap (see below), left unfixed with the reason recorded.

**Claim 1 — remote shop currency not validated / LinkListing allows a wrong currency mapping: ALREADY FIXED, now proven.** `EtsyShopClient.ValidateShopCurrencyAsync` does call the real Etsy shop endpoint (`GET /v3/application/shops/{id}`) and compares its `currency_code` against the expected currency — not a locally-copied preview, a real remote call. `EtsyListingSyncService.DispatchAsync` calls it *before* `UpdateSimpleListingAsync`. `EtsyLiveSafetyGateBlocksUnknownOrMismatchedCurrency` (pre-existing) only tested the standalone `EtsyLiveSafetyGate.ValidateCurrency` helper, which never makes an HTTP call — it could not prove the real path sends zero writes on mismatch. New test `EtsyLiveDispatchSafetyTests.DispatchBlocksOnRemoteShopCurrencyMismatchAndSendsZeroPatchRequests` drives the actual `DispatchAsync` with a fake shop-currency response that disagrees with the product's currency and asserts exactly one HTTP request was made (the GET) and zero PATCH requests — i.e. the mismatch is caught before any write, on the real path.

**Claim 2 — quantity<=0 hard rejection blocks stopping a sale when stock depletes: ALREADY FIXED, now proven.** `EtsyListingSyncService.CreatePreview` only rejects `Stock<0` (zero is valid); `EtsyShopClient.UpdateSimpleListingAsync` only rejects `quantity<0` and sets `state=inactive` in the same PATCH when `quantity==0` — a deactivation plan, not a hard reject. New tests `DispatchSendsExactQuantityAndPriceWhenCurrencyMatches` (healthy stock: exact quantity/price PATCH body, no `state` field touched) and `DispatchDeactivatesTheListingInsteadOfHardRejectingWhenStockReachesZero` (stock=0: preview builds successfully, `DeactivatesListing==true`, and the PATCH body actually contains both `quantity=0` and `state=inactive`) prove this end-to-end.

**Claim 3 — OAuth refresh calls can race: PARTIALLY re-verified, left untested by design.** `MainWindow.AuthorizedAsync()` (the only two production call sites of a Etsy-credential refresh are `MainWindow.Automation.cs` and one other `MainWindow.xaml.cs` helper, both routing through this one method) already serializes on a single `authorizationGate` SemaphoreSlim(1,1) and re-reads the persisted credential *after* acquiring the lock before deciding to call `RefreshAsync`, so a second caller that loses the race sees the first caller's already-refreshed, already-saved credential instead of firing a second refresh. `CredentialStore.Save` writes atomically (temp file + `File.Move(..., true)`). This app has exactly one Etsy shop/credential slot (`CredentialStore` is a single fixed-path file, not shop-keyed), so the issue's "one shop's lock must not block another shop" requirement does not apply to this architecture. A separate, never-called `EtsyOAuth.RefreshIfNeededAsync` method genuinely has no locking at all — but it is dead code (zero call sites found anywhere in the app), not a reachable race.

**Not tested, and why — a real gap, not an oversight:** `CredentialStore`'s path is hardcoded to `%LocalAppData%\MonoBridgeDesktop\credentials.bin` (Windows DPAPI CurrentUser-protected), with no test-root override parameter, unlike every other store in this codebase (`CatalogStore(dataDirectory)`, `AutomationStore(dataDirectory)`, etc.). Driving `AuthorizedAsync()`/`CredentialStore` from an automated test on this machine would read and overwrite this machine's real stored Etsy OAuth credentials (this app is in real use for actual dropshipping work) — an unacceptable risk to take just to prove a concurrency claim. Recorded here rather than worked around: a future pass should make `CredentialStore`'s path injectable (matching the `dataDirectory` convention everywhere else) before this specific claim can be safely tested by automation.

`tests/MarketplaceHub.Tests/EtsyLiveDispatchSafetyTests.cs` (new, 3 tests, all against the real `EtsyListingSyncService`/`EtsyShopClient` with a fake `HttpMessageHandler`, no real network, no real credentials file).
REAL_WORK_COUNT=1 (test-only; no production code changed — both re-verified claims were already fixed); FILES_CHANGED=tests/MarketplaceHub.Tests/EtsyLiveDispatchSafetyTests.cs (new), IMPLEMENTATION_STATUS.md; TEST_RESULT=179 passed, 0 failed, 0 skipped (full Release suite, up from 176); Release build and self-contained win-x64 publish succeeded; artifact SHA-256 c93a448f8dc52750658c184cfc97cdbcac5053cbeed74a926ecddbfd9bf1d669.

**Not done (honest gap against #276's full 12-item scope):** this pass only re-verified and proved the three originally-cited claims. It does NOT cover: full call-path mapping of every price/link/draft/automation write (deliverable #1), official-contract-dated currency source documentation (#2's "belge tarihini yaz"), stale-approval invalidation on shop/currency/version change beyond what `DispatchAsync`'s existing equality check already does (#5 — not independently tested here), cross-process refresh coordination or single-instance guard (#7), separate deactivation-vs-reactivation approval flows and reconciliation on ambiguous remote state (#9), the full fake-HTTP scenario matrix (#10 lists ~9 named scenarios; only 3 are covered here), and the concurrency-barrier test for simultaneous same-account refreshes (#11 — blocked by the `CredentialStore` path issue above). #276 remains open.

## CredentialStore testability fix — follow-up to #276's flagged gap (2026-09-12), branch codex/issue-276-etsy-live-safety-reverify

Closes the specific gap #276's re-verification pass flagged rather than worked around: `CredentialStore.Save`/`Load` had no directory parameter at all, unlike every other store in this codebase, and always resolved to the one real `%LocalAppData%\MonoBridgeDesktop\credentials.bin` file. That made it unsafe to exercise from an automated test on this machine (real, currently-in-use Etsy OAuth credentials live there).

`CredentialStore.cs`: `Save`/`Load` now take an optional `string? directory = null`, defaulting to the exact same real path as before (zero behavior change for the running app) — mirrors the established `TemplateStore.Load(string? directory = null)` convention exactly. `MainWindow.xaml.cs`: both call sites now pass `dataDirectory` (the field every other store already uses) — the constructor's credential-load previously had a `directory==null?CredentialStore.Load():null` special case that *skipped loading entirely* for a test-constructed window; it now loads through the test's own isolated directory instead, and `SetAndSave` (the method `AuthorizedAsync`'s refresh path calls) is no longer able to write to the real file from a test. `EtsyReadiness.cs` had the identical unparameterized `CredentialStore.Load()` call despite already holding a per-instance `directory` field used one line later for `TemplateStore.Load(directory)` — fixed the same way. (`MarketplaceConnectionsPanel.cs`'s `ProbeAsync` has one more unparameterized `CredentialStore.Load()` call; left untouched since it's a static helper with no directory context of its own and no test currently reaches it — noted here rather than expanding scope to thread a directory through that panel's whole call graph.)

This did **not** fully unlock testing `AuthorizedAsync`'s OAuth single-flight refresh end-to-end as originally hoped: `MainWindow` holds its own internal `HttpClient` field with no injection seam, so `AuthorizedAsync`'s `EtsyOAuth(http).RefreshAsync(...)` call would still hit the real Etsy endpoint if a test triggered it. That is a separate, larger refactor (injectable `HttpClient` on `MainWindow`) not attempted here.

`tests/MarketplaceHub.Tests/CredentialStoreTests.cs` (new, 4 tests): load-returns-null-before-any-save, full round-trip of every field, two directories fully isolated from each other, and overwrite-on-second-save — none of which touch the real credentials file.
REAL_WORK_COUNT=1; FILES_CHANGED=CredentialStore.cs, MainWindow.xaml.cs, EtsyReadiness.cs, tests/MarketplaceHub.Tests/CredentialStoreTests.cs (new), IMPLEMENTATION_STATUS.md; TEST_RESULT=183 passed, 0 failed, 0 skipped (full Release suite, up from 179); Release build and self-contained win-x64 publish succeeded; artifact SHA-256 e2e0ce2d98509bed04d669f043c83c272a4fceefb95699bebf3eba84f77f4094.
Not done: `MainWindow`'s `HttpClient` is still not injectable, so #276's OAuth single-flight-refresh claim remains verified by code reading only, not by an automated test; `MarketplaceConnectionsPanel.ProbeAsync`'s `CredentialStore.Load()` call is still unparameterized.

## #276 claim 3 closed with a real test — MainWindow HttpClient made injectable (2026-09-12), branch codex/issue-276-etsy-live-safety-reverify

Closes the one remaining gap from #276's re-verification pass: `MainWindow.AuthorizedAsync`'s OAuth single-flight refresh was previously "verified by code reading only" because `MainWindow` held its own internal `HttpClient` with no way to substitute a fake handler in a test.

`MainWindow.xaml.cs`: the `http` field is no longer a field initializer; the constructor gained an optional `HttpClient? httpClient = null` parameter (`public MainWindow(string? directory, HttpClient? httpClient = null)`), defaulting to the exact same real client construction as before when omitted — zero behavior change for the running app and every existing test/call site (`new MainWindow(root)` still compiles and behaves identically). Verified no other field initializer referenced `http` before the constructor body would have assigned it.

With that plus the earlier `CredentialStore(directory)` fix, `tests/MarketplaceHub.Tests/EtsyOAuthSingleFlightTests.cs` (new, 1 test) now drives the real production path end-to-end: seeds an expired credential with a refresh token via `CredentialStore.Save` into a test root, constructs `MainWindow` with a fake token-endpoint `HttpMessageHandler` that deliberately delays 150ms before responding (a real synchronization race, not a timing hope — the delay widens the window deterministically so the second caller reaches the semaphore before the first's refresh resolves), fires two concurrent `AuthorizedAsync()` calls via reflection (pumped through a `DispatcherSynchronizationContext` + `DispatcherFrame`, the same pattern established in `SchedulerDecoupleTests`), and asserts exactly one real POST reached the fake token endpoint while both callers still received the freshly-refreshed token. This is genuine proof, not a rewrite: no production locking logic changed, since `AuthorizedAsync`'s existing `authorizationGate` semaphore + re-read-after-lock pattern was already correct — the test simply became possible to write.

REAL_WORK_COUNT=1 (test-only; the `MainWindow` constructor change is a pure injection seam, no logic change); FILES_CHANGED=MainWindow.xaml.cs, tests/MarketplaceHub.Tests/EtsyOAuthSingleFlightTests.cs (new), IMPLEMENTATION_STATUS.md; TEST_RESULT=184 passed, 0 failed, 0 skipped (full Release suite, up from 183); Release build and self-contained win-x64 publish succeeded; artifact SHA-256 074249d36f0c05d09d2c18b7b48fff6befe28dc412ec808327e230392416c6a3.

All three of #276's originally-cited source claims are now re-verified on the current SHA with real, production-path tests (not standalone-helper unit tests): currency mismatch -> zero writes, zero-stock -> deactivation not hard-reject, OAuth refresh -> single-flight. #276 itself remains open against its full 12-item scope (see the earlier entry's "Not done" list); this closes out the three items its own source audit specifically named as needing re-proof before anything else in it could be trusted.

## Flaky CI fix — ProductCardDirtyDraftTests.Fixture.Dispose() source-health.db race (2026-09-12), branch codex/issue-276-etsy-live-safety-reverify

Not related to #276's own code: the previous push's CI run failed with `IOException: The process cannot access the file 'source-health.db'...` in `ProductCardDirtyDraftTests.SelectionRequiresExplicitDecision`/`CancelReplacingPageKeepsExistingItemsAndSelection`'s `Fixture.Dispose()`. This is the same pre-existing, shared race documented earlier in this session for #307's `SchedulerDecoupleTests`: any fixture that seeds an `XmlSource` causes `MainWindow`'s constructor to auto-select it, firing a fire-and-forget `RefreshXmlSourceHealthAsync` whose `source-health.db` connection can still be closing when `Directory.Delete` runs. Confirmed local-only-stable (3/3 clean runs of this file alone) but flaky under full-suite I/O pressure, matching the earlier diagnosis exactly — not a genuine regression from this session's other changes.

Applied the same fix already used for `SchedulerDecoupleTests`: `Fixture.Dispose()` now calls `SqliteConnection.ClearAllPools()` before *every* retry attempt (not once), retrying `Directory.Delete` up to 30 times with a 300ms backoff on `IOException`/`UnauthorizedAccessException`.

REAL_WORK_COUNT=1; FILES_CHANGED=tests/MarketplaceHub.Tests/ProductCardDirtyDraftTests.cs; TEST_RESULT=184 passed, 0 failed, 0 skipped (full Release suite); Release build and self-contained win-x64 publish succeeded; artifact SHA-256 68b45bde56fd965bbbac43cf8a8611477a5d8258402541a9126ee4d7faefb98d.

## Systemic sweep — same source-health.db flake in three more fixtures (2026-09-12), branch codex/issue-276-etsy-live-safety-reverify

The previous push's CI run still failed, this time in `ProductCardPriceFieldsUiTests.Fixture.Dispose()` -- the same root cause, a different fixture. Rather than keep patching them one at a time as CI happens to surface them, searched every test file in the suite for the vulnerable pattern (`new MainWindow(root)` constructed after an `XmlSource` was already imported into that root, whose auto-select on construction fires the fire-and-forget health check that can still be closing `source-health.db` when `Directory.Delete` runs). Found and fixed three more: `ProductCardPriceFieldsUiTests.cs`, `ProductCardKeyboardSmokeTests.cs`, `ProductOrderReportIntegrationTests.cs` -- all now use the same per-attempt-`ClearAllPools()` retry loop already applied to `SchedulerDecoupleTests` and `ProductCardDirtyDraftTests`. Verified the remaining `new MainWindow(...)` call sites in the suite (`EtsyOAuthSingleFlightTests.cs`, two fixtures in `SafetyAndStabilityTests.cs`) never import an `XmlSource` into their root before constructing the window, so they cannot trigger this race and were left unchanged.

Full suite run twice locally after the fix, 184/184 both times.
REAL_WORK_COUNT=1; FILES_CHANGED=tests/MarketplaceHub.Tests/ProductCardPriceFieldsUiTests.cs, tests/MarketplaceHub.Tests/ProductCardKeyboardSmokeTests.cs, tests/MarketplaceHub.Tests/ProductOrderReportIntegrationTests.cs; TEST_RESULT=184 passed, 0 failed, 0 skipped (full Release suite, 2 consecutive local runs); Release build and self-contained win-x64 publish succeeded; artifact SHA-256 e7b1449507a6c90fe99283595b238679b34908e9245f2775bc823b1951a316aa.

## #692 — XML source URL reachability and latency health check (2026-09-12), branch codex/issue-692-xml-source-health off codex/issue-276-etsy-live-safety-reverify

New issue, no prior audit history; found the exact gap it names by reading the existing code: `MainWindow`'s existing `RefreshXmlSourceHealthAsync` only summarizes *local* state (product count, last import run, quarantine counts) with no network call at all, and the one method that does make a real request (`CheckXmlSourceAsync`, the "Kaynak sağlığını kontrol et" button) is manual-only, measures no latency, classifies every failure through one generic catch-all message, and persists nothing. Separately, `AutomationKind.Health` jobs have existed since earlier this session's #307/#321 work but were **entirely dead**: `AutomationRunner.RunClaimed` enqueues a `"health-check"` `SyncRequest` for them and nothing anywhere in the codebase ever consumes that operation — confirmed by grep, not assumption.

`Catalog/XmlSourceHealthChecker.cs` (new): `XmlSourceHealthChecker.CheckAsync(HttpClient, XmlSource, CancellationToken, TimeSpan? timeout)` reuses the existing `ApiHealthClassifier.FromResponse`/`FromException` (`ApiHealth.cs`) for status classification instead of inventing a second scheme, adding only what that classifier doesn't cover: a `Stopwatch`-measured `LatencyMs`, and the local-file-vs-HTTPS distinction an XML source (unlike a marketplace API) actually has (an existing readable file is `HEALTHY` with near-zero latency and *no* network call at all; a missing/empty file is `CLIENT_ERROR`; a non-HTTPS, non-existent-file location is rejected before any request). A bounded linked-cancellation timeout (default 15s, matching the existing `EtsyHttp.SendJsonAsync`/`XmlSourceReader.ReadAsync` pattern) becomes `TIMEOUT`; genuine external cancellation propagates as `OperationCanceledException` rather than being misreported as a timeout — the same distinction those two existing methods already draw.

`Catalog/Models.cs`: `XmlSource` gained `LastHealthCheckUtc`, `LastHealthState` (default `"NEVER_CHECKED"`), `LastHealthHttpStatus`, `LastHealthLatencyMs`, `LastHealthError` — JSON-blob fields, zero migration, matching the Desi/PriceFields pattern from earlier this session.

`MainWindow.xaml.cs` `ScheduledAsync` (the real production entry point, not a new one): added a `dueHealth` set (every `Enabled` source whose `LastHealthCheckUtc` is stale beyond its own `IntervalMinutes`, independent of `AutoImport` — a source with auto-import off is still worth watching for reachability) and a loop that actually calls `XmlSourceHealthChecker.CheckAsync` and persists the result via the existing `store.SaveSource`. Skips a source that already ran a real import this same tick (that import attempt is itself a stronger reachability signal). Extended the #307 empty-tick guard (`due.Count==0&&dueJobs.Count==0`) to a three-way check so a health-only tick isn't skipped either.

`tests/MarketplaceHub.Tests/XmlSourceHealthCheckerTests.cs` (new, 8 tests): healthy response with measured latency, 404 classified as CLIENT_ERROR, 500 as SERVER_ERROR (distinct states), a slow feed past a 100ms bounded timeout classified TIMEOUT without hanging, genuine external cancellation propagating instead of being misreported as a timeout, an existing local file HEALTHY with *zero* network calls (proven via a handler that throws if ever invoked), a missing local file not silently HEALTHY, and an empty location short-circuiting to NOT_CONFIGURED. `tests/MarketplaceHub.Tests/SchedulerDecoupleTests.cs`: new `ScheduledTickPerformsAndPersistsAHealthCheckForADueEnabledSource` drives the real `ScheduledAsync` production path (not just the standalone checker) with a fake `HttpMessageHandler` — proving `ScheduledAsync` actually performs and persists the check, not merely that the checker function works in isolation. This is exactly the class of proof this session's #285/#276 work established is required: a standalone-helper test does not close a "wire it into the real path" gap.

REAL_WORK_COUNT=1; FILES_CHANGED=Catalog/XmlSourceHealthChecker.cs (new), Catalog/Models.cs, MainWindow.xaml.cs, tests/MarketplaceHub.Tests/XmlSourceHealthCheckerTests.cs (new), tests/MarketplaceHub.Tests/SchedulerDecoupleTests.cs; TEST_RESULT=193 passed, 0 failed, 0 skipped (full Release suite, up from 184); Release build and self-contained win-x64 publish succeeded; artifact SHA-256 9b805bc78a1c1db0efba08a73bfe1d288a992931749909f7c36ee00bb596989e.
Not done: no UI surface for the persisted health fields yet beyond what's already shown by the pre-existing local-summary `RefreshXmlSourceHealthAsync`/`xmlSourceHealth` TextBlock (a follow-up could show `LastHealthLatencyMs`/`LastHealthHttpStatus` there); no alerting/notification on a source going unhealthy (that's #694's "feed fingerprint ve değişim oranı alarmı" territory, a separate issue); Basic-Auth-protected feeds get an unauthenticated reachability ping (a 401 response is itself informative via the AUTH_ERROR classification, but this does not verify the *configured* credentials work).

## #693 — XML content-type and payload signature validation (2026-09-12), branch codex/issue-692-xml-source-health

New issue, no prior audit history. The core risk it names -- a well-formed-enough HTML error/login page can parse "successfully" as generic XML syntax (matching tags) -- is real: writing the failing tests first showed `<!DOCTYPE html>...` was already rejected by the existing `DtdProcessing.Prohibit` setting (a DOCTYPE declaration trips XML's DTD prohibition), but only with a generic "invalid XML" message, and a bare `text/html` content-type with a body that never triggers a DOCTYPE/DTD path was not rejected at all before the parser even ran.

`XmlSourceReader.cs` `ReadAsync`: added an explicit `Content-Type` check rejecting `text/html`/`application/xhtml+xml` before any parsing is attempted (fails fast, before downloading/decoding the body). `ParseAsync`: added an explicit empty-body check (was previously only caught indirectly via a confusing generic XML-parse-error message) and a new `EnsureLooksLikeXml` payload-signature sniff (peeks the first 4KB, skips a BOM, and rejects `<!DOCTYPE html`/`<html` or anything not starting with `<` with a specific HTML-aware message) so a server that *mislabels* its content-type (claims `text/xml` but actually returns an HTML error page) is still caught by the payload itself, not just the header. The pre-existing 25MB size cap and the `XmlReader`'s own encoding auto-detection (which already fails closed on a declared-vs-actual encoding mismatch via `XmlException`) needed no changes — confirmed by test, not assumption.

`tests/MarketplaceHub.Tests/XmlSourcePayloadValidationTests.cs` (new, 6 tests, all via `XmlSourceReader` with a fake `HttpMessageHandler` -- no real network): valid XML accepted normally; an honest `text/html` content-type rejected before parsing even when the body would otherwise parse as valid XML syntax; a *mislabeled* `text/xml` response whose body is actually an HTML error page rejected by its own signature; an empty body rejected with a specific message; an oversized payload rejected without buffering it in full; a declared-UTF-16-but-actually-UTF-8 payload rejected instead of silently importing garbled text.

REAL_WORK_COUNT=1; FILES_CHANGED=XmlSourceReader.cs, tests/MarketplaceHub.Tests/XmlSourcePayloadValidationTests.cs (new), IMPLEMENTATION_STATUS.md; TEST_RESULT=199 passed, 0 failed, 0 skipped (full Release suite, up from 193); Release build and self-contained win-x64 publish succeeded; artifact SHA-256 36d6ca72a4c5e6db4c94576ea108dc87823983e627554a5e8255f2ca773a9d2d.
Not done: the content-type check is a narrow allowlist-of-rejections (HTML family only) rather than a strict allowlist-of-acceptances, to avoid false-rejecting real supplier feeds served with a generic/incorrect-but-not-HTML content-type (e.g. `text/plain`, `application/octet-stream`), which are common in practice; a stricter policy was deliberately not adopted without evidence it wouldn't break real feeds.

## #694 — feed anomaly alarm re-verified against the real import path (2026-09-12), branch codex/issue-692-xml-source-health

New issue; substantially overlaps #283's already-contested anomaly-detection territory (4 prior audit rounds, still open) but is narrower. Read the current code before writing anything: `Catalog/CatalogStore.cs`'s `Import` already calls `EnsureDropshipFeedSafe` -> `DropshipAnomalyGuard.Evaluate` on every import, and that guard already checks `PRODUCT_COUNT_SPIKE` (covers mass addition), `MISSING_PRODUCTS` (mass deletion), `MASS_ZERO_STOCK`, and `PRICE_JUMP` -- all *before* the write transaction starts (fail-closed). The one existing test exercising this via the real path (`SafetyAndStabilityTests.CatalogImport_BlocksMassZeroSupplierFeedBeforeWrite`) only covered the zero-stock case. #694's own TEST list (unchanged, normal delta, mass deletion, mass addition, restart) was **not** actually proven end-to-end anywhere; `DropshipAnomalyGuardFailsClosedAndAuditsOverride` (also pre-existing) only exercises the standalone `DropshipAnomalyGuard` class directly, the same "helper works in isolation, real path unproven" gap pattern this session's #285/#276/#692 work kept finding.

`tests/MarketplaceHub.Tests/FeedAnomalyRealImportPathTests.cs` (new, 5 tests, all via the real `CatalogStore.Import`, no production code changed -- the wiring was already correct): an identical re-submitted feed is not flagged; a 10% count delta is allowed (and clarifies, by assertion, that import is additive/upsert-only -- a normal non-complete batch never deletes products merely absent from it, so "allowed" means "not blocked," not "catalog shrank"); a 75% count drop (mass deletion) is blocked with the catalog left completely unchanged; a 200% count jump (mass addition, e.g. a corrupted/duplicated feed) is blocked the same way; and — the one scenario with no prior direct proof — a **second, independent `CatalogStore` instance pointed at the same directory** (simulating an app restart) still uses the persisted catalog as its anomaly baseline and correctly blocks a mass-deletion feed, rather than treating the restarted process as having no history.

REAL_WORK_COUNT=1 (test-only; re-verification found the wiring already correct, matching this session's established discipline of not rewriting what's already right); FILES_CHANGED=tests/MarketplaceHub.Tests/FeedAnomalyRealImportPathTests.cs (new), IMPLEMENTATION_STATUS.md; TEST_RESULT=204 passed, 0 failed, 0 skipped (full Release suite, up from 199); Release build and self-contained win-x64 publish succeeded; artifact SHA-256 57c403210a8e0286664ecf9f0efdce1151a8e868163a268476cac68290a4a299.

**Not done, deliberately left to #283 rather than re-litigated here:** `EnsureDropshipFeedSafe` hardcodes `CategoryChanges`/`BrandChanges` to `0,0` when building the current-run metrics, so `MASS_TAXONOMY_CHANGE` can never actually fire — this is a real, narrow gap, but it's #283's own previously-identified territory (a 4-times-audited, still-open issue with much larger stated scope: persistent supplier profiles, override audit trail durable across restart, cross-supplier isolation). Fixing the taxonomy-change wiring in isolation here, under a different issue number, would risk exactly the kind of fragmented, duplicate-effort problem #283's own audit trail repeatedly warns against ("aynı düzeltmeyi iki kere yapma"). Content-fingerprint comparison (as opposed to count/price aggregates) was also not added, for the same reason: #694's own use of the word "fingerprint" most likely refers to this count/price-aggregate comparison already in place, not a content-hash diff, and inventing a new content-hash-based anomaly dimension without a concrete requirement for it would be scope invention, not a fix.

## #784/#785 — safe exception serialization + local path disclosure guard (2026-09-12), branch codex/issue-784-785-safe-exception-serialization off codex/issue-692-xml-source-health

New issues, no prior audit history. Found the concrete gap by reading the existing code: `AuditStore.Sanitize` (the codebase's central log/audit redaction function, already covering Authorization headers, query-string secrets, key=value secrets, and email/phone PII) had **no rule at all for a local file path** -- `C:\Users\<real-username>\...` would pass through untouched into any log line, support export, or audit row. Separately, grep confirmed **no global exception handler existed anywhere** (`Application.DispatcherUnhandledException`, `AppDomain.UnhandledException`, `TaskScheduler.UnobservedTaskException` all unwired) -- an exception that reached the top of the stack today would hit .NET's default unhandled-exception behavior with zero safe diagnostic captured first, and every existing call site that DOES catch exceptions (`sync.Fail(id, error.Message)`, `MainWindow.Log`, etc.) only ever kept `.Message`, discarding the stack trace and inner-exception chain entirely.

`AuditStore.cs` `Sanitize`: added two regexes redacting only the username segment of a Windows (`C:\Users\<name>\...`) or Unix/macOS (`/home/<name>/...`, `/Users/<name>/...`) profile path, leaving the rest of the path intact (still useful for diagnostics: "yes, this was under AppData\Local\Temp") and leaving a path with no such segment (relative paths, `Program Files`, etc.) completely untouched -- confirmed by a dedicated negative test.

`SafeExceptionSerializer.cs` (new): `Serialize(Exception, maxDepth=5)` walks the full inner-exception chain (bounded depth, since an unusually deep chain is a realistic risk even though .NET's own chain cannot literally cycle), sanitizing each exception's `Message` and `StackTrace` **independently** rather than sanitizing one giant joined string -- avoiding `Sanitize`'s existing 2000-char cap ever truncating a legitimately long stack trace.

`App.xaml.cs`: was a fully empty template with zero exception handling. Added `OnStartup` wiring for all three global handlers, each calling a new `LogCrash` that writes the sanitized report (with a UTC timestamp) to `%LocalAppData%\MonoBridgeDesktop\crash.log`, wrapped in its own try/catch so a failure while logging a crash can never mask the original exception. Deliberately does **not** set `DispatcherUnhandledException.Handled = true`: a genuinely unhandled exception means the app's state is no longer trustworthy, and this handler's job is to leave a safe diagnostic before the normal crash proceeds, not to keep a corrupted app alive.

`tests/MarketplaceHub.Tests/SafeExceptionSerializationTests.cs` (new, 9 tests): a Windows user-profile path has its username redacted; the same holds for a path under that profile's Temp folder; a normal relative path is left byte-for-byte untouched (proves no over-redaction); `Serialize` preserves exception type, message, and a real stack trace (not a placeholder); it walks a genuine inner-exception chain; a secret embedded in a message is redacted; a local path embedded in a message is redacted; a bearer token embedded in an HTTP error message is redacted (via the pre-existing `Sanitize` pattern, confirming the wiring reuses it rather than reimplementing); and an unusually deep exception chain is capped rather than walked without bound. `App.LogCrash`'s file-write side effect was deliberately left untested directly (it targets the same fixed real `%LocalAppData%` path as `CredentialStore` did before this session's earlier fix) -- its only real logic is `SafeExceptionSerializer.Serialize`, already fully covered.

REAL_WORK_COUNT=1; FILES_CHANGED=AuditStore.cs, SafeExceptionSerializer.cs (new), App.xaml.cs, tests/MarketplaceHub.Tests/SafeExceptionSerializationTests.cs (new), IMPLEMENTATION_STATUS.md; TEST_RESULT=213 passed, 0 failed, 0 skipped (full Release suite, up from 204); Release build and self-contained win-x64 publish succeeded; artifact SHA-256 747b71e0f7e4f1dcb231e011588cc7d6882abbd7987456c8126b6b8e6eba640f.
Not done: existing catch sites throughout the app (`sync.Fail(id, error.Message)`, `MainWindow.Log`, job-failure records) still only persist `.Message`, not the full `SafeExceptionSerializer.Serialize` report -- retrofitting every one of those call sites to capture full stack/inner-exception detail is a much larger, higher-blast-radius change than adding the previously-nonexistent global safety net, and was left out of this pass; "token-like value" detection is limited to the pre-existing keyword/header-based patterns in `Sanitize` (e.g. `Authorization: Bearer ...`, `key=value`) -- a bare high-entropy string with no such marker is not detected, since heuristic secret-shape detection risks false-positive redaction of legitimate non-secret values (SKUs, GUIDs) without a concrete pattern to anchor on.

## #786 — clipboard copy redaction core (2026-09-12), branch codex/issue-784-785-safe-exception-serialization

New issue. Checked the premise before writing anything: grep found **zero** `Clipboard.*` call sites anywhere in this app -- there is no copy-to-clipboard action on any diagnostic, order, or customer screen today. The issue's own wording ("copy-to-clipboard aksiyonlarında... classification uygula") assumes such actions already exist and just need classification added; they don't. Building actual copy commands across the diagnostics/order/customer screens would be new UI feature work on multiple screens, not a security fix to existing functionality, so that was deliberately not invented here.

`ClipboardRedaction.cs` (new): the classification/masking core a future copy command can call. Reuses `AuditStore.Sanitize` for content-based detection (if sanitizing a value changes it, the value contained something Sanitize already knows to redact — a secret, a token, PII, or a local path) rather than duplicating pattern-matching; adds a small explicit field-name list (`Token`, `Password`, `Secret`, etc.) for fields whose *entire* value is inherently sensitive, where content-based detection has nothing to anchor on and full masking (not partial, in-place redaction) is the safer choice.

`tests/MarketplaceHub.Tests/ClipboardRedactionTests.cs` (new, 5 tests): a field named as a secret is never copied raw regardless of its value; a bearer token embedded in a generic field's text is redacted by content; an email or phone number embedded in generic text is redacted; plain non-sensitive text is copied byte-for-byte unchanged (proves no over-redaction); and a mixed multi-field "selection" is judged field-by-field rather than masking the whole batch because one field was sensitive, or letting one sensitive field slip through because most weren't.

REAL_WORK_COUNT=1; FILES_CHANGED=ClipboardRedaction.cs (new), tests/MarketplaceHub.Tests/ClipboardRedactionTests.cs (new), IMPLEMENTATION_STATUS.md; TEST_RESULT=218 passed, 0 failed, 0 skipped (full Release suite, up from 213); Release build and self-contained win-x64 publish succeeded; artifact SHA-256 bfd77faa9d7193fa7828e001a3dc741ae7d0fb3404800b775e6096325a4b6ccc.
Not done: no actual copy-to-clipboard UI commands exist yet on any screen (diagnostics, order, customer, or elsewhere) for this to be wired into -- that is a separate, larger feature-build task, not a redaction fix, and is explicitly out of scope for this pass.

## #779 — SQLite busy/locked diagnostics (2026-09-12), branch codex/issue-779-sqlite-busy-diagnostics off codex/issue-784-785-safe-exception-serialization

New issue. Found the existing state by reading the code first: `SqliteConnectionPolicy.Open` (the shared connection factory most stores already use) already sets `PRAGMA busy_timeout=15000` on every connection -- SQLite's own engine already waits/retries internally for 15 seconds before ever raising SQLITE_BUSY/SQLITE_LOCKED. That is already a real, working retry mechanism; the gap was what happens once that window genuinely expires. `MainWindow.Safe(Exception)` (the central point most operations funnel their error message through for the user) classified anything that wasn't `InvalidOperationException`/`ArgumentException` into one generic "İşlem tamamlanamadı. Dosya biçimini, erişim izinlerini ve bağlantıyı kontrol et." bucket -- a persistent database lock would land there, indistinguishable from a corrupt file or a permissions problem, with no signal to the user that simply retrying in a few seconds might work.

`SqliteBusyDiagnostics.cs` (new): `IsBusyOrLocked(Exception)` recognizes a real `SqliteException` with `SqliteErrorCode` 5 (SQLITE_BUSY) or 6 (SQLITE_LOCKED); `Describe` returns a clear, actionable Turkish message for that case. `MainWindow.xaml.cs` `Safe(Exception e)`: checks this classification first, before falling through to the existing generic buckets.

Deliberately did **not** add a second, application-level retry loop on top of SQLite's own 15-second `busy_timeout` wait: a "transient lock" already resolves transparently within that window with no code change needed, and a "persistent lock" should fail with a clear message promptly once the window expires, not be made to wait even longer by an additional retry layer stacked on top.

`tests/MarketplaceHub.Tests/SqliteBusyDiagnosticsTests.cs` (new, 3 tests, all against a **real** SQLite lock, not a hand-constructed fake exception): one connection holds an uncommitted write transaction on a file while a second, contending connection attempts a write and receives a genuine `SqliteException`, correctly classified and described; an unrelated `SqliteException` (querying a nonexistent table) is not misclassified as busy; a non-SQLite exception is never misclassified. Along the way, discovered and worked around a real Microsoft.Data.Sqlite gotcha worth recording: its `SqliteConnectionStringBuilder.DefaultTimeout` connection-string property silently overrides a `PRAGMA busy_timeout` issued after `Open()`, and `DefaultTimeout=0` means "wait forever" (following ADO.NET `CommandTimeout` convention), not "fail immediately" -- the first version of this test hung for that exact reason before being fixed to use `DefaultTimeout=1`.

REAL_WORK_COUNT=1; FILES_CHANGED=SqliteBusyDiagnostics.cs (new), MainWindow.xaml.cs, tests/MarketplaceHub.Tests/SqliteBusyDiagnosticsTests.cs (new), IMPLEMENTATION_STATUS.md; TEST_RESULT=221 passed, 0 failed, 0 skipped (full Release suite, up from 218); Release build and self-contained win-x64 publish succeeded; artifact SHA-256 9c4e5195183b27195bd570658ce4b72bbf2e0722476aacd3647eadc61518e83e.
Not done: `Safe(Exception)` is the central point for most MainWindow-originated operations, but not the only catch site in the app (e.g., `AutomationRunner.RunClaimed`'s per-product catch already records `error.Message` directly into a `SyncRequest` failure without going through `Safe`) -- a full sweep of every catch site to route through this classifier was not attempted, since most of those are backend job-failure records rather than user-facing terminal diagnostics, which is what the issue's own "ENTRY: DB write/read critical paths" points at.

## #780 — WAL checkpoint health diagnostics (2026-09-12), branch codex/issue-779-sqlite-busy-diagnostics

New issue. `DatabaseHealth.Inspect` (the existing "ENTRY: health/maintenance checks" point) already covered `quick_check`, schema version, `journal_mode`, and `foreign_keys` via a read-only connection; it reported nothing about the WAL file's actual size or whether a checkpoint could make progress.

`DatabaseHealth.cs`: `DatabaseHealthResult` gained two additive fields (`WalSizeBytes`, `CheckpointBusy`, both defaulted so every existing call site is unaffected). A new `InspectWal` reads the `.db-wal` file's real size directly from the filesystem (zero risk, no connection needed) and separately opens a short-lived, properly read-write connection (the existing checks stay on the read-only one) to run `PRAGMA wal_checkpoint(PASSIVE)` -- SQLite's own non-disruptive checkpoint mode, which never blocks or forces out a concurrent reader/writer, so this is safe to run against a live, in-use database as a routine health check. No size or duration threshold is hardcoded anywhere, per the issue's own "hardcoded tuning yok" -- the raw byte count is reported as-is.

Writing the "locked checkpoint" test first caught a real distinction worth recording: `PRAGMA wal_checkpoint`'s `busy` column alone only means the checkpoint *couldn't even start* (e.g. a conflicting checkpoint already in progress); a long-open reader instead lets it run to completion but only partially (`checkpointed < log` in the pragma's own result columns) without ever setting `busy`. The initial implementation only looked at `busy` and the "locked checkpoint" test failed for exactly this reason (a single open reader transaction correctly did *not* set busy=1) -- fixed by treating `busy || checkpointed < log` as the single "needs attention" signal, since both are legitimately the same "the WAL isn't shrinking" condition from a diagnostic's point of view.

`tests/MarketplaceHub.Tests/WalCheckpointHealthTests.cs` (new, 4 tests, all against a real SQLite WAL file, not mocked): a fresh WAL with no contention checkpoints cleanly; a WAL grown by 2000 uncheckpointed inserts (while a held reader prevents checkpointing) reports its actual multi-hundred-KB size, not a guess; an open reader blocking a full checkpoint is reported as busy without throwing or hanging; and the WAL size is still correctly read after closing and reopening all connections (simulating a restart).

REAL_WORK_COUNT=1; FILES_CHANGED=DatabaseHealth.cs, tests/MarketplaceHub.Tests/WalCheckpointHealthTests.cs (new), IMPLEMENTATION_STATUS.md; TEST_RESULT=225 passed, 0 failed, 0 skipped (full Release suite, up from 221); Release build and self-contained win-x64 publish succeeded; artifact SHA-256 72929e15cf666ddc35c8adf161e1d33148f6ed4de4f540af0ef8c990acec458e.
Not done: no UI surface displays these new fields yet (the existing health check has no visible panel in the app that this session found wired to it either, beyond whatever calls `DatabaseHealth.Inspect` directly); no automatic corrective action (e.g. triggering a more aggressive checkpoint mode) is taken when a checkpoint is reported as blocked -- this issue asked for diagnostics, not remediation, and taking an aggressive checkpoint action automatically would risk exactly the kind of disruptive behavior PASSIVE mode was chosen to avoid.

## #781 — order list query plan (2026-09-12), branch codex/issue-781-order-list-query-plan off codex/issue-779-sqlite-busy-diagnostics

New issue. Measured the real query plan before changing anything: `OrdersStore.ReadPage` filters by `marketplace`+`shop` (the common "one store's order list" view) and always sorts by `json_extract(payload,'$.UpdatedAt') DESC`, but the three existing indexes were each single-purpose (marketplace+shop alone; status alone; UpdatedAt alone) -- none of them let SQLite satisfy both the filter and the sort from one index walk. `EXPLAIN QUERY PLAN` on the real query confirmed exactly this: `SEARCH orders USING INDEX IX_orders_marketplace_shop (...) | USE TEMP B-TREE FOR ORDER BY` -- a real, measurable extra sort step on every single call to the most common order-list view, not a micro-optimization.

`OrdersStore.cs`: added `IX_orders_shop_updated ON orders(marketplace,shop,json_extract(payload,'$.UpdatedAt') DESC)` -- a compound index covering both the filter and the sort together, so the query can be satisfied by one index walk. `CREATE INDEX IF NOT EXISTS` on an already-open database is a no-op cost-wise for existing installs (SQLite skips it if present) and adds the index on next open otherwise -- no migration step needed.

`tests/MarketplaceHub.Tests/OrderListQueryPlanTests.cs` (new, 2 tests): `EXPLAIN QUERY PLAN` on the store-scoped, date-sorted query no longer contains `TEMP B-TREE` and does show `USING INDEX` (deterministic, machine-speed-independent proof, not a timing assertion); and a realistic 5,000-order dataset across two marketplaces and two shops still returns the correct page (right count, correctly filtered, correctly ordered newest-first) after the index change.

REAL_WORK_COUNT=1; FILES_CHANGED=OrdersStore.cs, tests/MarketplaceHub.Tests/OrderListQueryPlanTests.cs (new), IMPLEMENTATION_STATUS.md; TEST_RESULT=227 passed, 0 failed, 0 skipped (full Release suite, up from 225); Release build and self-contained win-x64 publish succeeded; artifact SHA-256 37b300bee9f350adb76de3ebd13f184cb4d2db76de4aa673293dc2d3eb5b9d50.
Not done: the issue's own "100k orders" test scale was not run as a wall-clock timing benchmark (5,000 rows was used for the functional-correctness test); the `EXPLAIN QUERY PLAN` structural proof is the primary evidence here since it is dataset-size-independent -- the query plan a compound index produces at 5,000 rows is the same plan it produces at 100,000, so a much larger synthetic dataset would mostly cost test time without adding proof; the status+date-sort combination (a query filtering by `RawStatus` and sorting by `UpdatedAt`) was not given its own compound index, since `ReadPage`'s status filter is typically combined with a marketplace/shop scope already covered by the new index, and adding an index for every possible filter combination without a demonstrated real query shape hitting it would be speculative tuning.

## #777 — orphan reference audit in the health check (2026-09-12), branch codex/issue-777-orphan-reference-audit off codex/issue-781-order-list-query-plan

New issue. Checked what "foreign key" even means here before writing anything: this app's product/source/mapping relationships are *logical* references -- `CatalogProducts.$.SourceId` inside a JSON blob pointing at `Sources.Id`, and `MarketplaceMappings.LocalId` pointing at `CatalogProducts.Id` -- not declared SQL foreign keys. So `PRAGMA foreign_key_check` can never see them, and `PRAGMA foreign_keys` being on or off is irrelevant to them; an audit has to resolve them itself. No `DeleteSource` exists in the store today, so an orphan product is a corruption/migration artifact rather than a normal-flow outcome, which is exactly what a diagnostic audit is for.

`DatabaseHealth.cs` (the existing "ENTRY: health check"): `DatabaseHealthResult` gained three additive, defaulted fields (`OrphanProductCount`, `OrphanMappingCount`, `RepairPreview`) so every existing caller and the pre-existing `DatabaseHealthInspectsReadOnlySqliteAndDetectsMissingDatabase` test are unaffected. New `InspectReferences` runs on the same read-only connection as the integrity checks and is a repair *preview* (counts plus up to three sample ids per category, surfaced in `Detail` as "Sahipsiz kayıt: ..."), never a repair. Two deliberate scoping choices: an empty `SourceId` is "no source" by design and is not counted as dangling; and `MarketplaceMappings` is created lazily by `MarketplaceMappingStore`, so a database that predates it simply has no such table -- treated as a legitimate legacy state yielding zero orphan mappings, not an error.

That legacy-robustness point bit immediately in the other direction: the first version assumed `CatalogProducts`/`Sources` always exist, and the four `WalCheckpointHealthTests` (#780) -- which build a bare `catalog.db` with raw SQL and no catalog schema -- flipped the whole health check to `ERROR` via "no such table". Fixed by guarding those two tables the same way: a partially-initialized or foreign `catalog.db` still gets the integrity check, with nothing to audit. The suite caught a real gap in the new code, not a test-side artifact.

`tests/MarketplaceHub.Tests/OrphanReferenceAuditTests.cs` (new, 4 tests matching the issue's own TEST list, all against a real `catalog.db` built by the real stores): a valid database reports 0/0 and an empty preview; two products whose `Sources` row was deleted underneath them plus one mapping pointing at a nonexistent product are counted exactly (and the valid mapping is not), with both ids named in the preview; a legacy database with no `MarketplaceMappings` table and `foreign_keys` at SQLite's default (off) is still fully audited without turning into an error; and the preview is read-only and idempotent (row count unchanged after two inspections, identical output both times).

REAL_WORK_COUNT=1; FILES_CHANGED=DatabaseHealth.cs, tests/MarketplaceHub.Tests/OrphanReferenceAuditTests.cs (new), IMPLEMENTATION_STATUS.md; TEST_RESULT=231 passed, 0 failed, 0 skipped (full Release suite, up from 227); Release build and self-contained win-x64 publish succeeded; artifact SHA-256 8f5877e08efb16842b56c6a9382b1b86cd87b9f1e329e69ac7467998163e42cc.
Not done: no actual repair action (deleting orphan mappings, or re-parenting/quarantining orphan products) -- the issue asked for an audit with a repair *preview*, and an automatic delete of rows on a health check would be exactly the kind of unrequested destructive action a diagnostic must not take; cross-database references (`media.db`'s `ProductMedia` -> product id, `SyncJobs.EntityId`, which doubles as an Etsy listing id for the etsy channel and so cannot be resolved as a product id) were not audited; orphans do not change `Status` (still integrity/schema-driven) -- they are reported in their own fields and in `Detail`.

## #778 — transaction boundary audit of multi-step mutations (2026-09-12), branch codex/issue-778-transaction-boundaries off codex/issue-777-orphan-reference-audit

New issue. This one is an audit first, so the deliverable had to start with an honest inventory rather than a speculative refactor. Read every multi-step product/order/import mutation: `CatalogStore.ImportCore` (products plus the source-state row in one non-deferred transaction, `ReconcileMissingSource` deliberately after commit because it writes a different database), `SaveProduct`/`DeleteProduct`/`Undo`/`SaveSource` (each one transaction), `OrdersStore.Save` (one transaction per batch, upsert per order), `MarketplaceMappingStore.Save` (single upsert), and every lease/claim in `XmlRunStore`, `AutomationStore` and `SyncStore` (`TryStart`, `TryClaimLease`, `Complete`/`Fail` are single conditional `UPDATE`s -- atomic by construction; `AutomationStore.Complete/Fail` do a read first, but the read is not a write, and the `WHERE LeaseToken=$token` guard is what makes them safe). Result: those boundaries are already correct.

The one real half-commit was in `AutomationRunner.RunClaimed`'s per-product catch block: `sync.Enqueue(...)` (an `INSERT` in `Pending`) followed by `sync.Fail(id, message)` (an `UPDATE` to `Failed`) -- two statements on two connections with nothing tying them together. Between them the `":error"`-payload job is `Pending`, i.e. exactly the state `TryStart` dispatches; a crash there (or the `Fail` call throwing, e.g. `SQLITE_BUSY`) leaves a bogus job live in the queue whose payload is the literal word `error`. Fixed with `SyncStore.EnqueueFailed(request, error)`: one `INSERT ... ON CONFLICT(Channel,ShopId,Operation,EntityId,Version) DO UPDATE` written directly in `Failed` with `FailureCount=1`, so no `Pending` state ever exists for a failure record, and a repeat of the same failure bumps the count instead of creating a duplicate -- the same idempotency contract `Enqueue` already has. `RunClaimed` now calls it; nothing else changed.

`tests/MarketplaceHub.Tests/TransactionBoundaryTests.cs` (new, 4 tests, real stores). The interesting one proves the window rather than just the end state: a `SyncJobsTrace` table fed by `AFTER INSERT`/`AFTER UPDATE` triggers records every status the row ever had, at machine speed and independent of timing. Run against the old two-call code (fix stashed) it fails with `Expected:<0>. Actual:<1>` -- the row was observably `Pending` -- and passes with the atomic write; the failure is injected through the real path (a price policy with no expense fields is blocked by the #285 money preflight inside `PreviewPrice`). The other three: same failure twice → one row, `FailureCount=2`, never `Pending`; an import batch whose third row is a duplicate SKU rolls back rows one and two *and* the source-state row to the last committed feed (`feed-1`, count 3 -- verified against a real prior commit, not an empty database); and a failure record can still be retried by an operator while a job orphaned `Running` by a crash is recovered to `Pending` on the next launch without being duplicated (retry + restart scenarios from the issue's TEST list).

REAL_WORK_COUNT=1; FILES_CHANGED=Catalog/SyncStore.cs, Catalog/AutomationRunner.cs, tests/MarketplaceHub.Tests/TransactionBoundaryTests.cs (new), IMPLEMENTATION_STATUS.md; TEST_RESULT=235 passed, 0 failed, 0 skipped (full Release suite, up from 231); Release build and self-contained win-x64 publish succeeded; artifact SHA-256 4c1e8aadbd9138be2d14c39649e97b5ef95446e01e6167801dcc2b3e6c703ad4.
Not done: `SyncStore.Enqueue` is find-then-`INSERT` (two statements) -- not a half-commit, but two concurrent enqueues of the same key would make the loser throw `SQLITE_CONSTRAINT` instead of returning the existing row; left as is because there is one scheduler thread today, noted for when there is not. `ImportCore` + `ReconcileMissingSource` span two databases (`catalog.db`, `source-health.db`) and cannot share a transaction; a crash between them loses one quarantine observation, which the next complete feed re-derives, so no data is permanently wrong -- documented, not changed. No cross-database two-phase commit was attempted.

## #783 — dashboard aggregate snapshot cache (2026-09-12), branch codex/issue-783-dashboard-aggregate-cache off codex/issue-778-transaction-boundaries

New issue. ENTRY is `DashboardPanel` -> `DashboardDataService.Load()`, which is the first thing shown after every navigation and aggregates every store from scratch each time: all products, all orders, all sync jobs, the last 100 XML runs, sources, connections and the data-quality summary. `DashboardPanel.Create` also constructs a fresh service per navigation, so any cache had to be process-wide, keyed by data directory, to ever be warm on the real path.

`DashboardData.cs`: `Load()` is now `Load(bypassCache)` over a `ConcurrentDictionary<directory, (revision, storedUtc, snapshot)>`, with the old body as `Build()`. An entry is served only when it is younger than `MaxAge` (default 10 s, injectable with a clock for tests) *and* its revision still matches. "Revision-aware" is done without touching a single store: the revision is computed from the SQLite files themselves -- name, length and the first 32 bytes (WAL header salts / checkpoint sequence, or the rollback-journal change counter) of every `*.db` and `*.db-wal` under the directory, read through a file handle so an open writer's bytes are seen rather than stale directory metadata (`-shm` is excluded because readers touch it). Every committed write appends WAL frames or rewrites the WAL header, so a product import, a source save, an order sync or a job state change from this or any other process invalidates the entry immediately; the TTL is only the bound on staleness if something ever escaped that signal. Two details that matter: the revision is captured *after* the build, because opening the stores itself writes (schema checks, and `SyncStore.Open` rebuilds its unique index on every open) and those writes must not read as "changed" on the very next load; and a failed build throws before anything is stored, so a failure is never cached and never masks the next attempt. `DashboardPanel.cs`: navigation loads through the cache; the user's explicit "Durumu yenile" click calls `Load(bypassCache: true)` -- an explicit refresh is a request for an authoritative re-read and must never be answered from a cache.

`tests/MarketplaceHub.Tests/DashboardAggregateCacheTests.cs` (new, 6 tests, real stores in temp directories). Cache behaviour is judged by identity (`ReferenceEquals` on the immutable snapshot record), which is deterministic and machine-speed independent, unlike timing. Written red first: the warm-load test fails against the old service because every `Load()` produced a new instance. Covered: warm load across two service instances returns the same snapshot; a product import (a second supplier's first feed -- the first version of this test tripped the #776 dropship anomaly gate with a +100% count spike on the same source, which was the test's mistake, not the cache's) and then a `SaveSource` each invalidate immediately with the right counts; a store switch serves each directory its own snapshot and switching back is still warm; past `MaxAge` the snapshot is rebuilt with no disk change; an explicit refresh bypasses and replaces the entry; and a store corrupted underneath a warm cache makes the next load throw `SqliteException` rather than serve the stale snapshot, after which a repaired store loads from scratch (the failure was not cached).

REAL_WORK_COUNT=1; FILES_CHANGED=DashboardData.cs, DashboardPanel.cs, tests/MarketplaceHub.Tests/DashboardAggregateCacheTests.cs (new), IMPLEMENTATION_STATUS.md; TEST_RESULT=241 passed, 0 failed, 0 skipped (full Release suite, up from 235); Release build and self-contained win-x64 publish succeeded; artifact SHA-256 5b338b9fa8ad2cc83327ae6a9a618d962f02e849b5e3fa2ff23e60c732549f55.
Not done: no per-query caching inside `Build()` (each aggregate still reads its whole table when a rebuild happens; the issue asked for the aggregate *result* to be cached, which this is); the dashboard's 14-day order trend still uses `DateTime.Today`, so a cached snapshot can be up to `MaxAge` stale across midnight; `SyncStore.Open` dropping and recreating `UX_SyncJobs_Key` on every open is a real write-per-read cost discovered here and left for its own issue rather than folded into this one; no UI-level test of the "Durumu yenile" bypass (the wiring is a one-line argument change, exercised through the service tests).

## #782 — order timeline lazy loading (2026-09-12), branch codex/issue-782-order-timeline-lazy-loading off codex/issue-783-dashboard-aggregate-cache

New issue. ENTRY is the order detail pane: `OrdersPanel.Edit` -> `ShipmentEditor`, which created one `TextBlock` per observation for the whole shipment history in a single loop on the UI thread, every time an order or shipment was selected. The events are already in memory (they travel inside the order's JSON payload), so the cost is element creation and layout, and for a long history that is a UI-thread stall on every selection.

`OrderTimelineLoader.cs` (new, no WPF dependency): orders newest-first (as before), owns the cursor, and appends one page (50) at a time in chunks (25) through a caller-supplied sink, awaiting a caller-supplied `yield` between chunks. Two properties carry the issue's guarantees: the cursor advances per item actually appended, so a cancelled page leaves the cursor exactly at what is on screen and a later resume neither skips nor repeats; and a request while a page is in flight, or for a cursor that is not the current position, appends nothing -- a double click or a late scroll event can never append the same page twice. `ShouldLoadOnScroll` is the scroll predicate (bottom within 64 px, or content shorter than the viewport; an unmeasured pane never triggers). `OrdersPanel.cs`: `ShipmentEditor` renders the header count, a timeline panel, and a "Daha fazla gözlem yükle (n kaldı)" button; the yield is `Dispatcher.Yield(DispatcherPriority.Background)` so input is processed between chunks; the detail `ScrollViewer`'s `ScrollChanged` requests the next page near the bottom; `Edit` (refresh / order switch), shipment switch and `Unloaded` cancel the in-flight page through a per-editor `CancellationTokenSource`.

Tests, red first (the loader tests failed to compile without the class). `OrderTimelineLazyLoadingTests.cs` (6): 1000 events paged newest-first across 20 pages with no loss or duplication and exactly one yield per page; duplicate request while a page is in flight, a stale cursor and an arbitrary cursor all append nothing while the correct cursor continues; refresh restarts from the newest event with the identical first page; cancellation stops at a chunk boundary, the cursor counts only what was appended, and a fresh token resumes without gaps; the scroll predicate; an empty history. `OrderTimelineUiTests.cs` (1, on the real entry path): a real `OrdersStore` with a 1000-observation order and a 3-observation order, the real `OrdersPanel`, selection through the real `DataGrid` -- opening renders exactly 50 lines, one click appends 50 more, two rapid clicks append exactly one page (the second is rejected as a duplicate of the page in flight), and switching orders while a page is in flight shows only the new order's 3 lines with the "more" button collapsed.

That UI test found a real subtlety worth recording rather than a bug in the feature: on a bare STA test thread with no `SynchronizationContext`, the loader's `await yield()` continuation was resumed on the thread pool -- `Dispatcher.Yield` completes *inside* a dispatcher operation, where WPF installs a `DispatcherSynchronizationContext`, so the runtime refuses to inline a continuation that was registered without one. The production UI thread always has that context (continuations post back at Normal priority), and the existing MainWindow tests install it by hand; the new test now does the same. Diagnosed with a throwaway experiment (three pumping strategies, the wrapped `Func<Task>`, and the real loader), deleted afterwards. Also learned in the same test: `OrdersStore.SaveManual` appends its own state observation when the shipment state differs from the last event's, which is why the seeded 1000 becomes 1001 and the seeded 3 stays 3 -- asserted as such.

REAL_WORK_COUNT=1; FILES_CHANGED=OrderTimelineLoader.cs (new), OrdersPanel.cs, tests/MarketplaceHub.Tests/OrderTimelineLazyLoadingTests.cs (new), tests/MarketplaceHub.Tests/OrderTimelineUiTests.cs (new), IMPLEMENTATION_STATUS.md; TEST_RESULT=248 passed, 0 failed, 0 skipped (full Release suite, up from 241); Release build and self-contained win-x64 publish succeeded; artifact SHA-256 a200f014d61e006e8af5f0b9568011bdea4df63cc61965d58edee7dc3bdc6fdd.
Not done: the scroll-driven paging is covered only by the predicate test, not by a laid-out UI test (the detail pane is never measured in the UI test, deliberately, so paging there is button-driven and deterministic); the rest of the order detail (items, stock preview) is still rendered eagerly -- those lists are small by nature; no UI virtualization (`ListBox`/`VirtualizingStackPanel`) was introduced, because the issue asked for lazy/paged loading and virtualization would have changed the pane's selection and keyboard behaviour; the per-event `TextBlock` is still a wrapped text block rather than a lighter run.

## #790 — pricing provenance integration suite (2026-09-12), branch codex/issue-790-pricing-provenance-suite off codex/issue-782-order-timeline-lazy-loading

New issue (TEST). The suite runs the whole chain -- FX, commission, shipping, VAT, rounding, minimum-margin guard -- through the real pricing entry point `CatalogStore.PreviewPrice` (and `AutomationRunner.RunDue` where dispatch is the point) against a real `catalog.db`, never the standalone calculator. Fixture: cost 100 TRY, commission 20% of sale, shipping 10 TRY, so a TRY sale S nets 0.8·S − 110 and the zero-contribution point is S = 137.50 exactly; every expectation below is derived from that by hand.

Writing the suite found a real regression, which is the whole reason a suite like this exists: `PricePolicy.MinimumMarginTry` -- the operator's "at least N TRY per sale" -- is edited in `MainWindow.PricePolicies`, shown in `PolicyCenterPanel`, copied by `PolicyCenterStore`, validated non-negative on save, and **enforced nowhere**. Before #285 it was compared with the naive formula-minus-cost (which ignored every fee); #285 replaced that check with the money gate and dropped the guard entirely, so a policy with a 25 TRY minimum happily queued a price whose real contribution was 18 TRY (naive diff +60). Fixed in `PreviewPrice` right after the money preflight: `money.NetContribution < p.MinimumMarginTry` throws `"Fiyat gönderimi engellendi: net katkı 18.00 TRY, asgari kâr 25.00 TRY altında; local/min-25."` -- the guard now means what the operator typed, after commission, shipping, transaction cost and VAT, and the message carries the numbers and the channel/shop it applies to. Invariant formatting so the text is culture-independent.

`tests/MarketplaceHub.Tests/PricingProvenanceSuiteTests.cs` (new, 6 tests; the two minimum-margin tests were red first -- one on `PreviewPrice`, one on the real automation dispatch which queued the under-margin price). Rounding boundary: `x*1.375` (137.500, net exactly 0) and `x*1.375005` (→137.50) are blocked, `x*1.37505` rounds half-away-from-zero to 137.51 and passes with the preview showing 137.51 -- banker's rounding would have blocked it. Tax change: 160 TRY is Ready at 0% VAT, becomes `BlockedNegativeMargin` once the operator records 20% VAT *included* (133.33 − 32 − 10 − 100 = −8.67), and stays Ready with the same 20% on a VAT-exclusive price. Missing fee: each of shipping/commission/VAT/transaction missing on its own blocks with `BlockedMissingInput`; the complete policy is the positive control. FX leg: a USD policy at 30 TRY/USD with a 1-hour-old observation converts 159.90 TRY to 5.33 USD and the preview carries the provenance (`FormulaPriceTry` 159.9, `CostTry` 100, currency USD); a 25-hour-old observation is `BlockedStaleFx`; no observation at all is `BlockedMissingInput`. Minimum margin: 25 blocks an 18.00 contribution, exactly 18 is allowed, 18.01 blocks; through `RunDue` nothing is queued and the recorded failure names the guard.

REAL_WORK_COUNT=1; FILES_CHANGED=Catalog/PricePolicy.cs, tests/MarketplaceHub.Tests/PricingProvenanceSuiteTests.cs (new), IMPLEMENTATION_STATUS.md; TEST_RESULT=254 passed, 0 failed, 0 skipped (full Release suite, up from 248); Release build and self-contained win-x64 publish succeeded; artifact SHA-256 2fbfad4bdc3528ffb26391b1b3f69008857d4b714f9647a7dda8ee29006cbc50.
Not done: `PricePreview` still does not carry the net contribution / status itself (the guard's numbers live in the block message; adding `NetContributionTry` to the preview record would be the next provenance step and touches the UI preview grid); `MinimumPrice` (the floor on the sale price) was already enforced and is not re-tested here; the `PriceFieldName` path shares the same gate and guard but the suite exercises only the formula path; no test of `PolicyCenterStore`'s policy copy carrying `MinimumMarginTry` across shops.

## #787 — store isolation integration suite (2026-09-12), branch codex/issue-787-store-isolation-suite off codex/issue-790-pricing-provenance-suite

New issue (TEST). First question was what "store" means here, because the suite is only worth anything against the real model: there is one data directory per installation and no store switch in the app; the operator's stores are the marketplace shops (`ShopId`), and every shop-owned record is keyed by (channel|marketplace, shop, ...) -- orders, stock receipts/movements/restores, price and stock policies, listing mappings, sync jobs, channel plans, connections. The product master and the XML supplier sources are deliberately shared (one catalog, many storefronts). So isolation means: shop A's records are never visible to, borrowed by, or overwritten from shop B's scope, through the real repository/service entry points (`OrdersStore.ReadPage`, `OrderStockDecisionService`, `CatalogStore.PreviewPrice` + `AutomationRunner.RunDue`, `MarketplaceMappingStore`, `OrderTransferCodec`, `GlobalSearchIndexService`), never raw SQL; and the unscoped reads that do exist (`ReadAll`, global search) must attribute every record to its shop rather than merge identities.

`tests/MarketplaceHub.Tests/StoreIsolationSuiteTests.cs` (new, 6 tests, all with two shops holding an order under the *same* number `o-1`): shop-scoped order read/search/report -- shop A's `o-1` is shop A's record, a text search issued in shop B's scope never finds shop A's item, an unknown shop sees nothing, a product-order report fed from shop B's scoped read counts nothing of shop A's, and the unscoped read is an explicit all-shops view; a stock deduction applied for shop A's `o-1` neither marks shop B's `o-1` as already applied nor blocks it, while both draw once from the one shared stock; a price policy is never borrowed across shops and the two shops' automation jobs produce one sync record each, shop A's staying `Pending` while shop B's fails for lack of a policy; listing mappings resolve only within their shop and both shops may map the same product to the same external id without overwriting each other; an order export built from a shop-scoped read carries only that shop and round-trips; global search returns two attributed hits for `o-1`, never one merged record.

Red first, and the red was a real defect rather than a missing isolation: the shop-scoped search `ReadPage(shopId: "shop-a", query: "Kırmızı")` returned 0 for an order whose item is literally "Kırmızı kupa". `OrdersStore.ReadPage` matches the query with `payload LIKE '%…%'` against the raw JSON payload, and `System.Text.Json` escapes every non-ASCII character on write (the dotless i is stored as a backslash-u escape), so any order search containing ı/ğ/ş/ç/ö/ü silently found nothing -- for a Turkish-language operator that is most product names. Fixed in `OrdersStore.cs` by matching the query in both forms: raw (ids, and any payload written unescaped) and JSON-encoded exactly as the serializer writes it (`JavaScriptEncoder.Default.Encode`); no migration, no change to what is stored, and the #781 compound-index query plan is unaffected (`OrderListQueryPlanTests` still pass).

REAL_WORK_COUNT=1; FILES_CHANGED=OrdersStore.cs, tests/MarketplaceHub.Tests/StoreIsolationSuiteTests.cs (new), IMPLEMENTATION_STATUS.md; TEST_RESULT=260 passed, 0 failed, 0 skipped (full Release suite, up from 254); Release build and self-contained win-x64 publish succeeded; artifact SHA-256 39ba1aadc0e46ee0ace5ef81773f3cd684627633969a057440467514d6b58de9.
Not done: the search fix makes Turkish text findable but is still case-sensitive for non-ASCII letters ("KIRMIZI" does not find "Kırmızı"; SQLite's LIKE folds ASCII only) -- a proper fix is a normalized search column, which is a schema change and its own issue; `ReturnManagement.ValidateBatch`'s shop filter was already covered by an existing test and is not repeated; `ChannelProductsStore`, `MarketplaceConnectionStore` and stock policies are keyed the same way but were not given their own cases; `PolicyCenterStore`'s explicit cross-shop policy copy is an operator action (allowed by design) and was noticed to drop the #285 expense fields on the copy -- fail-closed on the target shop, recorded here as a follow-up, not changed.

## #788 — return and refund reconciliation (2026-09-12), branch codex/issue-788-return-refund-reconciliation off codex/issue-787-store-isolation-suite

New issue (TEST, but the inventory came first): returns and refunds had no reconciliation to test. What existed was `ReturnManagement` (shop scope + fingerprint dedupe of return cases, no quantities against anything), `EtsyOrderCompletion` (event dedupe, a refund *stock* preview with no amount), and the whole-order restore in `OrderStockRestore`, which puts back the full receipt quantity however much actually came back -- a partial return of 1 of 3 would have restored 3. Nothing compared a refund amount with the order total or a currency with the order's. So the suite needed a service under it, on the real path.

`Catalog/OrderReturnReconciliation.cs` (new, `CatalogStore` partial, same shape as the existing stock decisions): `PreviewOrderReturn(order, event)` / `ApplyOrderReturn(order, event, approved)` take the `OrderSnapshot` (fetched by the caller through the new exact lookup `OrdersStore.Find(marketplace, shop, orderId)`) and an `OrderReturnEvent` (event key, SKU, quantity, refund amount, currency), and reconcile it against five things: the ordered quantity of the SKU; the *fulfilled* quantity -- what the stock receipt deducted, i.e. what can physically come back (no receipt: the quantity still reconciles against the order but nothing is restored); the order total for cumulative refunds; the order currency (case-insensitive, and only when an amount is refunded); and the new ledger `OrderReturnEvents` (created lazily in `catalog.db`) of events already applied, keyed by event, so the same event is never counted or restored twice. Statuses: `OK_PARTIAL`, `OK_FULL`, `BLOCKED` (reasons `OVER_RETURN`, `OVER_REFUND`, `CURRENCY_MISMATCH`, `SKU_NOT_IN_ORDER`, `ORDER_SCOPE_MISMATCH`, `QUANTITY_INVALID`, `REFUND_INVALID`, `EVENT_KEY_INVALID`), `DUPLICATE`. Apply is one transaction: ledger row plus the bounded stock restore (never more than fulfilled minus already restored, and nothing if the old whole-order restore already ran); a blocked event is never written and applying one throws. `OrderExceptionsPanel.cs` (the production entry for cancel/return decisions): SKU / adet / tutar / para birimi inputs with "İade mutabakatı önizle" and "İade mutabakatını uygula", the event key derived from the exception record and the inputs (same inputs twice = duplicate, changed inputs = new event), a full return resolving the record.

Second real defect, found by the seventh test rather than by reading: `OrderExceptionStore` lower-cases the marketplace of every record ("yerel"), `CreateOrderRestockPreview` lower-cases its argument too, but `ApplyOrderStock`/`GetOrderStockStatus` store and look up receipts with the order's own casing ("Yerel") -- so `Reconcile` promised `PreviewReady` for a returned manual order and the panel's very next click threw "daha önce uygulanmış stok hareketi bulunamadı". Reproduced through the real queue (`Reconcile` → `List` → `CreateOrderRestockPreview` with the record's fields) and fixed in `CatalogOrderStock.cs` by matching the marketplace with `COLLATE NOCASE` on the two receipt lookups (shop and order ids stay exact); the new reconciliation uses the same rule. Etsy orders were unaffected only because "etsy" happens to be lowercase already.

`tests/MarketplaceHub.Tests/ReturnRefundReconciliationSuiteTests.cs` (new, 7 tests, real stores; the file was red before the service existed and the seventh stayed red until the case fix): partial then remainder → `OK_PARTIAL` then `OK_FULL`, stock 7 → 8 → 10; over-return blocked both at once (4 of 3) and cumulatively (2 + 2), restoring nothing and writing nothing; over-refund blocked against the total including refunds already applied, exactly the remainder allowed, negative amount blocked; currency mismatch blocked with nothing recorded, codes compared case-insensitively; a duplicate event key recognised, counted once, stock restored once; another shop's order not found, unknown SKU blocked, an order that never had stock deducted reconciles to `OK_FULL` with zero to restore; and the mixed-case marketplace restock preview.

REAL_WORK_COUNT=1; FILES_CHANGED=Catalog/OrderReturnReconciliation.cs (new), Catalog/CatalogOrderStock.cs, OrdersStore.cs, OrderExceptionsPanel.cs, tests/MarketplaceHub.Tests/ReturnRefundReconciliationSuiteTests.cs (new), IMPLEMENTATION_STATUS.md; TEST_RESULT=267 passed, 0 failed, 0 skipped (full Release suite, up from 260); Release build and self-contained win-x64 publish succeeded; artifact SHA-256 a3a3e0bcfa9967c544c67276f3f3ca5f7f694eb8f0902588b28afbad20dfcd93.
Not done: shipment quantities are not modelled per line anywhere in `OrderShipment` (a shipment has no items), so "shipment quantity" reconciliation uses the stock receipt as the fulfilled quantity -- an order fulfilled without a stock deduction reconciles against the ordered quantity only; Etsy refund events (`EtsyOrderCompletion.PreviewRefund`) are not yet fed into the ledger automatically -- the operator enters the return in the exception queue; the ledger has no UI listing of its own (the reconciliation preview shows the cumulative numbers); no UI-level test of the panel's new buttons (the service is exercised directly, the wiring is input parsing plus the two calls); `OrderStockRestore`'s whole-order restore still exists alongside the partial path and is honoured (no double restore), but not unified with it.

## #789 — shipping lifecycle integration suite (2026-09-12), branch codex/issue-789-shipping-lifecycle-suite off codex/issue-788-return-refund-reconciliation

New issue (TEST). Inventory first: the shipping lifecycle lives in `OrdersStore.Save` (the merge of an API re-save over local observations, and the out-of-order guard `previous.UpdatedAt >= o.UpdatedAt`), `OrderNormalizer.Normalize` (run by `OrdersEtsyClient` on every receipt: state normalization, length limits), `OrdersRules.Validate` (duplicate package ids) and `ShippingLabelCenter.CreatePreview` (duplicate tracking codes). Two things the issue asks to test did not exist: carrier normalization -- the normalizer only trimmed the carrier, so "yurtici", "YURTİÇİ KARGO" and "Yurtiçi Kargo A.Ş." were three carriers in every label and filter -- and any delivery SLA at all.

`OrdersModels.cs`: `OrderNormalizer.NormalizeCarrier` maps the common Turkish and international carriers on a folded form (Turkish-aware lower-casing -- `İ` is handled before `ToLowerInvariant`, which would otherwise turn it into i + combining dot -- diacritics stripped, letters and digits only) and keeps an unknown carrier verbatim rather than guessing; it runs inside `Normalize`, i.e. on the real Etsy read path. `OrdersRules.EvaluateSla(shipment, fallbackShippedAtUtc, nowUtc, maxTransitDays = 5)` returns `DELIVERED` / `RETURNED` / `EXCEPTION` (terminal, never late), `NOT_SHIPPED`, `IN_TRANSIT` or `DELAYED` with the days in transit counted from the first shipped/in-transit observation; an API shipment carries no observations (the Etsy client records none), so the caller passes the receipt's source timestamp as the latest moment the package can have shipped. `OrderSnapshot.SlaLabelAt(now)` rolls that up per order (a delayed package makes the order late, otherwise the longest transit, otherwise delivered/unshipped) and `[JsonIgnore] SlaLabel` feeds a new "Teslimat SLA" column in the orders grid; the shipment editor shows the package's own SLA line. Time-dependent values are never serialized into the payload.

`tests/MarketplaceHub.Tests/ShippingLifecycleSuiteTests.cs` (new, 6 tests, real `OrdersStore` on disk, times fixed relative to one `Now`; red first on the three missing members): a split shipment keeps both packages with their own carrier, tracking and state and shows every distinct state, and a partial shipment (one delivered, one not yet notified) stays visible as such with no stray separators; carrier normalization on the real read path collapses three spellings into one carrier and keeps an unknown one verbatim; duplicate tracking is blocked at the label preview even across orders/carriers while a code-less package is not a duplicate, and a batch with duplicate package ids is rejected as a whole with nothing written; an older notification arriving after a newer one is ignored, and an API re-save never overwrites a local delivery observation (state, source, tracking URL and the observation history all survive); the SLA evaluator on late / on-time / delivered / unshipped / observation-less packages including the exact-allowance boundary and the invalid allowance; and a saved order re-read by a fresh store instance after a restart reports "Gecikti (8 gün)" for its undelivered package while an all-delivered order reports "Teslim edildi".

REAL_WORK_COUNT=1; FILES_CHANGED=OrdersModels.cs, OrdersPanel.cs, tests/MarketplaceHub.Tests/ShippingLifecycleSuiteTests.cs (new), IMPLEMENTATION_STATUS.md; TEST_RESULT=273 passed, 0 failed, 0 skipped (full Release suite, up from 267); Release build and self-contained win-x64 publish succeeded; artifact SHA-256 1c943029843f5c400a77c8a77b30423d3599b32f9eecb043e2ebd4f323b9ef73.
Not done: the SLA allowance is a constant (5 days) rather than a per-carrier or per-marketplace setting; the Etsy client still records no "shipped" observation for API shipments, so their transit time is bounded by the receipt timestamp instead of measured; no SLA filter in the orders list (the column is sortable, that is all); the carrier table is a short hard-coded list -- unknown carriers pass through unchanged by design and can be added as they appear; the Etsy client's own per-receipt dedupe of packages (`receipt_shipping_id`) is not exercised here because it needs the HTTP fixture, which the existing Etsy safety tests own.

## #791 — end-to-end data integrity regression suite (2026-09-12), branch codex/issue-791-e2e-integrity-suite off codex/issue-789-shipping-lifecycle-suite

New issue (TEST, the umbrella over #787-#790). `tests/MarketplaceHub.Tests/EndToEndIntegritySuiteTests.cs` (new, 6 tests) runs one deterministic chain through the production entry points on real stores -- supplier feed import (complete feed with hash) → product master → price policy and stock decision → order, shipment observation, return reconciliation → product-order report and dashboard snapshot -- and then re-runs the same chain under the issue's six conditions: happy path with identity and provenance (a second feed with a changed price updates the same product id, never a new one; the price preview carries the cost it priced from; the source names the feed it came from; report and dashboard agree on one order, one applied stock decision, one return), partial failure (a feed failing on its third row lands nothing and the source still points at the last good feed; a blocked over-return changes nothing downstream), restart (fresh store instances after the pools are cleared find the policy, mapping, observation history, receipt and return ledger), duplicate input at every step (same feed hash → `AlreadyApplied`, second stock decision deducts nothing, same return event → `DUPLICATE`, same order snapshot re-saved → ignored), wrong-store access (shop B finds, sees, prices and reconciles nothing of shop A), and redaction (secrets entering through a sync error, an exception message and a product name never come back out of `LastError`, the exception record or the search index).

The first run found two things. One was a real leak: `SyncStore.Redact` replaced the *names* of secrets ("access_token" → "[redacted]") and left their *values* intact, so a failed price job stored `Authorization: Bearer <token>` verbatim in `LastError` -- which the dashboard notifications and the sync panel display; the token value in the test survived only mangled because it happened to contain the word "secret". Fixed by running the central `AuditStore.Sanitize` (bearer headers, key=value secrets, PII) before the name replacements. The other was a contradiction rather than a bug in the chain: the orders screen promised "İşlenen ürünün XML stok kilidi etkinleştirilir; böylece tedarikçi güncellemesi satılan stoğu geri yazmaz", but nothing ever sets `LockStock` after a sale -- `ApplyOrderStock` deliberately treats the movement as transient ("do not turn it into the user's manual supplier-lock"), and the next complete feed writes the supplier's stock back (10 → 8 sold → 9 returned → 10 from the feed). For dropshipping the supplier's count *is* the truth and a permanent lock would silently stop all future stock updates for that product, so the behaviour is kept and the two UI texts now say what actually happens (the movement is local and transient; the product card's stock lock is the permanent control), and the suite asserts the real semantics with the receipt and return ledger keeping the history. A third, smaller inconsistency surfaced on the manual path: `SaveManual` did not run the #789 carrier normalization, so "aras" typed by the operator and "Aras Kargo" from Etsy were two carriers; it now does.

REAL_WORK_COUNT=1; FILES_CHANGED=Catalog/SyncStore.cs, OrdersStore.cs, OrdersPanel.cs, tests/MarketplaceHub.Tests/EndToEndIntegritySuiteTests.cs (new), IMPLEMENTATION_STATUS.md; TEST_RESULT=279 passed, 0 failed, 0 skipped (full Release suite, up from 273); Release build and self-contained win-x64 publish succeeded; artifact SHA-256 40a746b9e4527dec5afa1beaf4417ac584d3384110c615c85febf1c068ccacff.
Not done: the chain uses manual ("Yerel") orders, not an Etsy pull -- the HTTP path stays with the Etsy safety tests; the second feed reports every known row as "updated" because the first import does not stamp `PriceSource`/`StockSource`/`SourceKind` while updates do (a counting quirk in `ImportSummary`, recorded, not changed); local sales are still overwritten by the next supplier feed by design -- a reservation model (supplier stock minus open local movements since the last feed) would be the real answer to overselling and is its own design issue; `OrderExceptionStore` redacts with `MarketplaceConnectionStore.Redact` rather than the central sanitizer (it covers headers and key=value tokens, not PII) -- not unified here.

## #792 — product list column layout persistence (2026-09-13), branch codex/issue-792-product-list-layout-persistence off codex/issue-791-e2e-integrity-suite

New issue, first of the DESIGN block. ENTRY is the product list load/save layout in `MainWindow`. What existed persisted only the *hidden* column headers, under `columns:products`, and applied them from `AddProductFilters` -- which `BuildProducts` calls **before** it adds a single column, so the loop ran over an empty collection and nothing was ever restored after a restart. Width, order and the sort choice were not persisted at all.

`DataGridLayoutState.cs` (new, no WPF dependency): `DataGridLayoutState`/`DataGridColumnLayout` plus `DataGridLayoutCodec` with the persistence format and, more importantly, the trust and fallback rules. Columns are keyed by *binding path*, not header text, so a renamed or translated header keeps its layout; widths are device-independent pixels, which is what makes a layout saved at 100% DPI lay out identically at 150% and 200% (WPF scales DIPs itself -- storing `ActualWidth` in pixels would have shrunk every column at high DPI). Anything untrustworthy -- empty, not JSON, another schema version, no columns -- deserializes to null and the caller keeps the defaults, while an individual entry with no key or an impossible width is dropped without discarding the rest. `ResolveOrder` is the order fallback: persisted keys in their saved order, a saved key that no longer exists simply skipped, and every column the layout has never seen inserted at *its own default index* rather than appended, so a column added by a newer version appears where the code puts it.

`MainWindow.Filters.cs`: `InitializeProductLayout` (called from `BuildProducts` once the columns exist) applies the stored layout and then subscribes -- `ColumnDisplayIndexChanged`, `ColumnReordered`, and a `DependencyPropertyDescriptor` per column for `Width` and `Visibility`, plus `Closing` and the filter bar's apply -- with an `applyingProductLayout` guard so restoring never writes back. The sort controls are exposed to the layout code through two delegates set where they are created, so the sort travels with the layout and lands in both the combo box and `productFilter`. The column chooser now writes through `SaveProductLayout` as well. The legacy `ApplyProductColumnPreferences` stays as the fallback for a database that only has the old hidden-header list, so an existing install keeps its hidden columns on first run and gets the full layout from then on. Scope: the key lives in the store's own `ui-preferences.db`, so a different data directory is a different layout by construction.

`tests/MarketplaceHub.Tests/DataGridLayoutCodecTests.cs` (new, 3) covers the format round-trip with DIP widths, every untrustworthy input, and the order resolution including removed columns, new columns in the middle, and two saved columns claiming the same index. `tests/MarketplaceHub.Tests/ProductListLayoutPersistenceTests.cs` (new, 3, real `MainWindow` on an STA thread, red first): order, width, visibility and sort survive a restart (a second `MainWindow` over the same directory); a layout naming a removed "Ghost" column applies the rest with the saved relative order while unknown columns keep their default positions, and a corrupt layout falls back to the defaults; and a layout saved for one store is not applied to another. Two of my own expectations were wrong about where an unknown column lands and were corrected to the documented rule -- the implementation was right, the test was guessing.

REAL_WORK_COUNT=1; FILES_CHANGED=DataGridLayoutState.cs (new), MainWindow.Filters.cs, MainWindow.xaml.cs, tests/MarketplaceHub.Tests/DataGridLayoutCodecTests.cs (new), tests/MarketplaceHub.Tests/ProductListLayoutPersistenceTests.cs (new), IMPLEMENTATION_STATUS.md; TEST_RESULT=285 passed, 0 failed, 0 skipped (full Release suite, up from 279); Release build and self-contained win-x64 publish succeeded; artifact SHA-256 b08f8c18841873fa5710e8885bbc2fee6b3361eae1bc866491c6175bdf02d45c.
## #793 — product list density switch (2026-09-13), branch codex/issue-793-product-list-density off codex/issue-792-product-list-layout-persistence

New issue. ENTRY is the product list view settings. The requirement that matters is "aynı DataGrid/style owner üzerinde" -- one control, one style owner, no parallel UI -- and that font, row height, thumbnail and hit target scale *together*, which is exactly what goes wrong when density is bolted on as a row-height setter and the text stays the same size in a squashed row.

`ProductListDensity.cs` (new): the two modes as one metrics table (`RowHeight`, `FontSize`, `ThumbnailSize`, `CellPadding`) so the four numbers can never drift apart in separate styles, plus `Normalize` (unknown input falls back to comfortable rather than to an empty style; accepts the Turkish label back) and `Label`. Compact is bounded by usability rather than by how small it can go: 28 DIP rows are still a comfortable pointer target and 12 DIP text is still legible, and because these are device-independent units WPF scales them itself at 150%/200% DPI. `MainWindow.Filters.cs`: `BuildProductDensitySelector` adds one combo box to the existing product toolbar, restores the stored mode, and on change calls `ApplyProductDensity` -- which sets `RowHeight`/`MinRowHeight`/`FontSize` and a `CellStyle` carrying the mode's padding on the *same* `products` grid -- then persists under `density:products` in the store's `ui-preferences.db`, next to the #792 layout. Nothing rebinds `ItemsSource`, which is what keeps the selection and the recycling virtualizer untouched.

`tests/MarketplaceHub.Tests/ProductListDensityTests.cs` (new, 2; written first, red as a compile failure against the missing selector and metrics). The first test pins the metric relationships rather than the literals -- compact is denser in every dimension, the row never goes below 24 DIP, text never below 11, a thumbnail is never taller than its row, row height stays proportional to the font, and an unknown mode falls back -- so a future tweak to the numbers stays honest. The second runs the real `MainWindow`: select a row, switch to compact, and the selection survives while the live grid's `RowHeight` and `FontSize` match the compact metrics and `EnableRowVirtualization` / `IsVirtualizing` / `VirtualizationMode.Recycling` are all still in place; then a second `MainWindow` over the same directory comes up compact.

REAL_WORK_COUNT=1; FILES_CHANGED=ProductListDensity.cs (new), MainWindow.Filters.cs, MainWindow.xaml.cs, tests/MarketplaceHub.Tests/ProductListDensityTests.cs (new), IMPLEMENTATION_STATUS.md; TEST_RESULT=287 passed, 0 failed, 0 skipped (full Release suite, up from 285); Release build and self-contained win-x64 publish succeeded; artifact SHA-256 61b173c0a069bdcf55bfb7b17453f3531ef2f17f046bbc29d34cf9655005dc4a.
## #794 — product row state hierarchy (2026-09-13), branch codex/issue-794-product-row-state-hierarchy off codex/issue-793-product-list-density

New issue. ENTRY is the product row template. The list showed one thing about a row's condition -- a "Durum" column reading Aktif/Pasif from `StatusLabel` -- while the fields that actually decide whether an operator can do anything with the row (`Duplicate`, `SourceMissing`, `Anomaly`, an attempted Etsy dispatch with no listing id, a feed that has gone quiet) were invisible unless you opened the card. There was no hierarchy to speak of, so the first job was to define one rather than to paint states.

`ProductRowState.cs` (new): `Classify(product, nowUtc)` reads only the product's own persisted fields -- no second source of truth -- and resolves the six states by one documented precedence: **disabled** (nothing else matters if the row cannot sell) > **error** (duplicate identity or a missing source record) > **warning** (feed anomaly) > **pending** (dispatch attempted, no listing id back) > **stale** (a feed-backed product untouched by its source for more than seven days; a `manual` product is never "stale", it is simply as old as the operator left it) > normal. The precedence picks the badge and the border, and *every* condition that also applies is listed in the reason, so nothing is hidden by the ranking. Each state carries a glyph, a word and a border weight, so the row reads on a monochrome display; `AccentBrush(key, highContrast)` puts colour last and abandons the app palette for `SystemColors` under high contrast -- a hand-picked amber is exactly what a high-contrast theme exists to override. `Tooltip` gives the state and the reason plus stock, deliberately not the buying cost, the margin or the source identity (a hover tooltip is the easiest thing to catch in a screenshot or a screen share), sanitized through `AuditStore.Sanitize` and capped at 400 characters so a pathological product name cannot turn it into a wall of text.

Wiring: the "Durum" column now binds the whole row through `ProductRowStateBadgeConverter` to the badge (glyph + word), and `InitializeProductRowStates` hooks `LoadingRow` -- the one event that also fires when the recycling virtualizer *reuses* a row, so a scrolled list can never leave a previous product's border or tooltip behind -- to set the border thickness and brush, the tooltip, and `AutomationProperties.ItemStatus` so a screen reader announces the state too.

`tests/MarketplaceHub.Tests/ProductRowStateTests.cs` (new, 4, red first): each state classified from its own field, including the two negative cases (a listing id clears pending; a manual product is not stale); combined states resolving by precedence with the other conditions still named in the reason, plus a check that severity is monotonic in that order; every state distinct by badge alone with a glyph, a word and a ≥3 DIP border, and high contrast changing the colour while leaving the non-colour signals identical; and the tooltip containing the state but never the cost, never the source id, never a bearer token, and never more than 400 characters.

REAL_WORK_COUNT=1; FILES_CHANGED=ProductRowState.cs (new), MainWindow.Filters.cs, MainWindow.xaml.cs, tests/MarketplaceHub.Tests/ProductRowStateTests.cs (new), IMPLEMENTATION_STATUS.md; TEST_RESULT=291 passed, 0 failed, 0 skipped (full Release suite, up from 287); Release build and self-contained win-x64 publish succeeded; artifact SHA-256 6f4175ae3c901e7f24a00999a78a42604d4e9f137ce045c5cff1663d493dc975.
## #795 — product list selection summary bar (2026-09-13), branch codex/issue-795-selection-summary-bar off codex/issue-794-product-row-state-hierarchy

New issue. ENTRY is the product list's selection-changed event. The list already had three bulk actions in the toolbar ("Aktife al", "Pasife al", "Ürünü sil") and nothing that told the operator what they were about to act on: no count, no scope, no warning that some of the selection cannot be deleted, and no way to clear a selection except ctrl-clicking rows back off. Worse, the selection survives a filter change, so a row that has scrolled out of the filter is still in `SelectedItems` and would have been acted on unseen.

`ProductSelectionSummary.cs` (new, pure): `Describe(selected, visible, matchTotal)` reconciles the selection against the rows *currently in the list* -- anything no longer there is counted as stale, excluded from `LiveIds`, and stated in the scope text rather than silently acted on -- reports the live count against the full match count (so selecting a page is never mistaken for selecting the whole filter), and computes risky-action availability from the set rather than always offering it: "activate" is only enabled when something in the selection is actually passive, and the deletable/blocked split mirrors `CatalogStore.DeleteProduct`'s refusal of anything with an Etsy listing or a dispatch attempt, so the bar says "2 ürün silinemez" before the operator meets that error one dialog at a time. Only counts are rendered -- no product text -- which is what keeps a name or a cost from travelling into the bar.

`MainWindow`: `BuildProductSelectionBar` is a sticky `Border` docked between the toolbar and the grid (new three-part `Dock` overload), collapsed while nothing is selected, carrying the headline, the scope/risk detail and a "Seçimi temizle" button, with `AutomationProperties` name and help text so the bar is announced rather than being a visual-only affordance. It updates from its own `SelectionChanged` subscription -- separate from the existing edit-resolution handler, so the summary is correct regardless of which path that takes.

`tests/MarketplaceHub.Tests/ProductSelectionSummaryTests.cs` (new, 4, red first): 0 / 1 / 1000 selected rows each with their own headline and a scope naming both the selection and the 5.000 matches behind it; a selection left behind by a filter change reported as stale, excluded from `LiveIds`, and -- when *every* selected row is gone -- collapsing the bar rather than offering actions on nothing; risky-action availability across a mixed set (activate off when everything is already active, 2 deletable vs 2 blocked, all-blocked disabling delete entirely); and a product whose name carries a bearer token and a four-figure cost, with every string the bar renders asserted free of both.

REAL_WORK_COUNT=1; FILES_CHANGED=ProductSelectionSummary.cs (new), MainWindow.Filters.cs, MainWindow.xaml.cs, tests/MarketplaceHub.Tests/ProductSelectionSummaryTests.cs (new), IMPLEMENTATION_STATUS.md; TEST_RESULT=295 passed, 0 failed, 0 skipped (full Release suite, up from 291); Release build and self-contained win-x64 publish succeeded; artifact SHA-256 f1da8cad29dec6fdac6f41ed3cac8969bcaf8c57b99933d1a0ca06c8dfc547e1.
## #796 — product quick-inspect drawer (2026-09-13), branch codex/issue-796-product-quick-inspect-drawer off codex/issue-795-selection-summary-bar

New issue. ENTRY is the product row's inspect command. Checking why a row is in the state it is meant opening the editor -- which loads the whole card, arms the unsaved-changes machinery, and takes the operator off the row they were scanning. The issue asks for a read-only drawer instead, and is explicit about two constraints: no mutation controls, and no PII or secrets.

`ProductQuickInspect.cs` (new, pure): `Build(product, jobsForThatProduct, nowUtc)` returns a flat list of section/label/value rows covering the five things the issue names -- identity, price, stock, source, readiness, and the last error. Read-only is a property of the *shape* rather than a rule someone has to remember: the model holds strings, so there is nothing a drawer could two-way bind to. Readiness is derived from what the product actually lacks (SKU or barcode, name, sale price, description, image), so "Eksik (3)" lists the three; the last error is the most recent **failed** sync job for that product id, which a later success does not erase. Missing data renders as an em dash rather than a blank row or an epoch date, and every value goes through `AuditStore.Sanitize` plus a 300-character cap -- product names and supplier descriptions are free text that has, in practice, carried tokens and customer contact details.

`MainWindow`: `BuildProductInspectHost` wraps the grid in a two-column host with a 320 DIP drawer that stays collapsed until asked for, opened with Ctrl+I on the selected row and closed with Esc (both as `KeyBinding`s on the grid, plus Esc on the drawer itself and a "Kapat (Esc)" button), moving focus into the drawer on open and back to the grid on close, with an `AutomationProperties` name so it is announced. It rebuilds its content on every open, so reopening after a refresh shows current values rather than the first snapshot.

Tests, red first. `ProductQuickInspectTests.cs` (4): all six sections present with the right values and the most recent failure winning over an older success; readiness derived from the missing fields; missing data as em dashes including a product no feed ever touched; and a product whose name carries a bearer token, whose description carries an email and a phone number, and whose sync error carries an access token -- none of which appear in any row. `ProductQuickInspectDrawerTests.cs` (2, real `MainWindow`): the drawer opens on the command with the selected row's identity, exposes **zero** `TextBox`/`CheckBox`/`ComboBox`/`DataGrid` and exactly one button (close) -- read-only verified against the realized visual tree, not just asserted about the model -- closes again, and does the same through the Ctrl+I and Esc key bindings; and reopening after the stock changed shows 41 where the first open showed 3.

REAL_WORK_COUNT=1; FILES_CHANGED=ProductQuickInspect.cs (new), MainWindow.Filters.cs, MainWindow.xaml.cs, tests/MarketplaceHub.Tests/ProductQuickInspectTests.cs (new), tests/MarketplaceHub.Tests/ProductQuickInspectDrawerTests.cs (new), IMPLEMENTATION_STATUS.md; TEST_RESULT=301 passed, 0 failed, 0 skipped (full Release suite, up from 295); Release build and self-contained win-x64 publish succeeded; artifact SHA-256 f58b3e42d65fd539c001a40d3cc49bcca36de7064fd159dfb6551b734b406a5e.
## #797 — product card media fallback states (2026-09-13), branch codex/issue-797-media-fallback-states off codex/issue-796-product-quick-inspect-drawer

New issue. ENTRY is the product image presenter, which is `MediaPanel`'s preview. It had one `Image` with a fixed 360×260 and no states at all: selecting a record cleared the source and left an empty area while the fetch ran, a missing record looked identical to a failed one, and a failure wrote `"Önizleme yüklenemedi: " + error.Message` into the status line -- the transport's own message, which for an image fetch is the request URL, signature and all.

`ProductMediaPresentation.cs` (new, pure): the five presentation states (`Loading`, `Missing`, `Broken`, `Ready`, `Cached`) as one shared description with a glyph, a word and a hint, and -- the point of the issue -- one `BoxSize` for all of them, so the placeholder reserves exactly the space the picture will take and the card cannot jump. `Classify` maps a `ProductMediaRecord` plus the load outcome onto those: no record or no URL is *missing* rather than broken, every `MediaStatus` failure (`NotFound`, `Timeout`, `InvalidUrl`, `TooLarge`, `RateLimited`, `UnsupportedFormat`, `Error`) is *broken*, a record the store believes is fine but that would not decode is broken too, and a local file resolves as *cached* because it is not a fresh remote fetch. `SafeSourceLabel` reduces a URL to its host and nothing else -- no path, no query, no embedded credentials -- and a `file:` URL becomes "yerel dosya" rather than naming somebody's home folder. `FailureText` composes the operator-facing line from the status and that host, and deliberately ignores the raw exception text.

`MediaPanel.cs`: the preview is now an `Image` and a placeholder stack inside one fixed-size `Border`, switched by `ShowMediaState`, which also sets the box's automation name so the state is announced. The fetch reports loading before it starts, ready (or cached) on success, and broken on the narrowed set of transport exceptions, with the status line coming from `FailureText`.

`tests/MarketplaceHub.Tests/ProductMediaPresentationTests.cs` (new, 4, red first): the four states classified from the record and the load outcome including every failure status; all five states reporting an identical box size and each carrying a distinct word (loading ≠ missing ≠ broken, which is exactly what a single grey box gets wrong); the URL reduced to a host with the signature, the credentials and the path all gone and a local path never named; and a failure line that says "bulunamadı" and the host but never the token or the query.

REAL_WORK_COUNT=1; FILES_CHANGED=ProductMediaPresentation.cs (new), MediaPanel.cs, tests/MarketplaceHub.Tests/ProductMediaPresentationTests.cs (new), IMPLEMENTATION_STATUS.md; TEST_RESULT=305 passed, 0 failed, 0 skipped (full Release suite, up from 301); Release build and self-contained win-x64 publish succeeded; artifact SHA-256 64bc6290c4a1fd24bcfc7c8bd400792e796736281d21d15db51f2a21b6523a4a.
## #798 — product card price information hierarchy (2026-09-13), branch codex/issue-798-price-information-hierarchy off codex/issue-797-media-fallback-states

New issue. ENTRY is the product card's pricing summary, which did not exist: the card opened with "Alış fiyatı", "Satış fiyatı", "Satış para birimi" and "KDV" as four equally weighted text boxes, so nothing led, the last calculation time was invisible, and a price sold below cost looked exactly like a healthy one. The issue is explicit that no new pricing feature belongs here, which settles the design question: everything shown is derived from figures already on the product, and the authoritative decision stays the dispatch-time money preflight (#285) and the operator's minimum-margin guard (#790).

`ProductPriceSummary.cs` (new, pure): `Build(product, nowUtc)` returns the ranked block -- sale price, then approximate margin, then when it was last calculated -- with the price and its currency in *separate* fields (never concatenated), which is what stops a long or odd currency label from deforming the line; the label itself is clamped. A missing price is an em dash and a warning rather than a rendered zero, and it yields `Unknown` margin rather than a -100%. Margin is only computed when the cost and sale currencies match -- subtracting a USD cost from a TRY price is not a margin, and saying so is the honest answer -- and is classified negative / thin (under 10%) / healthy, each with a warning in the operator's words. Staleness reuses the money calculator's own 24-hour window so the card and the gate agree on what "stale" means, and flagging it never alters the number displayed. Every string carries the caveat that the figure is approximate and that commission, shipping and tax are computed at dispatch.

`MainWindow`: `ShowProductPriceSummary` renders that block at the top of the product card (called from `BindProductEdit`, so it follows the selection), with the price as the one large number, the margin coloured by level *and* labelled, the calculation time small beneath, warnings listed, and an automation name that reads the price and margin together. It is read-only -- no control in the block writes anything, and there is no live marketplace call anywhere in it.

`tests/MarketplaceHub.Tests/ProductPriceSummaryTests.cs` (new, 5, red first): the ranked order and healthy case; a missing price as an em dash with `Unknown` margin and a warning; a stale calculation flagged (and a 20-hour-old one not) without changing the price; negative and thin margins warned about, plus mixed currencies refusing to guess; and a ten-character currency code clamped while the price keeps its own field. One assertion of mine was wrong about case (`Yaklaşık` vs `yaklaşık`) and was made case-insensitive rather than bending the copy.

REAL_WORK_COUNT=1; FILES_CHANGED=ProductPriceSummary.cs (new), MainWindow.Filters.cs, MainWindow.ProductEditing.cs, MainWindow.xaml.cs, tests/MarketplaceHub.Tests/ProductPriceSummaryTests.cs (new), IMPLEMENTATION_STATUS.md; TEST_RESULT=310 passed, 0 failed, 0 skipped (full Release suite, up from 305); Release build and self-contained win-x64 publish succeeded; artifact SHA-256 ea05e8f7d3e34e3aea44d9f06e483fdbf9630f652c3c6c66e763167ef8f48ad3.
## #799 — product card stock status composition (2026-09-13), branch codex/issue-799-stock-status-composition off codex/issue-798-price-information-hierarchy

New issue. ENTRY is the product card's stock summary, which was a single editable "Stok" box: the number on the card was the on-hand figure, while what a channel would actually be sent is `min(stock − safety buffer, maximum cap)` and could be a completely different number -- or zero -- with nothing on the card saying so. The issue's edge note is the design constraint: *do not copy the calculation owner, use the existing stock projection*.

`ProductStockSummary.cs` (new, pure): `Build(product, policy, projectedAvailable, nowUtc)` composes on-hand, the projected available quantity, the safety buffer, the maximum cap, how much is withheld, and the age of the figure. The available quantity is **passed in**, never computed -- `CatalogStore.PreviewStock` owns that projection, and a second implementation here is precisely how a card ends up contradicting the dispatcher. "Withheld" is likewise stated as the observed difference between on-hand and the projection rather than re-derived from buffer and cap, so a projection rule this card does not know about still adds up. Levels (`Ready`, `BufferOnly`, `Capped`, `OutOfStock`, `Invalid`, `NoProjection`) each carry a glyph and a distinct word, so the composition reads without colour; a negative stock is `Invalid` with a warning rather than displayed as sellable; and with no saved policy there is no projection, which the card says outright instead of showing on-hand stock as if it were available. Staleness reuses `ProductRowState.StaleAfter`, so "stale" means one thing across the app, and a manually maintained product is never stale.

`MainWindow`: `ShowProductStockSummary` (called from `BindProductEdit`) reads the policy and asks `PreviewStock` for the projection -- catching the "no policy / policy disabled" `InvalidOperationException` as *no projection* rather than as an error -- and renders the level, the three quantities, the buffer/cap/age line, and any warnings above the editable stock field, with an automation name that reads the level and both quantities.

`tests/MarketplaceHub.Tests/ProductStockSummaryTests.cs` (new, 6, red first). The first one is the point of the issue: given a projection that deliberately disagrees with the naive arithmetic, the summary reports the projection -- if it recomputed, it would say 7 and the card would lie. Then: no projection saying so rather than inventing a number; zero, buffer-only and negative each reading differently; every level distinct by word with a stale feed called out and the quantity unchanged by the flag; large quantities grouped and kept in separate fields; and a final test that runs the **real** `CatalogStore` -- import, `SaveStockPolicy(safety 3, cap 5)`, `PreviewStock` -- and asserts the card shows the store's own 5 and reads it as capped.

REAL_WORK_COUNT=1; FILES_CHANGED=ProductStockSummary.cs (new), MainWindow.Filters.cs, MainWindow.ProductEditing.cs, MainWindow.xaml.cs, tests/MarketplaceHub.Tests/ProductStockSummaryTests.cs (new), IMPLEMENTATION_STATUS.md; TEST_RESULT=316 passed, 0 failed, 0 skipped (full Release suite, up from 310); Release build and self-contained win-x64 publish succeeded; artifact SHA-256 26587b010ad7e1f6868df5b00d822c5aa30bd2d166b35ba4ae4d4f6329929e2a.
## #800 — product card source provenance affordance (2026-09-13), branch codex/issue-800-source-provenance-affordance off codex/issue-799-stock-status-composition

New issue. ENTRY is the product card's provenance command. The card exposed the raw per-field markers as four more columns in the grid (`SourceKind`, `PriceSource`, `StockSource`, `MediaSource`) and nothing on the card itself, so "where did this price come from, and is that source's mapping still the one that wrote it?" had no answer short of reading the XML source screen.

`ProductProvenance.cs` (new, pure): `Build(product, source, nowUtc)` turns the markers the import already maintains into one row per field (Fiyat, Stok, Görseller, Başlık, Açıklama), each naming its origin -- the source's name, or "Elle girildi" for an operator-set field -- and saying what that means next time a feed runs, which is where the lock flags belong: a locked field is precisely the reason the import will *not* take it back. The header is the one-line answer ("3 alan kaynaktan, 2 alan elle girildi"), and `SourceSummary` carries the source and the mapping revision that actually wrote these values; when the source's `MappingRevision` has moved past `LastAppliedMappingRevision`, that is flagged as a warning because the field origins may no longer describe what a fresh import would produce. A product whose source cannot be resolved says so instead of showing an empty summary, and a product that never came from a feed reads "Elle oluşturuldu" rather than being reported as missing a source.

The security rule is enforced by shape: the summary takes the source's **name** and nothing else. `XmlSource.Location` is the feed URL, which in this app routinely carries a key in its query string, so it is never passed in -- and the test asserts that not even the host appears, since a provenance line has no use for it.

`MainWindow`: `ShowProductProvenance` (from `BindProductEdit`) fills a collapsed `Expander` on the card -- the issue's "kartı kalabalıklaştırma" -- whose header alone carries the summary line, so the detail is one keyboard-reachable expand away and costs nothing when closed.

`tests/MarketplaceHub.Tests/ProductProvenanceTests.cs` (new, 5, red first): each field naming its source with operator overrides and their locks called out; a mapping revision that has moved on flagged with both numbers; an unresolvable source and a never-fed product distinguished; mixed origins summarized without hiding either side; and a source whose location carries `user:pw@` and `?key=abc123secret` producing a summary that contains the source name and none of that, not even the host.

REAL_WORK_COUNT=1; FILES_CHANGED=ProductProvenance.cs (new), MainWindow.Filters.cs, MainWindow.ProductEditing.cs, MainWindow.xaml.cs, tests/MarketplaceHub.Tests/ProductProvenanceTests.cs (new), IMPLEMENTATION_STATUS.md; TEST_RESULT=321 passed, 0 failed, 0 skipped (full Release suite, up from 316); Release build and self-contained win-x64 publish succeeded; artifact SHA-256 9a7b701bc1267546bfefb498dbc8f60fa1e9c65654dbf4be196f4eb628faf036.
## #801 — product workspace section navigation (2026-09-13), branch codex/issue-801-workspace-section-navigation off codex/issue-800-source-provenance-affordance

New issue. ENTRY is the product workspace's navigation. It was a five-tab `TabControl` whose sections were addressed nowhere but by position -- no key, no deep link, and no memory: every return to the workspace dropped the operator back on the first tab. The tab set did not match the six sections the issue names either: description and images shared one tab, there was no price/stock section, and a "XML / provenance" tab duplicated what #800 had just put on the card.

`ProductWorkspaceSections.cs` (new, pure): the six sections as one catalogue -- `identity`, `content`, `price-stock`, `media`, `channel`, `audit` -- with stable keys (the deep-link surface) separate from labels (display text, freely translatable). `Resolve` matches case-insensitively and trimmed, and a key this build does not have resolves to the default section **and reports `Found = false`**, so a caller can say the link was stale instead of quietly showing the wrong pane. `Route`/`ParseRoute` are the `products#section` link format, round-tripping for every catalogued section. `ProductWorkspaceSectionMemory` is the focus-restore rule in one place: an explicit deep link wins and becomes the remembered section, navigating without one keeps the operator where they were, and an unknown key never overwrites a good remembered section.

`MainWindow`: the workspace is rebuilt as those six tabs, each tagged with its catalogue key, in the catalogue's order; `SelectProductSection` resolves through the memory, selects the tab and moves keyboard focus into its content (the focus-restore half of the issue); and the tab strip writes back to the memory on user selection.

The restructure broke two existing tests, and both were right to break: they addressed tabs by *index*, which is the coupling this issue exists to remove. `ProductCardKeyboardSmokeTests` now asserts the workspace exposes exactly the catalogued keys in the catalogue's order -- which immediately caught that my tab order had `media` before `price-stock` while the issue (and the catalogue) list price/stock first, so the tabs were reordered rather than the catalogue bent. `ProductCardDirtyDraftTests` now looks sections up by key and searches the section's subtree for its fields, because a body may nest fields under headings; it previously assumed they were direct children.

`tests/MarketplaceHub.Tests/ProductWorkspaceSectionsTests.cs` (new, 4, red first): the catalogue's stability, order and uniqueness; known keys resolving and unknown ones falling back while reporting it; deep links round-tripping for every section with a bare page link carrying no section; and the memory restoring the last section, ignoring an unknown key, and letting an explicit link win.

REAL_WORK_COUNT=1; FILES_CHANGED=ProductWorkspaceSections.cs (new), MainWindow.xaml.cs, tests/MarketplaceHub.Tests/ProductWorkspaceSectionsTests.cs (new), tests/MarketplaceHub.Tests/ProductCardKeyboardSmokeTests.cs, tests/MarketplaceHub.Tests/ProductCardDirtyDraftTests.cs, IMPLEMENTATION_STATUS.md; TEST_RESULT=325 passed, 0 failed, 0 skipped (full Release suite, up from 321); Release build and self-contained win-x64 publish succeeded; artifact SHA-256 715cd62a76a1f0e53a7bc2638d720dcf3bf46808a1f137a1f644f3f4c67828fc.
## #802 — product workspace unsaved-change indicator (2026-09-13), branch codex/issue-802-unsaved-change-indicator off codex/issue-801-workspace-section-navigation

New issue. The dirty-state *guard* already existed and works (`ProductEditIsDirty` plus the Kaydet/Vazgeç/İptal prompt on selection change). What it lacked was any visibility: an operator with unsaved work saw nothing until they tried to leave, could not tell **which** part was unsaved, and had only an all-or-nothing discard.

`ProductDirtySections.cs` (new, pure): every writable `CatalogProduct` property is mapped to one of the #801 workspace sections and to an operator-facing field name. `Compare(baseline, current)` reports the dirty sections in the workspace's own order with the changed field **names** -- values are serialized only to detect the difference and never leave the method, because a banner reading "Alış fiyatı: 1234,56 → 4321,99" puts the cost on any shared screen. `ResetSection` is the per-section undo: it restores that section's properties from the baseline and leaves every other section's unsaved work intact. `UnmappedEditableProperties` exists so a test can fail the day someone adds a property with no section -- otherwise a change to it would silently show no indicator at all.

`MainWindow`: `RefreshProductDirtyIndicator` marks each dirty section's tab with a dot, shows a summary line ("Kaydedilmemiş değişiklik: 2 bölüm, 3 alan") and one row per section listing its changed field names with a "Geri al" button wired to `ResetProductSection`; `AutomationProperties` carries the summary. It refreshes on binding (via `BindProductEdit`) and on `LostKeyboardFocus` within the card, which is when WPF's default `LostFocus` bindings actually write back to the product.

Tests, red first. `ProductDirtySectionsTests.cs` (6): a clean product has nothing; each field attributed to its own section; many dirty sections listed in workspace order with a counted summary; the leak test (a four-figure cost, a four-figure price, an email in a description -- none of which may appear anywhere in the rendered indicator, while the field names must); section reset restoring only its own fields and leaving the rest; and the completeness check that no editable property is unmapped. `ProductDirtyIndicatorUiTests.cs` (1, real `MainWindow`): editing the price marks the price/stock tab and not the content tab, the bar names "Satış fiyatı" and does not contain "250", a second edit adds a second section, and clicking that section's "Geri al" restores the price to 10 while the description edit survives.

REAL_WORK_COUNT=1; FILES_CHANGED=ProductDirtySections.cs (new), MainWindow.Filters.cs, MainWindow.ProductEditing.cs, MainWindow.xaml.cs, tests/MarketplaceHub.Tests/ProductDirtySectionsTests.cs (new), tests/MarketplaceHub.Tests/ProductDirtyIndicatorUiTests.cs (new), IMPLEMENTATION_STATUS.md; TEST_RESULT=332 passed, 0 failed, 0 skipped (full Release suite, up from 325); Release build and self-contained win-x64 publish succeeded; artifact SHA-256 bb5e582e1c6e00e8d98d1e5985c631ade53114e1f1bfdc3d1bc1b36c0c2d676a.
## #803 — product workspace validation summary panel (2026-09-13), branch codex/issue-803-validation-summary-panel off codex/issue-802-unsaved-change-indicator

New issue, and the follow-up #802 flagged as not done. Validation existed only as a refusal: `CatalogStore.Valid` threw one lumped message ("Ürün adı, kimliği, fiyatı, stoku veya KDV oranı geçersiz.") at save time, so an operator with three problems learned about one of them, in words that did not say which field, with no way to reach it.

`ProductValidation.cs` (new): one evaluator returning findings as (severity, section, field, message), ordered blocking → warning → info and then by the #801 workspace section order, which is what makes "the first blocker" a meaningful thing to link to (`FirstBlockingRoute` is a `products#section` deep link straight to it). Blocking is exactly what the store refuses; warnings are saveable-but-not-listable gaps (no price, no description, no image, active with zero stock); info is context (locked fields). Messages name the field and the rule -- a limit, a required value -- and never quote the offending content, because product text has carried tokens and customer addresses before.

The part that matters more than the panel: **`CatalogStore.Valid` now delegates to that evaluator** (`ProductValidation.BlockingMessage`). Writing the rules twice is how a panel ends up promising a save the store rejects, or refusing one it would have accepted; now there is one owner, and the store's messages became specific as a side effect ("KDV oranı 0 ile 100 arasında olmalı." instead of the lumped sentence). `Valid` keeps its own null guard, since the evaluator reads those properties.

`MainWindow`: `ShowProductValidation` renders the summary at the top of the card -- a headline with the counts, an "İlk soruna git" button wired to `SelectProductSection`, and the findings grouped under their section labels -- refreshed on binding and on every dirty-indicator refresh, and hidden entirely when there is nothing actionable.

`tests/MarketplaceHub.Tests/ProductValidationSummaryTests.cs` (new, 5, red first): a clean product with no blockers and an empty route; three blockers grouped by section with the first one deep-linked and ordered by the workspace's own order; missing description/image reported as warnings rather than errors; a 900-character name, a description carrying a bearer token and an email, and a missing SKU producing findings that name the fields and quote none of the values; and -- the equivalence test that justifies the refactor -- four blocking cases each asserted to be *both* reported by the evaluator *and* actually refused by `CatalogStore.SaveProduct`, plus a passing product that really saves.

REAL_WORK_COUNT=1; FILES_CHANGED=ProductValidation.cs (new), Catalog/CatalogStore.cs, MainWindow.Filters.cs, MainWindow.ProductEditing.cs, MainWindow.xaml.cs, tests/MarketplaceHub.Tests/ProductValidationSummaryTests.cs (new), IMPLEMENTATION_STATUS.md; TEST_RESULT=337 passed, 0 failed, 0 skipped (full Release suite, up from 332); Release build and self-contained win-x64 publish succeeded; artifact SHA-256 8c4d605afbc6b6f218726f3b5e39a1036768c4aa7d3c9e16747496be5526d9f3.
## #804 — product media gallery keyboard navigation (2026-09-13), branch codex/issue-804-media-gallery-keyboard off codex/issue-803-validation-summary-panel

New issue. ENTRY is the product media gallery, which did not exist: the media screen listed records in a `DataGrid` (with the full URL in a column) beside the single preview box from #797. There was no thumbnail strip, so "look through this product's images" meant arrowing a table row by row, and nothing summarized what was selected.

`MediaGalleryNavigation.cs` (new, pure): the movement rules -- Left/Up back one, Right/Down forward one, Home/End to the ends, everything else leaving the selection alone -- with the edges decided once rather than inside a key handler. The strip **stops at its ends instead of wrapping**, because wrapping surprises someone scanning left to right; an index left over from a longer gallery is clamped rather than trusted; an empty gallery stays at "nothing selected"; and the first key press on an unselected gallery picks the first image. `Summary` names position, state and host (reusing #797's wording and its host-only URL rule) and is the line an operator screenshots, which is exactly why it never carries the URL.

A real leak surfaced while wiring it: the media `DataGrid` shows the full URL in a column and, with WPF's default `ClipboardCopyMode`, **Ctrl+C copied whole rows -- signed supplier URLs and all -- onto the clipboard**, silently. That is the "remote URL clipboard leakage" the issue names. Implicit copy is now off (`DataGridClipboardCopyMode.None`), and copying an address is a deliberate, labelled act: a "Görsel adresini kopyala" button that copies the selected image's URL and only that. `ExplicitCopyValue` is where that distinction is written down and tested against `ClipboardRedaction`, which is what any *automatic* copy still goes through.

`MediaPanel.cs`: a horizontal, virtualizing `ListBox` strip whose tiles carry the state glyph, the host and the position (never the URL), keyboard flow through the shared rules with focus moved onto the tile so it is visibly focused, `ScrollIntoView` so keyboard movement does not scroll off-screen, Enter/Space triggering the copy action, a summary line beneath, and `AutomationProperties` carrying that summary. Selecting in the strip drives the same selection and preview the grid does.

`tests/MarketplaceHub.Tests/MediaGalleryNavigationTests.cs` (new, 5, red first): arrows stepping and stopping at both ends with Home/End and an unowned key; one-image and empty galleries plus a stale index; Enter/Space as activation; the summary naming position, state, host and "Ana görsel" while containing neither the signature nor the path nor embedded credentials, and still summarizing a record with no URL; and the copy distinction -- redacted for the implicit path, verbatim for the operator's explicit one.

REAL_WORK_COUNT=1; FILES_CHANGED=MediaGalleryNavigation.cs (new), MediaPanel.cs, tests/MarketplaceHub.Tests/MediaGalleryNavigationTests.cs (new), IMPLEMENTATION_STATUS.md; TEST_RESULT=342 passed, 0 failed, 0 skipped (full Release suite, up from 337); Release build and self-contained win-x64 publish succeeded; artifact SHA-256 42d9b7d3554dbf534ce7586738d1af6efcbf1f736d1749234bbea5f8a0c6dd59.
## #805 — product content before-after preview (2026-09-13), branch codex/issue-805-content-before-after-preview off codex/issue-804-media-gallery-keyboard

New issue. ENTRY is the save preview. #802 made it visible *that* content had changed and #803 whether it was valid, but the operator still could not see *what* the customer-facing text would become before committing it -- which matters most for the description, the one field a supplier feed rewrites wholesale.

`ProductContentDiff.cs` (new, pure): `Build(baseline, current, stored)` returns one row per changed content field (title, description, brand, category) with the before and after values. This is deliberately the opposite rule from #802's indicator: that one is always on screen and therefore names fields only, while this one is opened on purpose to see the values -- so the values are shown, but sanitized through `AuditStore.Sanitize`, flattened to a single line (a description carries CRLFs and markup) and capped at 400 characters with a visible ellipsis, so neither a 4 KB HTML blob nor a token pasted in from a feed can fill or leak through the dialog. An emptied field reads "(boş)" rather than as a blank line, and unchanged fields are left out entirely. Passing the currently persisted product marks the preview stale when the record has moved on under the operator, because a diff against a baseline that no longer exists describes a save that will not happen.

`MainWindow`: an "İçerik değişikliklerini önizle" button opens a read-only dialog listing each field's before and after with the staleness warning at the top. The dialog contains no control except Close -- nothing in this path writes anything, locally or to a marketplace.

`tests/MarketplaceHub.Tests/ProductContentDiffTests.cs` (new, 6, red first): an unchanged product previewing nothing and saying so; only the changed fields appearing, in a fixed order, with their before/after values; emptying and filling a field reading as "(boş)" on the right side; a 4 KB HTML description flattened, capped and visibly truncated; a stale baseline detected and warned about; and a description carrying a bearer token and an email producing a preview with neither.

REAL_WORK_COUNT=1; FILES_CHANGED=ProductContentDiff.cs (new), MainWindow.Filters.cs, MainWindow.xaml.cs, tests/MarketplaceHub.Tests/ProductContentDiffTests.cs (new), IMPLEMENTATION_STATUS.md; TEST_RESULT=348 passed, 0 failed, 0 skipped (full Release suite, up from 342); Release build and self-contained win-x64 publish succeeded; artifact SHA-256 4cd609c12f54f8e3130616d3889b2de6480f289e6ea566940c9679e004860eba.
## #806 — product audit history readability (2026-09-13), branch codex/issue-806-audit-history-readability off codex/issue-805-content-before-after-preview

New issue, and the last of the product block. ENTRY is the product audit tab, which showed the order report and three source fields -- the audit store's events were reachable only from the global diagnostics screen, unfiltered, as raw rows.

`ProductAuditTimeline.cs` (new, pure): `Build(events, productId, nowUtc)` filters to the product, orders newest-first, and -- the readability decision -- collapses **consecutive** events sharing action, module and outcome into one entry carrying the attempt count and the span between the first and last try. Retries are what floods this list; twelve identical 429s are one thing that happened, and the span is what tells an operator whether it is still going. A different outcome breaks the run, so a success after a string of failures is never swallowed into them. The list is capped at 200 entries (keeping the newest) with the headline saying so, and each entry's detail -- the expandable part -- is re-sanitized through `AuditStore.Sanitize` and capped here regardless of what the writer did, because an audit row is exactly where a failed call's message and its token end up.

`MainWindow`: `ShowProductAuditTimeline` (from `BindProductEdit`) renders it into the workspace's "Geçmiş" section, each entry a one-line summary with its detail behind an `Expander` -- keyboard-reachable, collapsed by default -- and a read failure degrading to a message rather than taking the card down.

`tests/MarketplaceHub.Tests/ProductAuditTimelineTests.cs` (new, 6, red first): an empty history stating itself; only this product's events, newest first; twelve identical retries collapsing to one entry with the count and the 11-minute span; a success and a second action breaking the grouping into 1/2/1; five hundred events capped at 200 keeping the newest and staying strictly ordered; and a detail carrying an access token, a bearer header, an email and 3 KB of padding coming back redacted and capped, with `HasDetail` false when there was nothing to expand. One of my own assertions used ungrammatical Turkish ("kayıt yok" for "kaydı yok") and was corrected rather than bending the copy.

REAL_WORK_COUNT=1; FILES_CHANGED=ProductAuditTimeline.cs (new), MainWindow.Filters.cs, MainWindow.ProductEditing.cs, MainWindow.xaml.cs, tests/MarketplaceHub.Tests/ProductAuditTimelineTests.cs (new), IMPLEMENTATION_STATUS.md; TEST_RESULT=354 passed, 0 failed, 0 skipped (full Release suite, up from 348); Release build and self-contained win-x64 publish succeeded; artifact SHA-256 95422e3ce5d935cb5bab75d85399056b342d93561a44064f24954ca8533b9326.
## #807 — dashboard KPI freshness indicators (2026-09-13), branch codex/issue-807-kpi-freshness-indicators off codex/issue-806-audit-history-readability

New issue, first of the dashboard block. ENTRY is the KPI render. Six cards showed a number and nothing else: no data time, no coverage, and -- the failure that matters -- **a refresh that threw left the previous figures on screen looking exactly as current as before**, with only a red line in the status bar. A `DashboardFreshnessEvaluator` existed but was used for one thing, not the cards.

`DashboardKpiFreshness.cs` (new, pure): `Describe(dataAtUtc, now, maxAge, scope)` returns the state (`fresh` / `stale` / `no-data`), a sentence, the relative time and the coverage. Two decisions worth naming: an **unknown** data time is not treated as current (it is its own state, worded differently from "we know it is old" -- those are different answers), and `AfterRefreshFailure` re-marks a card so the number on screen is labelled as predating a refresh that did not succeed. `ForSnapshot` builds all six cards, each from **its own** data time rather than the snapshot's render time. The scope line says what a card counts and refuses to be a place for source locations: anything containing `://`, a slash or a query is replaced with a generic descriptor rather than trimmed.

`DashboardData.cs`: `DashboardSnapshot` gained four additive, null-defaulted per-KPI timestamps, filled in `Load()` from the records themselves (`products.Max(UpdatedUtc)`, `orders.Max(UpdatedAt)`, `sync.Max(UpdatedUtc)`, the latest connection test) -- a snapshot from before this existed reports "veri zamanı bilinmiyor" instead of borrowing `GeneratedUtc`, which is when the board was drawn, not when the figure was last true. `DashboardPanel.cs`: cards render the state sentence and coverage beneath the number with a warning glyph and border when stale (words, not only colour), an automation name reading value + state + scope, and the catch block now re-renders the last snapshot's cards through `AfterRefreshFailure`.

`tests/MarketplaceHub.Tests/DashboardKpiFreshnessTests.cs` (new, 4, red first): fresh, stale and missing timestamps each reading differently with the last two both counting as not-current; a failed refresh marking a one-minute-old card while keeping its scope; a scope carrying a signed feed URL replaced entirely (not even the host survives) while an ordinary description passes through; and a snapshot whose products are ten minutes old, orders three days old, sync unknown and XML four days old producing exactly fresh / stale / no-data / stale.

REAL_WORK_COUNT=1; FILES_CHANGED=DashboardKpiFreshness.cs (new), DashboardData.cs, DashboardPanel.cs, tests/MarketplaceHub.Tests/DashboardKpiFreshnessTests.cs (new), IMPLEMENTATION_STATUS.md; TEST_RESULT=358 passed, 0 failed, 0 skipped (full Release suite, up from 354); Release build and self-contained win-x64 publish succeeded; artifact SHA-256 c6fc6db13063a96b20fe7cdff6d2f99f8fbb91ca0b1264eaadc72297d3fa8f9b.
## #808 — dashboard anomaly cards (2026-09-13), branch codex/issue-808-dashboard-anomaly-cards off codex/issue-807-kpi-freshness-indicators

New issue. ENTRY is the dashboard's anomaly projection. These situations were already surfaced -- as undifferentiated lines in the notification centre ("Kritik stok", "Sync başarısız · …"), each one sentence with no sense of cost, no age, and no way to tell which of twenty lines to act on first.

`DashboardAnomalies.cs` (new, pure): `Project(input, nowUtc)` turns the four states the app genuinely tracks -- oversell risk, a supplier feed gone quiet, failed dispatches, and order lines matching no product -- into cards carrying **impact** (what it costs in the operator's terms), **age** (how long it has been true) and **next action** with a route. No anomaly type is invented that the data cannot support. Counts and timestamps are supplied by the caller because those queries have their own owners; this decides severity, wording and order. Ordering is severity first -- overselling leads because it is the one that costs money and creates cancellations -- then oldest-first within a severity, since a month-old stale feed outranks a one-minute-old failure. A count of zero produces no card whatever timestamp is still lying beside it, so a resolved anomaly disappears rather than lingering at zero. Cards carry counts and a scope label only, and a scope that looks like a location is replaced outright.

`DashboardData.cs`: `DashboardSnapshot` gained the anomaly counts and their oldest occurrences (additive, defaulted), gathered while the snapshot is already reading those collections so the cards never re-query. "Oversell risk" is an *active* product with no stock -- a live listing that cannot be fulfilled -- and staleness reuses the same seven-day rule as the rest of the app. `DashboardPanel.cs`: a new "Anomaliler" group above the notification centre renders each card with its glyph, count, impact, age, scope and a button that navigates to where the fix happens, with a heavier border for the critical one and an automation name carrying title, count and age.

`tests/MarketplaceHub.Tests/DashboardAnomalyCardsTests.cs` (new, 6, red first): a clean board saying so rather than looking broken; all four states becoming cards that each have an impact, an age, an action and a route; ordering by severity then age, checked with equal-age criticals and a month-old warning; a resolved anomaly with a leftover timestamp producing no card; the scope stripped when it looks like a signed URL and passed through when it is a shop label; and an anomaly with no known start refusing to claim an age.

REAL_WORK_COUNT=1; FILES_CHANGED=DashboardAnomalies.cs (new), DashboardData.cs, DashboardPanel.cs, tests/MarketplaceHub.Tests/DashboardAnomalyCardsTests.cs (new), IMPLEMENTATION_STATUS.md; TEST_RESULT=364 passed, 0 failed, 0 skipped (full Release suite, up from 358); Release build and self-contained win-x64 publish succeeded; artifact SHA-256 6d7ad0f40c95bbec8f03a4f7f3f8f9ece4bbe528c66a1668fb034664a3209aae.
Not done: "unmapped order" is approximated by the orders with no stock decision (`StockWaitingOrders`) rather than by the order-exception queue's own `MissingSku`/`AmbiguousSku` rows -- the exception store is a separate database and reading it here would put a second query owner in the snapshot; the store filter is a label passed through rather than an actual per-shop projection (the snapshot is per data directory, and per-shop counts need the queries themselves to be scoped -- the card shape is ready for it); the four severities are two (critical/warning) because nothing in these states is merely informational; the notification centre still lists the same situations as lines, so they appear twice until that list is narrowed to things that are not anomalies.

Not done: the six-hour staleness window is one constant for every KPI, though an order count and a supplier feed age at very different rates -- per-KPI windows are a settings change; the "store switch" case from the issue's test list is covered by #783's per-directory cache (a different store is a different snapshot, and its own timestamps come with it) rather than by a test here; the quick-action cards (Kategoriler, Excel, Raporlar…) carry no freshness because they are navigation, not figures; no UI test of the rendered card (the panel is a projection of the tested model, and the failure path re-renders through the same function).

Not done: `AuditEvent` has no actor field, so "actor" in the issue's list is represented by the module (which subsystem acted) -- recording a real actor is an audit-store change and would only ever say "operator" or a job name in a single-user desktop app; there is no per-field diff in the timeline (the store keeps a free-text `Detail`, not before/after values -- #805 covers the pre-save diff, and a persisted field-level diff means writing one on every save); the whole audit table is read and filtered in memory per binding rather than queried by product (the store has no `ProductId` index; at the 5000-row retention limit this is cheap, but it is the obvious next step); no UI test of the expanders.

Not done: the diff is per field, not per word -- a one-character title fix shows both strings in full rather than highlighting the change, and an inline word diff is a rendering job (and a much bigger one) than this issue's "before-after preview"; the preview covers the four content fields the issue names, not price/stock (those have their own summaries in #798/#799); it is a dialog rather than an inline panel, which keeps it out of the card's way but means it cannot be watched while editing; no UI test of the dialog (the content is a projection of the tested model and the dialog has one button).

Not done: the tiles show a state glyph and host rather than an actual thumbnail image -- decoding 50 remote images into a strip needs the image cache #797 recorded as missing, and a strip that fetches every URL on selection would be worse than the table it replaces; the focus ring is the `ListBoxItem`'s default visual rather than a custom high-contrast-aware adorner; no UI test of the strip (the rules are covered, the wiring is a straight projection); the URL column remains in the grid (it is genuinely useful on screen) -- only the silent clipboard path was closed; `Shift+Arrow` range selection is not supported, the strip is single-select by design.

Not done: WPF binding errors (the red-outlined numeric fields, `ValidBindings`) are still a separate check that throws at save and are not folded into the panel -- reading them means walking the visual tree per field and mapping controls to sections, which is a card-structure change; the panel does not move focus to the offending *field*, only to its section (the field-level jump needs a control registry the card does not have); "stale result" is handled by re-evaluating on every refresh rather than by tracking evaluation age; the import path's own `Valid` call now produces per-field messages, which is an improvement but changes the text of some import errors.

Not done: the indicator updates on focus loss rather than per keystroke, because the card's bindings are `LostFocus` bindings -- making it live would mean switching them to `PropertyChanged`, which changes when validation fires and is a card-wide behaviour change; validation errors are not yet folded into the indicator (the issue lists them, but the card has no per-field validation surface to read -- `ValidBindings` throws at save time instead); the dot is appended to the tab header text rather than being a separate adorner, so a very narrow tab strip will elide it with the label; no breadcrumb, because the workspace has none yet.

Not done: `Navigate` does not yet parse the `products#section` fragment -- `ParseRoute` and `SelectProductSection` exist and are tested, but wiring them into the navigation router means touching the back/forward history stack, which deserves its own change; the section memory is per-session and per-workspace, not persisted or per-product; the `identity` section still contains the price and stock *editors* while `price-stock` shows them read-only -- splitting the editors out is a card restructure beyond this issue's navigation scope; no UI test of the focus move itself (the tab selection is covered, the `MoveFocus` call is not).

Not done: provenance is per *field group*, not per individual property -- title and description share the product-level `SourceKind` because the import does not stamp them separately, and stamping them would be an import change rather than a card one; the revision shown is the source's applied mapping revision at the time of reading, not a revision recorded on the product itself, so a product imported two mappings ago reports the source's current applied revision rather than its own (recording it per product is the honest fix and is an import-side change); no UI test of the expander (the render is a projection of the tested model); the four raw source columns remain in the grid alongside the new summary.

Not done: the card asks for the `local/default` channel's projection only -- a product listed on several channels has one projection per channel, and showing all of them needs a channel picker in the card (the summary itself is already per-policy and ready for it); "reserved" in the issue's wording has no counterpart in this app (order stock is deducted, not reserved), so it is reported as "withheld" with its real causes rather than inventing a reservation concept; `PreviewStock` is called per card binding without caching; no UI test of the rendered block (the render is a straight projection of the tested model).

Not done: the margin here is `(price - cost) / price` -- deliberately the naive figure, labelled approximate, because computing the real contribution needs the channel's commission/shipping/VAT and that is the dispatch gate's job, not the card's; the "last calculation" time is `FxRateDate`, which is only stamped by the formula path, so a manually typed price reads as "Hesaplanmadı" (true, but it could distinguish "manually set" from "never calculated"); no UI test of the block (the render is a straight projection of the tested model); the thin-margin threshold is a constant rather than a per-channel setting.

Not done: "cached" is inferred from the URL scheme (a `file:` source) rather than from a real image cache -- there is no in-memory or on-disk image cache in the app yet, so a repeated remote fetch still reports as a fresh load; the states are wired into the media panel's preview only, not into a product-card thumbnail, because the product card has no image presenter yet (the same description is ready for it); no slow-network UI test -- the loading state is asserted through `Classify`, and driving a real delayed `HttpClient` through the panel needs a fixture the panel does not currently accept; the box size is a constant rather than a per-surface parameter.

Not done: the drawer reads the sync store on every open (a small query, but not cached) and does not refresh itself while open -- it is a snapshot taken when asked for, which is what "quick inspect" means here, but a long-lived open drawer will age; no image thumbnail in the drawer (that is #797's media fallback work, and pulling a remote image into a hover-level panel needs those states first); the drawer is a fixed 320 DIP rather than resizable; Ctrl+I is not yet advertised anywhere in the UI beyond the close button's label.

CI caught what the local run could not: the runner is en-US, where `N0` formats 1000 as "1,000", while this machine is tr-TR ("1.000") -- the test had the Turkish separator hard-coded and failed on the runner (294/295). The production formatting is right (a UI count belongs in the operator's locale); the expectation now computes the same string with the same format, so it holds in any culture. The same hard-coded-separator mistake was already in the #796 price test and was fixed with it.

Not done: the bar reports the stale part of a selection but does not yet *prune* `SelectedItems` -- the bulk actions still read the grid's own selection, so the honest next step is to route "Aktife al"/"Pasife al"/"Ürünü sil" through `LiveIds`, which changes their behaviour and belongs with the bulk-action issue rather than the bar's; delete remains single-product (`DeleteSelectedProduct` acts on the edited row) so `DeletableCount` currently informs rather than gates it; no keyboard shortcut for clear (the button is in the tab order); the bar wraps but is not tested at a narrow window width; scope counts the current filter's matches, not a cross-page "select all matches" action, which does not exist yet.

Not done: selected/hover/focus are still WPF's own `DataGridRow` visuals -- the issue lists them with the data states, but overriding the row `ControlTemplate` to restyle selection would also override the focus adorner and the high-contrast selection colours the OS provides, which is a bigger and riskier change than this issue's value justifies; the accent brush is applied to the row border rather than a dedicated stripe element, so a theme that hides row borders would lose it; high contrast is read once per row realization (`SystemParameters.HighContrast`) and not re-read on a live theme switch; the seven-day staleness threshold is a constant, not a per-source setting.

Not done: the product list has no thumbnail column today, so `ThumbnailSize` is carried in the metrics (and constrained against the row height) but nothing renders it yet -- it is there so the media column cannot be added at a fixed size later; density applies to the product list only, not to the other grids (same reuse point as #792); no keyboard shortcut for the switch (it is a focusable combo box in the toolbar); the 100k-row claim rests on virtualization staying enabled, asserted here, not on a measured 100k-row render.

Not done: only the product list is wired to the codec -- the sync, XML run, orders and taxonomy grids can reuse `DataGridLayoutCodec` unchanged and are left for their own issues; there is no per-saved-view layout (one layout per store, as the issue's "store/view scope" allows, since saved views persist filters not columns); no "reset layout to default" button; the DPI claim rests on storing DIPs rather than on a rendered high-DPI test, which needs a real display scale the test host does not have; a very long header still widens its column on autosize -- the layout stores whatever width results, it does not clamp it.

## #809 — dashboard store filter persistence (2026-09-13), branch codex/issue-809-dashboard-store-filter off codex/issue-808-dashboard-anomaly-cards

New issue. DESIGN: the board is read by an operator running several shops; until now it showed everything with no way to narrow it, and nothing about the view survived a restart.

`DashboardStoreFilter.cs` (new, pure) is two rules. `Options(candidates)` builds the offered list: "Tüm mağazalar" always first, then one entry per connection that exists **and** is enabled, keyed `channel|shopId` (channel lower-cased, duplicates and empty keys dropped) — a switched-off connection is not something the board can be pointed at, so it is not offered. `Resolve(savedKey, options)` is an **ordinal, exact lookup against that list**, not a parse of the saved string: a key that was never offered — however it got into the preference file — resolves to all-stores rather than being trusted, so a forged or stale value cannot aim the board at a store the operator cannot currently use. A saved store that has since been deleted or disabled falls back *visibly* (`FellBack` + a Turkish notice), because a board silently filtered to nothing is worse than one showing everything; never having chosen a store is not a fallback and says nothing. Each option also carries a human `Scope` label ("etsy / shop-a", "tüm mağazalar") with no storage key in it.

`DashboardPanel.cs`: a `ComboBox` docked right in the toolbar, backed by `UiPreferenceStore` under the data directory (so the choice is per store/profile and survives a restart). Every snapshot re-derives the options from `snapshot.Connections` and re-resolves the saved key; a fallback writes the notice to the status line **and** resets the stored preference so the dead key is not retried forever. An `applyingStoreFilter` guard keeps the programmatic `SelectedItem` assignment from re-entering the handler; a real selection persists the key and refreshes. The resolved scope now flows into `ShowAnomalies(..., scope)`, so #808's cards quote the filter instead of a hard-coded "tüm mağazalar". Preference writes are wrapped — a read-only profile directory must not break the dashboard.

`tests/MarketplaceHub.Tests/DashboardStoreFilterTests.cs` (new, 6, red first): all-stores-first with disabled connections excluded; a saved store restored after a restart; deleted *and* disabled both falling back with a notice; nothing-saved (null/empty/whitespace) and a connection-less installation resolving quietly; forged keys (`etsy|shop-admin`, `*`, `all|*`, `etsy|shop-a extra`, `ETSY|SHOP-A`) never resolving to an unoffered store while the exact offered key still works; and the scope label being readable rather than the storage key.

REAL_WORK_COUNT=1; FILES_CHANGED=DashboardStoreFilter.cs (new), DashboardPanel.cs, tests/MarketplaceHub.Tests/DashboardStoreFilterTests.cs (new), IMPLEMENTATION_STATUS.md; TEST_RESULT=370 passed, 0 failed, 0 skipped (full Release suite, up from 364); Release build and self-contained win-x64 publish succeeded; artifact SHA-256 a37440ea5b94efe422170264b2ff88c915a08edf75ef50c8ee78ae0b26cecdf9.
Not done: the filter is a scope *label* handed to the cards, not a per-shop re-query — `DashboardDataService` reads a whole data directory and its counts are not shop-scoped, so choosing a store today narrows what the anomaly cards claim to cover, not the figures themselves; scoping the queries means giving every KPI a shop predicate and is the honest next issue. The KPI cards and the order trend still ignore the filter for the same reason. The selection is stored per data directory, so two profiles keep separate choices but two windows on the same profile share one. No "only stores with problems" quick filter and no multi-select (the issue asks for one scope at a time). The combo lists connections, not saved views — a saved view still owns filters, not the dashboard scope.

## #810 — dashboard drill-through breadcrumbs (2026-09-13), branch codex/issue-810-drill-through-breadcrumbs off codex/issue-809-dashboard-store-filter

New issue. DESIGN: a KPI or anomaly card is a question with a context — the store the board was filtered to (#809) and the entity the card was counting (#808) — and following one threw both away. The window kept a `Stack<string>` of route keys, so Back returned to a *screen*, never to the view; and the breadcrumb printed `routeHistory.Reverse().Take(2)`, which shows the two **oldest** steps and never names the page you are on.

`NavigationDrillThrough.cs` (new, pure): `DrillTarget` (route, title, store key, entity kind/id/label), `DrillRequest` (a target plus the store keys this session offers) and `DrillThroughStack`. `Open` is the security line — a target naming a store outside the offered keys is refused (ordinal comparison against the offered list, never a parse of the target), and while the board is filtered to one store a target naming a *different* store is refused as a wrong-store deep link; an unscoped screen stays reachable either way, and a refusal leaves the trail untouched. `Back` returns the **whole** context of the crumb it lands on, filter included; landing on a crumb whose entity has since disappeared drops the entity, keeps its screen and says why, so a dead record is never re-opened; Back at the root stays at the root. `SwitchStore` clears a trail dug through the previous store's data (re-selecting the store you are on is not a switch). `TrailText` puts the current page last and elides the middle of a long trail with `…` while Back still walks every step.

`MainWindow.Navigation.cs`: `routeHistory` is gone — the stack is the trail. Sidebar navigation opens inside the board's current scope; `DrillThrough(request)` refuses a blocked link with a log line and no movement; `SwitchDashboardStore(key)` drops the trail and returns to the board; `Back_Click` goes through `Back(DrillEntityAlive)`, where liveness is a real lookup (`CatalogStore.FindProduct`, `OrdersStore.Find`) that fails open on error rather than trapping the operator. `DashboardPanel.cs`: KPI and anomaly cards now drill with their entity and the board's scope, and the store combo notifies the window before reloading. `MainWindow.UX.cs`: Alt+Left walks the trail from the keyboard.

`tests/MarketplaceHub.Tests/NavigationDrillThroughTests.cs` (new, 6, red first): nested drill-down with the current page last; Back restoring route *and* filter *and* entity, then the root, then refusing to underflow; a vanished entity degrading to its screen with a notice; a store switch clearing the trail while a same-store re-selection keeps it; forged/cross-store keys never opening; and a long trail eliding its middle. `tests/MarketplaceHub.Tests/DashboardDrillThroughWindowTests.cs` (new, 3): the same three behaviours through a real `MainWindow` — the breadcrumb text an operator reads, the back button's enabled state, and a wrong-store deep link leaving the window on the dashboard.

REAL_WORK_COUNT=1; FILES_CHANGED=NavigationDrillThrough.cs (new), MainWindow.Navigation.cs, MainWindow.UX.cs, DashboardPanel.cs, tests/MarketplaceHub.Tests/NavigationDrillThroughTests.cs (new), tests/MarketplaceHub.Tests/DashboardDrillThroughWindowTests.cs (new), IMPLEMENTATION_STATUS.md; TEST_RESULT=379 passed, 0 failed, 0 skipped (full Release suite, up from 370); Release build and self-contained win-x64 publish succeeded; artifact SHA-256 e90e7970fbe8dbe5baf530afacbd19f6284266343d71fa726e09dc00f58ac704.
Not done: the crumb carries the entity but the landing screen does not yet *select* it — drilling into "Aşırı satış riski" opens the product list, it does not pre-filter it to the risky rows, because each screen owns its own filter state and routing a context into it is a per-screen change (the target already carries everything those screens need). The trail is not clickable: crumbs render as text and only the last step is reachable, via Back or Alt+Left. It is in-memory, so it does not survive a restart the way `last-route` does. Liveness is checked only for products and orders — every other entity kind is assumed alive rather than pretending to a check that does not exist. `DrillEntityAlive` opens a store per Back press (cheap, single-row lookups, no cache).

## #811 — dashboard zero-data onboarding state (2026-09-13), branch codex/issue-811-dashboard-zero-data-onboarding off codex/issue-810-drill-through-breadcrumbs

New issue. DESIGN: an empty board showed a grid of zeros and the status line's "Yerel veriler yüklendi", which is the same answer for five different situations and useful in none of them.

`DashboardEmptyState.cs` (new, pure): `Evaluate(input, routeExists)` is a ladder that answers the *first* thing missing: no marketplace connection (`NO_STORE` → connections), a store but no XML source (`NO_SOURCE` → xml), a source that has never run (`NEVER_RUN` → xml, "önizle"), a source that has run but never delivered a successful feed (`DISCONNECTED_SOURCE` → xml, "yeniden bağla" — a different problem from unstarted, and the issue's "disconnected source" case), a filter hiding data that exists (`FILTERED_EMPTY` → dashboard, naming the scope), and a complete setup that simply produced no rows (`NO_RECORDS` → products). A board with data returns `NONE` and renders nothing. The "unsupported action" rule is the caller-supplied `routeExists`: a step whose screen this build does not have keeps its diagnosis and **drops the button** rather than promising a capability; nothing in the copy names an endpoint, a token or a marketplace operation.

`DashboardData.cs`: two additive snapshot fields, `SourcesEverRun` (`LastRunUtc` set) and `SourcesWithSuccessfulFeed` (`LastSuccessfulFeedUtc` set), read from the sources the snapshot already loads. `DashboardPanel.cs`: an onboarding host above the KPI cards, filled by `ShowEmptyState` on every refresh from the snapshot, the store filter (#809: filtered = a specific store; visible = that store's connection rows) and the resolved scope label; collapsed when the board has data; the action button navigates through the ordinary route. `MainWindow.Navigation.cs` passes `key => routes.ContainsKey(key)` so the "screen exists" check is the window's real route table, not a copy of it.

`tests/MarketplaceHub.Tests/DashboardEmptyStateTests.cs` (new, 7, red first): the four issue reasons each with a distinct sentence, a safe action and a known route; a working board saying nothing; ran-but-empty diagnosed as disconnected rather than unstarted; a missing screen dropping the button while keeping the diagnosis; no forbidden vocabulary (`http`, `api.`, `/v1`, `token`, `endpoint`) in any state's copy and every action landing on a known route; the ladder preferring "connect a store" over "your filter is too narrow" when nothing is set up; and — through a real `CatalogStore` + `MarketplaceConnectionStore` + `DashboardDataService.Load` — a source flipping from `NEVER_RUN` to `DISCONNECTED_SOURCE` once it has a `LastRunUtc` and no successful feed.

REAL_WORK_COUNT=1; FILES_CHANGED=DashboardEmptyState.cs (new), DashboardData.cs, DashboardPanel.cs, MainWindow.Navigation.cs, tests/MarketplaceHub.Tests/DashboardEmptyStateTests.cs (new), IMPLEMENTATION_STATUS.md; TEST_RESULT=386 passed, 0 failed, 0 skipped (full Release suite, up from 379); Release build and self-contained win-x64 publish succeeded; artifact SHA-256 2ab006b7a48a760344d63e6c138af86752d68b6b6d534eaae3ae09ee87b99c51.
Not done: `FILTERED_EMPTY` on the dashboard is driven by the only collection the store filter genuinely scopes today (connection rows), because the KPI queries are not shop-scoped (#809's open item) — it fires when the selected store has no connection row left, not when a store has no products; that becomes real once the snapshot is per-shop. "Disconnected" is inferred from "ran, never a successful feed", not from a live reachability probe, so a source whose credentials broke *after* a good run reads as stale (#808) rather than disconnected — the two are distinct states and the copy does not conflate them. The onboarding wizard (`OnboardingPanel.ShowWizard`) is not launched from here; the empty state routes to the screens, it does not start the wizard. No UI test of the rendered panel (the projection is tested; the panel is a straight rendering of it).

## #812 — navigation sidebar collapse persistence (2026-09-13), branch codex/issue-812-sidebar-collapse-persistence off codex/issue-811-dashboard-zero-data-onboarding

New issue. DESIGN: the shell's sidebar was a fixed 214-DIP column with no way to narrow it, and no state of it survived a restart.

`NavigationSidebarState.cs` (new, pure): `NavigationSidebarState(Collapsed, ExpandedWidth)` with `NavigationSidebar.Parse`/`Serialize` (JSON under `UiPreferenceStore` key `shell:sidebar`), `Toggle`, `Resize` (clamped to 160–360 DIP; a drag while collapsed changes nothing, since nothing was dragged open), `CurrentWidth` (56 DIP when collapsed) and `Present(title, description, collapsed)`. The stored value is untrusted: garbage, a non-object, a non-numeric or out-of-band width all fall back to the **default** rather than being clamped into a width nobody chose. Widths are DIPs — the unit WPF lays out in — so a DPI change re-renders the same split instead of drifting it; there is no pixel conversion to drift through. In icons-only mode `Present` prints a one- or two-letter glyph built with **Turkish casing** ("Ürün yönetimi" → "ÜY", "İlk kurulum" → "İK", a one-word title gets one letter), while `ToolTip` and `AccessibleName` are the full title in both modes — the label leaves the screen, not the accessibility tree.

`MainWindow.xaml`: the sidebar column is named, a toggle button sits above the brand, and a `GridSplitter` on the column's right edge allows the resize the issue asks for. `MainWindow.Navigation.cs`: `ApplySidebarState()` is the single renderer (column width/min/max, splitter enabled, brand/footer/search visibility, group headers hidden when collapsed, every entry's content, tooltip and `AutomationProperties.Name` through `Present`), used by toggle, drag-complete and startup alike; `SaveSidebarState()` persists and logs rather than throws on a read-only profile; menu search now matches on the entry's label rather than on `Content`, which in icons-only mode is the glyph. `MainWindow.UX.cs`: Ctrl+B toggles.

`tests/MarketplaceHub.Tests/NavigationSidebarStateTests.cs` (new, 5, red first): restart through a real `UiPreferenceStore` restoring collapsed *and* the open width; eight garbage/hostile saved values all yielding a usable default; resize clamping and the collapsed no-op; a fractional DIP width round-tripping exactly with no pixel unit in the payload; and icons-only presentation keeping name and tooltip with Turkish-cased glyphs. `tests/MarketplaceHub.Tests/NavigationSidebarWindowTests.cs` (new, 1): the real shell — collapsing to 56 DIP, the search box hidden, every entry keeping its accessible name and tooltip and printing a glyph, group headers hidden, an icons-only entry taking real keyboard focus, and a second `MainWindow` over the same directory opening collapsed and expanding back to the width in use.

REAL_WORK_COUNT=1; FILES_CHANGED=NavigationSidebarState.cs (new), MainWindow.xaml, MainWindow.Navigation.cs, MainWindow.UX.cs, tests/MarketplaceHub.Tests/NavigationSidebarStateTests.cs (new), tests/MarketplaceHub.Tests/NavigationSidebarWindowTests.cs (new), IMPLEMENTATION_STATUS.md; TEST_RESULT=392 passed, 0 failed, 0 skipped (full Release suite, up from 386); Release build and self-contained win-x64 publish succeeded; artifact SHA-256 5ae8e6c66beb74dd5599520c73027fe5ff7459e00f2f7af5fdca5ede36b4bda8.
Not done: the glyph is initials, not a per-route icon glyph set — two routes can share initials ("Sipariş istisnaları"/"Sipariş yönetimi" both "Sİ"), which the tooltip and accessible name disambiguate but the eye does not; a real icon font is a design asset this issue does not include. The DPI claim rests on storing DIPs, not on a rendered test at 125%/200% (the test host has one scale). The splitter drag is persisted on `DragCompleted` only — a window-resize that squeezes the column is not written back, so the stored width is the last *chosen* one. The collapsed state is per data directory, so two profiles keep separate choices. No animation on toggle.

## #813 — breadcrumb semantics across workspaces (2026-09-13), branch codex/issue-813-workspace-breadcrumb-semantics off codex/issue-812-sidebar-collapse-persistence

New issue. DESIGN: #810 gave the dashboard a context-carrying trail, but every other way into a product, an order or a source — the global search, the saved views, the sidebar — still went through `Navigate(key)`, landed on a screen with nothing selected, and gave Back nothing to restore. And a crumb that named a product did not select it on landing (#810's own open item).

`WorkspaceLinks.cs` (new, pure): `monobridge://<kind>/<id>[?store=<key>]` is the one way to open an entity from anywhere, for the kinds this build has entities for — `product`→products, `order`→orders (`marketplace|shop|id`), `source`→xml. `Parse` treats the link as untrusted: the **route is always taken from the kind table, never from the link**; another scheme, an unknown kind (including `settings`, and `report`), an empty/oversized id, extra path segments, raw whitespace or control characters, `..` in any form, or any query key other than `store` yields no target at all rather than a partial one, and a title lookup that throws is not a reason to open a half-built target. `Format`/`Parse` round-trip composite ids and store keys through escaping. `FromSearchHit` turns a search hit into a link only when its type and route agree and it names something a workspace can select — an order hit without its store stays a plain screen jump.

`MainWindow.Navigation.cs`: `AllowedStoreKeys()` reads the **enabled connections on disk** (a failure to read allows nothing but the all-stores scope, so an unreadable store table cannot widen what a link opens); `OpenWorkspaceLink(target)` is the single entry point — the same `DrillThroughStack.Open` refusal as the dashboard, then `SelectRoute`, then `RevealEntity`; `RevealEntity` selects the product in the grid (re-searching by SKU when it is off the current page), the source in the list (selecting the instance the list holds, after the refresh that replaces them) and the order through the panel's own reveal; `Back_Click` now re-selects what the crumb names; `DrillEntityAlive` learned `source`. `OrdersPanel.cs`: `Create(..., exposeReveal)` hands back a reveal that reloads, clears the filters that would hide the order, selects it (selection opens the detail) and scrolls to it. `MainWindow.UX.cs`: a search hit that names an entity opens it on the trail; any other hit is the screen jump it always was. `DashboardPanel.cs`: quick cards are filtered through the window's real route table.

**Finding, recorded not hidden:** the shell registers no `"reports"` route. `ScreenParityAudit.RequiredRoutes` lists it (so the parity line logs BLOCKED at every startup), the dashboard's "Raporlar" quick card navigated to it (a silent no-op — `SelectRoute` returns when the key is missing), and `ReportAnalytics`/`ProductOrderReport` are only used inline in the product card. The issue's fourth workspace therefore does not exist as a workspace. This branch does **not** invent one: there is no `report` link kind (a test pins that), and the dead quick card is dropped by the route check instead of pretending. Registering a real reports workspace is its own issue.

`tests/MarketplaceHub.Tests/WorkspaceLinkTests.cs` (new, 6, red first): each kind parsing to its own route with its entity; format/parse round-trips of composite order ids, reserved characters and store keys; nineteen hostile or malformed links producing nothing; the route coming from the table for every kind; search-hit mapping (product, source, composite order; bare order, audit and type/route disagreement refused); and the report link refused. `tests/MarketplaceHub.Tests/WorkspaceLinkWindowTests.cs` (new, 3): through a real `MainWindow` seeded with two products, a source and one enabled + one disabled connection — a product link selecting the product and a source link selecting the source, then Back re-selecting the product; a crumb whose product was deleted degrading to the product screen with the name gone from the trail; and links naming `allegro|shop-b`, `ALLEGRO|shop-a` and `etsy|shop-a` all refused while `allegro|shop-a` opens.

REAL_WORK_COUNT=1; FILES_CHANGED=WorkspaceLinks.cs (new), MainWindow.Navigation.cs, MainWindow.UX.cs, OrdersPanel.cs, DashboardPanel.cs, tests/MarketplaceHub.Tests/WorkspaceLinkTests.cs (new), tests/MarketplaceHub.Tests/WorkspaceLinkWindowTests.cs (new), IMPLEMENTATION_STATUS.md; TEST_RESULT=401 passed, 0 failed, 0 skipped (full Release suite, up from 392); Release build and self-contained win-x64 publish succeeded; artifact SHA-256 758d1aed2b7551e46c8a86aeaf5d54e79103e3371235445fde5b60bc9ff47ba6.
Not done: no reports workspace, as above — the `"reports"` route is missing from the shell and this branch records it rather than registering a placeholder. The search index's order hits carry only the order id in `TargetId` (marketplace and shop are in the display text), so an order found through global search still opens the orders screen without selecting the row; `FromSearchHit` is ready for a composite id the moment the index writes one, and that is an index change, not a navigation one. The order reveal is not covered by a window test (the panel's grid is private to its closure; the reveal path is exercised through `RevealEntity` only for product and source). Links are opened from code paths, not from a registered URI scheme — there is no OS-level `monobridge://` handler. Revealing a product that is off the current page rewrites the product search box to its SKU, which is visible and reversible but is a side effect the operator did not type.

## #814 — global toast queue policy (2026-09-13), branch codex/issue-814-toast-queue-policy off codex/issue-813-workspace-breadcrumb-semantics

New issue. DESIGN: the app had no toast at all — `Log(text)` wrote the status line, the history list, the log file and an audit row, with no severity, no lifetime, no dedupe and no way to dismiss; an error and a "kaydedildi" looked the same and both vanished under the next line.

`NotificationPolicy.cs` (new, pure): `NotificationSeverity`, `NotificationRequest`, `Toast` and `NotificationQueue`, driven by an injected clock. A severity owns its lifetime (`DurationFor`: success 4 s < info 6 s < warning 10 s; **error has none — it waits for a person**). The same text at the same severity inside a 30-second window is **one toast with a count**, refreshed rather than re-stacked; the same text at another severity is different news. At most **3** are on screen; the rest wait in arrival order and are promoted as slots free up, a promoted toast's clock starting when it is shown; the waiting line has a ceiling of 50 that drops the oldest *non-error* and counts the loss — an error is never the one dropped. `Expire(now)`, `Dismiss(id)`, `DismissTop()` (Escape closes what appeared last) and `DismissAll()` (the pending ones too). Every text goes through `AuditStore.Sanitize` (#791: bearer/basic auth, query and key=value secrets, e-mails, profile paths), then loses newlines and is capped at 240 characters; an empty text becomes a generic sentence rather than an empty box.

`MainWindow.xaml`: a `ToastHost` overlay (top-right over the module tabs, tab-cycle, live region). `MainWindow.Notifications.cs` (new partial): `Log(text, severity)` does everything `Log(text)` does **and** raises a toast; `Notify(request)` raises one with an optional action that navigates through the ordinary route (only when the route exists); `RenderToasts` draws glyph + word + text + `×N` + a focusable close button per toast, sets an automation name that includes the count, and marks errors assertive; a one-second `DispatcherTimer` expires and promotes, and stops itself when the host is empty. `Log(text)` is unchanged for its fifty callers (Info stays on the status line — a toast for every informational line would be the wall the issue is against). The **twelve error-path call sites** (`Log(Safe(e))` and the search-index one) now publish at `Error`, so real failures reach the toast host without touching the global exception handlers (which still do not set `Handled`). `MainWindow.UX.cs`: Escape dismisses the newest toast **only while the keyboard is inside the host**, so the product-inspect drawer keeps its own Escape.

`tests/MarketplaceHub.Tests/NotificationPolicyTests.cs` (new, 7, red first): a burst of twenty showing three and queueing seventeen in order with nothing dropped; a triple duplicate collapsing to one toast with count 3 and a refreshed clock, a different severity and an out-of-window repeat both being fresh; per-severity clocks with the error surviving an hour and going only on dismiss; expiry promoting the waiting line with fresh clocks; keyboard dismiss removing the newest and letting the next one in, dismiss-all clearing the pending line; secret, e-mail, profile path and newlines never reaching the text, a 5000-character text capped with an ellipsis, a blank text replaced; and the pending overflow dropping the oldest non-error, counting it and never the error. `tests/MarketplaceHub.Tests/NotificationWindowTests.cs` (new, 1): the real shell — an error published through `Log(text, Error)` appearing in the host with a sanitized automation name, its repeat showing "2 kez" instead of a second toast, Escape with focus inside the host closing the newest (a success) and leaving the error, and the error's close button emptying and collapsing the host.

REAL_WORK_COUNT=1; FILES_CHANGED=NotificationPolicy.cs (new), MainWindow.Notifications.cs (new), MainWindow.xaml, MainWindow.UX.cs, MainWindow.Filters.cs, MainWindow.ProductEditing.cs, MainWindow.xaml.cs, tests/MarketplaceHub.Tests/NotificationPolicyTests.cs (new), tests/MarketplaceHub.Tests/NotificationWindowTests.cs (new), IMPLEMENTATION_STATUS.md; TEST_RESULT=409 passed, 0 failed, 0 skipped (full Release suite, up from 401); Release build and self-contained win-x64 publish succeeded; artifact SHA-256 cf4728cca4965185d44ce8d724b8d22f432c703f5a0b0864d7db217ea1736779.
Not done: the panels (`OrdersPanel`, `SyncPanel`, the channel panels…) still report through their own status `TextBlock`s and `MessageBox.Show` (twenty-odd sites) — they do not have a handle to the window's queue; routing them through `Notify` needs the panels to receive a publisher, which is #816's error-banner work and the natural moment to do it. No toast has an action yet in production (`Notify` supports one; no caller passes one). The dedupe key is exact text, so two errors that differ only by an id stack separately, which is arguably right and arguably noisy. No animation, no hover-pause of the clock, and no "N more waiting" indicator on the host — the pending count exists on the queue but is not drawn.

## #815 — status tooltip content standard (2026-09-13), branch codex/issue-815-status-tooltip-standard off codex/issue-814-toast-queue-policy

New issue. DESIGN: status tooltips were ad hoc — the product row (#794) printed badge + reason + stock, the dashboard's connection rows and the API-health rows had none, and nothing said when a state last changed, where it came from, or what to do about it. There was also no rule against a tooltip carrying a raw response body.

`StatusTooltip.cs` (new, pure): `StatusTooltipContent(Status, Reason, LastChangeUtc, Source, NextAction, Detail)` and `Compose(content, nowUtc)` — one labelled line per field, in that order, **each hidden when empty** (no "—" placeholder lines; an empty status still says "bilinmiyor"). The reason is a sentence: newlines collapse, it is capped at 240 characters with an ellipsis, and a reason that `LooksLikeRawPayload` (a JSON or XML body, a stack trace) is replaced by "Ham yanıt gizlendi; ayrıntı için Tanılama ekranına bakın." rather than shown. `Relative` gives "az önce / N dk önce / N sa önce / N gün önce" (a future timestamp says "gelecekte" rather than a negative number) next to the absolute local time in the operator's culture. `NextActionFor(stateKey)` is the shared next step for pending/error/stale/**unsupported**/warning/disabled, so a surface with nothing more specific to say still points somewhere — and "unsupported" says so instead of suggesting a retry that cannot work. The assembled text passes `AuditStore.Sanitize` last (bearer/basic auth, query and key=value secrets, e-mails, profile paths), then is capped at 400.

Owners wired to the template: `ProductRowState.Tooltip` now composes through it (badge as status, reason, `UpdatedUtc` as last change, the source record as source, stock as detail, the shared next step) — the existing test surface for #794 keeps passing; `MainWindow.Filters.cs` sets `ToolTipService.ShowsToolTipOnKeyboardFocus` and `AutomationProperties.HelpText` on every product row so the tooltip opens from the keyboard and is read by assistive tech; `DashboardPanel.cs` projects a `Tooltip` per connection row (status, last error, last test, `channel / shop`, a retest step unless connected) bound through a `DataGridRow` style with the same two properties; `ApiHealthPanel.cs` does the same in `LoadingRow` with a next step per health state (`LIVE_API_BLOCKED` reads as unsupported, `RATE_LIMITED` says wait, `AUTH_ERROR` says renew).

`tests/MarketplaceHub.Tests/StatusTooltipTests.cs` (new, 6, red first): pending/error/stale/unsupported each composing with their own distinct next step; empty fields hidden and an empty status still a line; a 20-line reason becoming one capped sentence; five raw payloads (JSON object, JSON array, XML declaration, XML fragment, stack trace) hidden while a sentence that merely mentions a token is kept, and a bearer token, e-mail, profile path and query secret never surviving whichever field they were in; relative + absolute last-change in the current culture with the future guard; and the product row tooltip starting with the badge, carrying reason, stock and a next step. `tests/MarketplaceHub.Tests/StatusTooltipWindowTests.cs` (new, 1): a real `MainWindow` with a disabled product — the realized row's tooltip is the template, `ShowsToolTipOnKeyboardFocus` is on, `HelpText` equals the tooltip, and the row takes keyboard focus.

REAL_WORK_COUNT=1; FILES_CHANGED=StatusTooltip.cs (new), ProductRowState.cs, MainWindow.Filters.cs, DashboardPanel.cs, ApiHealthPanel.cs, tests/MarketplaceHub.Tests/StatusTooltipTests.cs (new), tests/MarketplaceHub.Tests/StatusTooltipWindowTests.cs (new), IMPLEMENTATION_STATUS.md; TEST_RESULT=416 passed, 0 failed, 0 skipped (full Release suite, up from 409); Release build and self-contained win-x64 publish succeeded; artifact SHA-256 77a144bda912a7049e86464a88fa975e3a1415abd99925e0167b7d1b0c1ccd18.
Not done: the sync-job grid and the channel panels' listing grids still have no row tooltip — they are the next owners to route through `Compose`, each a `LoadingRow` handler like the API-health one; the product row's "source" is the source **id** ("Kaynak kaydı <id>"), because the row classifier has no store handle to resolve the name and #800 already shows the name on the card; `LooksLikeRawPayload` is a heuristic (leading brace/bracket with a colon, XML, stack frames, two or more JSON keys) and a prose reason that happens to open with "[" would be hidden — the diagnostics pointer keeps that harmless; the keyboard-focus claim rests on the WPF property being set and the row taking focus, not on a rendered popup, which the test host cannot observe.
