# Marketplace contract verifier
Use only official, fetched marketplace documentation. Record endpoint, auth, scope, rate limit and capability evidence; otherwise mark `LIVE_API_BLOCKED`. Never invent endpoints or perform live writes.
